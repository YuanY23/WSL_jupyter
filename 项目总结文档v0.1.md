# SimLab / simhub 项目总结报告 v0.1

## 1. 项目概述

SimLab / simhub 是一个面向工程教育、原理仿真和 Notebook 作业评测的一体化在线平台。项目以 JupyterHub 作为多用户入口，以 DockerSpawner 为每个用户启动隔离的 JupyterLab 工作环境，以定制版 JupyterLab、定制版 nbgrader 和多套自研 JupyterLab 扩展共同组成完整的“学习、仿真、作业、评测、反馈”闭环。

从项目文件看，平台的核心目标并不是简单部署一个 JupyterHub，而是在 Jupyter 生态之上构建一个更适合教学的工程仿真系统。它把传统 Notebook 的透明代码、可复现实验、Python 科学计算能力，与图形化参数输入、可视化仿真模板、课程分组、作业分发、自动评测、中文界面和品牌化界面结合起来，使学生可以通过图形界面快速进入物理建模与数值计算，同时保留底层代码可查看、可修改、可学习的特性。

项目当前包含以下几类关键能力：

- 多用户在线实验环境：基于 JupyterHub + DockerSpawner，为教师和学生提供独立 JupyterLab 容器。
- 工程仿真能力：内置传热仿真平台、通用仿真平台、官方热力建模示例。
- 低代码交互能力：通过参数自动绑定插件，将 Notebook 参数层代码自动映射为滑块、数字框、下拉框、开关等控件。
- 作业评测能力：深度定制 nbgrader，实现课程化作业分发、回收、自动评分和手动评分。
- 课程权限能力：自定义注册页、自定义认证器、教师/学生角色、课程组、动态目录挂载。
- 中文与品牌体验：JupyterHub 模板、JupyterLab 菜单、nbgrader 页面、图标和 favicon 均进行了中文化与 SimLab 品牌化定制。

## 2. 项目目录结构说明

项目根目录是一个组合式工程仓库，既包含上游框架源码，也包含平台自研模块和部署配置。

| 路径 | 作用 |
| --- | --- |
| `jupyterhub_config.py` | JupyterHub 主配置，定义认证器、DockerSpawner、用户卷、课程目录挂载、服务 token、RBAC 权限和模板路径。 |
| `custom_authenticator.py` | 自定义 NativeAuthenticator，负责注册页面字段解析、中文提示、教师/学生角色分配和课程组创建。 |
| `Dockerfile-nbgrader` | 平台主镜像构建文件，安装 JupyterLab、nbgrader、自研扩展、科学计算库、中文字体和中文语言包。 |
| `docker_nbgrader_config.py` | 容器内 nbgrader 配置，启用 JupyterHubAuthPlugin、共享 exchange 目录、多课程路径和教师课程根目录。 |
| `docker_jupyter_server_config.py` | 容器内 Jupyter Server 配置，根据是否为教师容器禁用教师专属 server extension。 |
| `disable_teacher_extensions.sh` | 容器启动前脚本，学生容器自动禁用 Formgrader 与创建作业等教师前端扩展，并清理 Matplotlib 字体缓存。 |
| `templates/` | JupyterHub 登录、注册、主页和启动等待页面模板，已中文化和品牌化。 |
| `logo/` | SimLab favicon、主 Logo、Notebook、文件夹、收藏文件夹、普通文件图标。 |
| `jupyterlab/` | 定制版 JupyterLab 源码树，主要用于品牌图标、菜单与界面定制，并在 Docker 构建中编译安装。 |
| `nbgrader/` | 定制版 nbgrader 源码树，包含 JupyterHub 鉴权、Formgrader API、Lab 菜单和中文 UI 改造。 |
| `jupyterlab-param-binding/` | 自研 JupyterLab 参数自动绑定插件。 |
| `jupyterlab-thermal-design/` | 自研传热仿真平台插件，包含前端控制面板、Notebook 生成器和 Python 热求解器。 |
| `jupyterlab-simulation-platform/` | 自研通用仿真模板生成器插件。 |
| `jupyterlab-official-thermal-examples/` | 自研官方热力建模示例插件，当前核心示例为槽式太阳能光热发电过程。 |
| `courses/`、`course_test/` | 课程与测试数据目录，包含 gradebook 数据库、source/release 作业样例和 Notebook 样例。 |
| `tools/tests/` | 顶层 UI 文案、菜单、构建产物和品牌资源校验测试。 |

这个结构表明项目不是单一插件，而是一个完整 Jupyter 教学平台发行版：底层服务、容器镜像、认证系统、JupyterLab 前端、Notebook 生成器、科学计算求解器、作业评测系统和 UI 品牌系统都在同一个仓库中协同维护。

## 3. 总体技术架构

项目整体架构可以分为五层。

### 3.1 用户入口层

用户通过浏览器访问 JupyterHub。JupyterHub 负责登录、注册、主页、服务启动和路由入口。项目定制了 `templates/native-login.html`、`templates/signup.html`、`templates/home.html` 和 `templates/spawn_pending.html`，使入口页面更接近“原理仿真系统”产品，而不是默认 JupyterHub 管理界面。

### 3.2 认证与课程组织层

认证基于 `nativeauthenticator`，但项目通过 `CustomNativeAuthenticator` 做了扩展：

- 注册页面增加角色字段和课程字段。
- 教师注册时自动加入 `formgrade-{course_id}` 课程教师组和全局 `teachers` 组。
- 学生注册时自动加入 `nbgrader-{course_id}` 学生组。
- JupyterHub RBAC 为 `teachers` 组授予 `list:users`、`read:users`、`read:groups`、`list:groups`、`admin:groups`、`access:services` 等权限。
- 注册和错误提示已中文化，降低真实教学环境中的理解成本。

这套设计把“账号身份”和“课程身份”绑定在注册流程中，后续 DockerSpawner 挂载、nbgrader 权限和学生名单同步都围绕课程组自动展开。

### 3.3 容器与运行环境层

平台使用 DockerSpawner 为每个用户创建独立容器。主镜像基于 `jupyter/base-notebook:ubuntu-22.04`，并安装：

- JupyterLab `4.2.7`
- Notebook `7.2.2`
- Jupyter Server `2.14.2`
- JupyterHub
- nbgrader `0.9.5` 定制版
- Python 科学计算库：NumPy、SciPy、Matplotlib、Numba
- 数据库/迁移依赖：SQLAlchemy、Alembic
- HTTP 与工具依赖：requests、rapidfuzz、pytest
- 中文字体：`fonts-wqy-zenhei`
- JupyterLab 中文语言包：`jupyterlab-language-pack-zh-CN`

容器卷挂载策略分为基础卷和开发卷：

- 每个用户有独立持久化 home 卷：`jupyterhub-user-{username}`。
- 所有用户共享 nbgrader 交换目录：`/srv/nbgrader/exchange`。
- 教师容器会根据 `formgrade-{course_id}` 组动态挂载宿主机课程目录到 `/home/jovyan/{course_id}`。
- 设置 `SIMLAB_DEV_MOUNTS=1` 时，会额外挂载本地源码目录，便于开发调试扩展。

### 3.4 JupyterLab 工作台层

容器内的 JupyterLab 是主要工作界面。项目在 JupyterLab 中加入了多个主菜单：

- `通用仿真平台`，菜单 rank 为 5。
- `传热仿真平台`，菜单 rank 为 6。
- `官方热力建模示例`，菜单 rank 为 7。
- `作业评测平台`，菜单 rank 为 8。

同时，项目对 JupyterLab 进行了品牌化替换：

- favicon 替换为 `simlab-favicon.svg`。
- 主 Jupyter 图标和 wordmark 替换为 `simlab-logo.svg`。
- Notebook、文件夹、收藏文件夹、普通文件图标替换为 SimLab 风格 SVG。
- 顶层 UI 测试会检查这些替换是否进入源码与构建产物，避免只改源码但静态 bundle 未更新。

### 3.5 作业评测与数据闭环层

nbgrader 负责作业创建、分发、学生拉取、提交、回收、自动评分、反馈生成和手动评分。项目对 nbgrader 做了适配 JupyterHub + DockerSpawner + 多课程环境的深度改造：

- 使用 JupyterHub group 判断课程与权限。
- 交换区开启 `path_includes_course = True`，不同课程的数据路径隔离。
- 教师容器根据 `NBGRADER_COURSE_ID` 自动设置 `CourseDirectory.course_id` 和课程根目录。
- 学生容器禁用教师端 server extension 与前端 Lab extension。
- Formgrader 页面和 Lab 菜单中文化为“作业评测平台”“作业管理”“手动评分”“学生管理”等。

## 4. 使用的主要技术与框架

### 4.1 后端与平台技术

| 技术 | 在项目中的用途 |
| --- | --- |
| Python | JupyterHub 配置、认证器扩展、nbgrader 后端、传热求解器、Notebook 运行代码。 |
| JupyterHub | 多用户登录、认证、服务路由、用户服务器管理、RBAC 权限控制。 |
| NativeAuthenticator | 本地用户名密码注册与登录基础，项目在其上扩展注册字段和组管理。 |
| DockerSpawner | 为每个用户动态创建隔离的 JupyterLab 容器。 |
| Jupyter Server | 单用户容器内的 Notebook / Lab 后端服务。 |
| nbgrader | Notebook 作业分发、收取、自动评分、反馈和 Formgrader UI。 |
| SQLAlchemy / Alembic | nbgrader gradebook 和 JupyterHub 数据库相关能力。 |
| requests | 在 nbgrader 与认证器中调用 JupyterHub API，实现组和学生同步。 |
| NumPy | 数值数组、矩阵方程、有限差分、时间序列计算。 |
| SciPy | 通用仿真中的优化调度模板使用 `scipy.optimize.linprog`。 |
| Matplotlib | 生成 Notebook 中的温度分布、动态响应、能量曲线、调度曲线等图表。 |
| Numba | 镜像中安装，作为后续高性能数值计算扩展的基础依赖。 |

### 4.2 前端与扩展技术

| 技术 | 在项目中的用途 |
| --- | --- |
| TypeScript | 四个自研 JupyterLab 扩展、nbgrader Lab 扩展、JupyterLab 前端代码。 |
| React 18 | 参数绑定侧栏、仿真平台控制面板、官方热力示例界面等自研 UI。 |
| JupyterLab Extension API | 注册命令、主菜单、左侧栏、主工作区 Widget、命令面板和布局恢复。 |
| Lumino Widgets | JupyterLab 插件底层 Widget、MainAreaWidget、Menu、WidgetTracker。 |
| JupyterLab Notebook API | 读取 Notebook cells、修改 cell source、执行 cell 与下游 cells。 |
| JupyterLab Contents API | 生成 `.ipynb` 文件并保存到 `simulation-platform-results` 目录。 |
| ECharts / echarts-for-react | 传热平台依赖中包含，用于面向工程数据可视化的扩展基础。 |
| CSS | JupyterHub 页面、JupyterLab 扩展面板、nbgrader 页面和品牌 UI 样式。 |

### 4.3 构建、测试与工程化技术

| 技术 | 在项目中的用途 |
| --- | --- |
| jlpm / Yarn | JupyterLab 生态默认包管理与扩展构建。 |
| npm scripts | 自研扩展的 `build`、`build:prod`、`test`、`watch` 等脚本。 |
| TypeScript Compiler | 将扩展源码编译到 `lib/`。 |
| `@jupyterlab/builder` | 构建 prebuilt JupyterLab extension。 |
| setuptools / setup.py | 将自研扩展作为 Python 包安装，并把 labextension 静态资源安装到 Jupyter 目录。 |
| Hatch / hatch-jupyter-builder | JupyterLab 与 nbgrader 上游工程使用的 Python/JS 构建系统。 |
| Playwright / Galata | nbgrader 与 JupyterLab 上游 UI 测试框架基础。 |
| Node assert 测试 | 项目自研测试使用 Node 内置 assert 验证生成器、参数绑定和 UI 文案。 |
| Docker | 统一构建包含 JupyterLab、nbgrader、自研插件和科学计算库的运行镜像。 |

## 5. 自研 JupyterLab 扩展系统

项目中最重要的业务创新集中在四个自研 JupyterLab 扩展。

### 5.1 参数自动绑定插件：`jupyterlab-param-binding`

这是整个交互式仿真体验的基础插件。它把 Notebook 中的“参数层代码”变成可操作的图形控件。

核心机制如下：

1. 扫描当前 Notebook 的 Markdown 标题。
2. 当标题中出现“参数层代码”时，进入参数区域。
3. 在该区域内读取代码单元格，解析普通 Python 赋值语句。
4. 支持解析数值、布尔值、字符串三类基础字面量，例如 `H = 0.01`、`enabled = True`、`label = "case-a"`。
5. 根据参数类型自动推断控件类型，数值参数可渲染为数字框或滑块，布尔参数为开关，字符串参数为文本或下拉。
6. 控件配置写入 Notebook metadata 的 `simulation_param_bindings` 字段，实现随 Notebook 保存、分发和复用。
7. 用户修改控件时，插件精准改写对应代码行，而不是隐藏计算逻辑或维护另一套参数状态。
8. 如果启用自动计算，插件会从参数层起始单元格开始执行下游所有单元格。

这一设计的关键价值在于：GUI 并没有替代 Notebook 代码，而是成为 Notebook 代码的交互外壳。学生既能拖动滑块快速观察物理结果，也能打开参数层、模型层和求解层代码理解背后计算过程。

插件中还包含多项鲁棒性处理：

- 数值滑块自动推断 `min`、`max`、`step`。
- 对异常范围配置进行修正，避免 `max <= min` 或 `step <= 0`。
- 限制滑块范围步数，避免浏览器渲染过多离散点。
- 修改参数时通过 cell id 优先定位，cell id 不存在时回退到 cell index。
- Kernel 断开、重连或 busy 时在侧栏提示状态。
- 支持手动“运行下游”和自动“计算下游”两种模式。

### 5.2 传热仿真平台：`jupyterlab-thermal-design`

传热仿真平台是面向传热学课程的专用工作台。它提供图形化控制面板，用户选择物理场景并输入参数后，系统生成结构完整、可运行、代码可见的 Notebook。

该插件覆盖四大类、十四个传热场景：

| 类别 | 场景 |
| --- | --- |
| 稳态导热 | 无限大平板导热、多层复合平板导热、圆筒壁径向导热、等截面直肋导热、平壁内热源稳态导热、矩形平板二维稳态导热 |
| 瞬态导热 | 两侧恒温加热/冷却、一侧恒定热流 |
| 对流换热 | 平板层流强制对流、平板湍流强制对流、竖板自然对流、圆管内强迫对流 |
| 热辐射 | 平行平板辐射换热、三表面封闭空腔辐射 |

项目不仅写了前端生成器，还提供了 Python `thermal_solver` 包。求解器采用统一入口 `run_simulation(scenario_id, params)`，内部通过场景路由调用具体模型，并以 `===THERMAL_OUTPUT_START===` 和 `===THERMAL_OUTPUT_END===` 包裹 JSON 输出。这种输出协议便于前端或 Notebook 捕获结构化结果。

传热模型中使用的主要数值与工程方法包括：

- 有限差分法 FDM：一维稳态平板、圆筒壁径向导热、二维矩形板稳态导热等。
- 有限体积法 FVM：多层复合平板、等截面直肋、一侧恒定热流瞬态导热等。
- 显式 FTCS 时间推进：两侧恒温平板瞬态导热。
- 隐式格式：一侧恒定热流瞬态问题，用于提高时间推进稳定性。
- 解析解对照：平板导热、圆筒壁导热、内热源平壁等场景会计算解析结果或工程指标。
- 工程经验关联式：平板层流/湍流强制对流、Churchill-Chu 竖板自然对流、圆管内 Dittus-Boelter 等。
- 辐射网络法：平行灰体平板、含重辐射面的三表面空腔辐射。

生成的 Notebook 会包含问题描述、模型假设、参数说明、数学模型、参数层代码、模型层代码、求解层代码、可视化代码、关键结果输出、修改提示和分析提示。传热平台与参数绑定插件联动后，学生可以对厚度、导热系数、温度、发射率、雷诺数相关参数等进行滑块式探索。

### 5.3 通用仿真平台：`jupyterlab-simulation-platform`

通用仿真平台定位为“可见代码”的模板化 Notebook 生成器。它不局限于传热学，而是覆盖常见工程数学模型和能源系统分析场景。

当前内置七类模板：

| 模板 | 典型用途 | 主要方法 |
| --- | --- | --- |
| 代数方程 / 经验公式 | 光伏组件输出功率、经验公式计算 | 公式解析、参数扫描、曲线/柱状图 |
| 一阶动态系统 | 储能 SOC、简单状态方程 | 显式欧拉法 |
| 二阶动态系统 | 弹簧-质量-阻尼、振动响应 | 二阶 ODE 转一阶方程组，显式欧拉 |
| 线性方程组 / 网络平衡 | 热、电、能量节点平衡 | `numpy.linalg.solve` |
| 一维传热 / 扩散 | 稳态或瞬态一维扩散问题 | FDM、显式时间推进 |
| 时序能量平衡 | 光伏、储能、负荷逐时运行 | 规则驱动的能量流计算 |
| 简单优化调度 | 分时电价下储能调度 | `scipy.optimize.linprog` 线性规划 |

该插件有三点值得注意：

第一，模板配置是类型化的。`templates/types.ts` 定义了每类配置的 TypeScript interface，`registry.ts` 提供默认参数和校验逻辑，避免用户输入无效公式、无效矩阵或不一致的时间序列。

第二，生成器强调“可见代码”。测试明确检查生成结果不能隐藏在 `run_simulation(params)` 这种黑盒调用后面，而是把参数、模型、求解和可视化代码直接写入 Notebook。

第三，生成器自动写入 `simulation_param_bindings` metadata。这样通用仿真平台生成的 Notebook 一打开就能被参数自动绑定插件识别，直接在左侧栏出现滑块或控件。

### 5.4 官方热力建模示例：`jupyterlab-official-thermal-examples`

官方热力建模示例插件提供更完整、更接近工业流程的示范案例。当前核心案例为“槽式太阳能光热发电集热-储热-发电联合过程”。

该示例围绕 24 小时太阳直接辐射 DNI 序列，构建从太阳输入到发电输出的完整 Process 模型链：

1. 典型日照辐射谱输入：夏季晴天、冬季晴天、多云波动日、阴天低辐射日。
2. 槽式集热场：根据集热面积、光学效率、接收器热损失计算有效热功率。
3. 油盐换热：通过换热效率将集热侧能量转入熔盐或发电子系统。
4. 双罐熔盐储热：热盐罐和冷盐罐液位按小时动态更新。
5. 汽轮机发电：根据额定电功率、热效率和可用热量计算发电。
6. 运行策略切换：支持可用热量优先满发、白天优先发电、晚高峰优先发电。
7. 模式时间轴：记录太阳直供、充热、放热、待机、弃热等运行模式。

它同样生成可运行 Notebook，并写入完整参数绑定 metadata。参数包括太阳辐射谱、调度策略、集热场面积、光学效率、接收器热损失、储热罐体积、热盐温度、冷盐温度、熔盐密度、熔盐比热、初始热盐罐液位、汽轮机额定功率、热电效率和换热效率。

这个插件的教学意义在于，它展示了从单一物理公式到系统级工程过程的跨越。学生不只是看某个换热公式，而是能观察能源输入、储热状态、调度策略和发电输出之间的耦合关系。

## 6. nbgrader 与 JupyterHub 的深度集成

项目对 nbgrader 的改造是平台能否用于真实课程的重要基础。

### 6.1 双 Token 鉴权设计

在 DockerSpawner 环境中，JupyterHub 单用户容器依赖 `JUPYTERHUB_API_TOKEN` 完成自身 OAuth 授权。如果 nbgrader 为了管理组和用户直接覆盖这个 token，可能破坏 JupyterHub 单用户服务的认证流程。

项目采用单独的 `NBGRADER_ADMIN_API_TOKEN` 传递 nbgrader 管理权限：

- `jupyterhub_config.py` 中创建 `nbgrader` service，并生成 admin API token。
- 教师容器启动时通过环境变量注入 `NBGRADER_ADMIN_API_TOKEN`。
- `nbgrader/nbgrader/auth/jupyterhub.py` 优先读取 `NBGRADER_ADMIN_API_TOKEN`，没有时才回退到 `JUPYTERHUB_API_TOKEN`。

这个改动避免了“评测系统管理权限”和“用户容器自身登录权限”互相污染，是 JupyterHub + DockerSpawner + nbgrader 集成中的关键稳定性设计。

### 6.2 课程组与学生名单自动同步

传统 nbgrader 需要教师手动维护 gradebook 中的学生名单。项目改造了 Formgrader API：

- `StudentCollectionHandler.get()` 会读取 `NBGRADER_COURSE_ID`。
- 根据课程 ID 请求 JupyterHub API：`/groups/nbgrader-{course_id}`。
- 将组内用户自动写入当前课程的 `gradebook.db`。
- `StudentHandler.put()` 在教师新增或更新学生时，也会把学生同步加入 JupyterHub 的 `nbgrader-{course_id}` 组。

这样教师打开学生管理页面时，学生列表会从 Hub 课程组自动同步到 nbgrader 数据库，减少重复维护，也避免 Hub 组和 gradebook 不一致。

### 6.3 教师与学生容器差异化

平台通过多处配置区分教师和学生：

- 教师属于 `formgrade-{course_id}` 和 `teachers` 组。
- 教师启动容器时挂载课程目录，并设置 `NBGRADER_COURSE_ID`。
- 学生容器没有 `NBGRADER_COURSE_ID`，因此 `docker_jupyter_server_config.py` 会禁用 Formgrader 和 Course List server extension。
- `disable_teacher_extensions.sh` 会为学生容器写入 `page_config.json`，禁用 `@jupyter/nbgrader:formgrader` 和 `@jupyter/nbgrader:create-assignment`。

这一方案既保留学生拉取与提交作业的能力，又避免学生看到或调用教师端的创建作业、管理作业和评分入口。

### 6.4 nbgrader UI 中文化与兼容性

项目把 nbgrader Lab 菜单改为“作业评测平台”，并将 Formgrader 模板中的页面标题、导航、按钮、表格文案、验证提示、关闭按钮 aria-label 等大量文案中文化。

同时，Formgrader 静态依赖中 Bootstrap 固定到 `3.4.1`，避免新版本 Bootstrap 与原有 Formgrader 页面结构不兼容导致布局问题。

## 7. JupyterLab 定制与品牌化

项目把 JupyterLab 从“开发者工具界面”改造成更像教学产品的工作台：

- 自研扩展加入主菜单，形成清晰的业务入口顺序。
- 默认菜单中部分不适合教学场景的入口被测试约束为禁用状态，例如 View、Kernel、Tabs、Help。
- 文件浏览器图标改为 SimLab 风格，Notebook、文件夹、收藏文件夹和普通文件更有辨识度。
- JupyterHub 登录/注册/主页使用极简黑白风格，并统一 favicon。
- Docker 构建中不仅替换源码图标，还复制到 JupyterLab 静态目录和 labextension 安装目录。
- 构建后通过 `grep` 检查中文菜单、菜单 rank、静态 bundle 是否包含预期内容，避免构建产物与源码不一致。

这种品牌化并不是单纯美化，而是服务于教学平台的产品化：学生和教师看到的是“原理仿真系统”“通用仿真平台”“传热仿真平台”“作业评测平台”，而不是多个互不关联的开源插件。

## 8. 项目创新点

### 8.1 代码可见的低代码仿真交互

很多教学仿真系统会走向两个极端：一种是纯代码 Notebook，透明但门槛高；另一种是封闭 GUI，易用但学生看不到模型细节。该项目选择了第三条路径：GUI 控件直接绑定 Notebook 参数层代码。

用户拖动滑块时，系统实际修改的是 Notebook 中的 Python 赋值语句，并重新执行下游模型、求解和绘图单元。学生既能获得低门槛交互体验，也能看到真实计算代码。这对工程教育很有价值，因为它把“操作结果”和“模型逻辑”放在同一个学习对象中。

### 8.2 仿真模板与参数绑定 metadata 的统一协议

传热平台、通用仿真平台和官方示例都会生成 `simulation_param_bindings` metadata，参数绑定插件则独立读取这一协议。也就是说，模板生成器和交互侧栏之间不是硬编码耦合，而是通过 Notebook metadata 协议连接。

这带来两个好处：

- 新增仿真模板时，只要写入同样的 metadata，就能自动获得左侧参数交互能力。
- 已有 Notebook 只要按“参数层代码”结构组织，也能被插件扫描并半自动生成控件。

### 8.3 面向课程的 JupyterHub / nbgrader 自动组网

项目把课程 ID 贯穿注册、用户组、Docker 卷挂载、nbgrader course_id、exchange 路径和 gradebook 同步：

- 教师注册选择课程后自动拥有课程工作目录。
- 学生注册选择课程后进入课程学生组。
- 教师打开 Formgrader 后自动同步学生名单。
- nbgrader exchange 按课程路径隔离。
- 学生端自动隐藏教师功能。

这种自动组网能力显著降低教师和运维人员使用 nbgrader 的门槛，尤其适合多课程、多班级、多教师的实验教学场景。

### 8.4 传热学专用模型库与教学化 Notebook 生成

传热平台不是简单画几个图，而是把传热学中常见的导热、对流、辐射问题做成可解释的 Notebook。每个场景不只输出数值，还输出控制方程、边界条件、离散方法、工程指标和分析提示。

这使平台具备“教学解释”的能力：学生不仅得到结果，还能看到为什么这样建模、如何离散、误差如何验证、哪个参数影响结果。

### 8.5 从单点物理模型扩展到系统级能源过程

官方 CSP 示例展示了系统级 Process 建模能力。它把辐射输入、集热、换热、储热、发电、调度状态机和逐时结果放在一个 Notebook 中，形成从单一课程公式到工程系统仿真的过渡。

这类示例能帮助学生理解工程系统中的耦合：一个参数改变不只影响一条曲线，而会改变储热水平、发电时段、弃热量、日效率和运行模式。

### 8.6 镜像构建中的产物一致性校验

Dockerfile 中对多个关键字符串进行构建期检查，例如：

- nbgrader 静态 bundle 必须包含“作业评测平台”。
- nbgrader 菜单必须按 rank 8 注册。
- 通用仿真平台、传热仿真平台、官方热力建模示例的菜单 rank 必须进入静态 bundle。
- SimLab 图标必须复制到 JupyterLab 静态资源位置。

这种做法把“源码改了但构建产物没更新”的常见问题提前暴露在镜像构建阶段，提高发布可靠性。

## 9. 测试与质量保障

项目包含多类测试，覆盖核心业务逻辑和构建产物。

| 测试文件 | 覆盖内容 |
| --- | --- |
| `jupyterlab-param-binding/tests/param-binding.test.js` | 参数区域扫描、赋值更新、metadata 清理、数值范围修正、滑块步数限制、配置面板状态。 |
| `jupyterlab-simulation-platform/tests/notebook-generators.test.js` | 七类通用模板生成、Notebook 结构、参数绑定 metadata、公式变量校验、文件名安全。 |
| `jupyterlab-thermal-design/tests/notebook-generator.test.js` | 传热 Notebook 参数层合并、滑块范围覆盖、统一结果目录。 |
| `jupyterlab-official-thermal-examples/tests/csp-example.test.js` | CSP Notebook 完整过程代码、参数绑定 metadata、24 小时时间序列和文件名安全。 |
| `tools/tests/ui-labels.test.js` | 中文菜单、菜单 rank、JupyterHub 模板 favicon、nbgrader 中文化、静态 bundle、Dockerfile 构建检查、图标存在性。 |

这些测试体现出项目关注的不只是算法是否能跑，还包括：

- 生成的 Notebook 是否保持代码透明。
- 交互 metadata 是否正确。
- 菜单和 UI 文案是否符合产品命名。
- 构建产物是否真实包含源码修改。
- 文件名是否避免非法字符。
- 学生/教师界面是否存在预期差异。

## 10. 部署与运行方式概述

项目部署核心围绕 `Dockerfile-nbgrader` 和 `jupyterhub_config.py`。

典型构建流程如下：

1. 构建自定义镜像 `my_jupyterhub:latest`。
2. 镜像安装 JupyterLab、Notebook、Jupyter Server、JupyterHub、nbgrader 和科学计算库。
3. 复制并安装定制版 JupyterLab、定制版 nbgrader 和四个自研扩展。
4. 构建所有 Lab extension，并通过 `jupyter labextension develop . --overwrite` 注册。
5. 替换 JupyterLab 图标和静态资源。
6. 执行构建期 grep 检查，确保菜单、文案和 bundle 内容生效。
7. 启动宿主机 JupyterHub，使用 DockerSpawner 按用户拉起容器。

运行时关键数据流如下：

1. 用户在 JupyterHub 注册，选择教师/学生角色和课程。
2. 认证器将用户加入对应 Hub group。
3. 用户登录后，DockerSpawner 创建单用户容器。
4. 如果用户是教师，Hub 挂载课程目录并注入 `NBGRADER_COURSE_ID` 和 `NBGRADER_ADMIN_API_TOKEN`。
5. 如果用户是学生，容器启动脚本禁用教师扩展。
6. 教师在 JupyterLab 中打开仿真平台生成 Notebook，或用 nbgrader 创建/发布作业。
7. 学生获取作业、运行仿真、调整参数、完成答案并提交。
8. 教师回收、自动评分、手动评分并发布反馈。

## 11. 项目当前状态判断

根据仓库内容，项目已经具备较完整的 MVP 到产品雏形能力：

- JupyterHub 多用户入口和 DockerSpawner 隔离已配置完成。
- 自定义认证器、注册模板、课程分组和 RBAC 已实现。
- 自研 JupyterLab 扩展已成体系，且均有构建脚本和测试。
- 传热、通用仿真、官方 CSP 示例均能生成可运行 Notebook。
- 参数自动绑定已经抽象成独立插件，可被多个生成器复用。
- nbgrader 与 JupyterHub 的多课程集成、学生同步和教师/学生隔离已实现。
- 中文化和品牌化已深入到 JupyterHub、JupyterLab、nbgrader 和静态构建产物。

同时，仓库也体现出仍在快速迭代中的特征：

- 存在完整 `jupyterlab/` 和 `nbgrader/` 上游源码树，仓库体量较大，后续可考虑用 fork/submodule/patch 管理减少维护负担。
- `courses/`、`course_test/`、`jupyterhub.sqlite` 等运行数据在工作区存在，虽然 `.gitignore` 已忽略，但生产部署时仍需明确数据备份与权限策略。
- 部分依赖同时存在 yarn lock 与 package lock，例如 `jupyterlab-thermal-design` 中有 `yarn.lock` 和 `package-lock.json`，后续可统一包管理策略。
- 当前密码最小长度设置为 1，适合测试环境，不适合正式生产环境。
- JupyterHub service token 每次启动动态生成，适合本地运行；若跨进程或持久化服务需要更稳定的密钥策略。

## 12. 后续可演进方向

### 12.1 产品能力

- 增加教师端课程管理页面，允许教师创建课程、查看学生注册状态、批量导入学生。
- 增加模板市场或模板库，将传热、能源、电力、流体、控制等课程模板统一管理。
- 支持教师将生成的仿真 Notebook 一键转为 nbgrader 作业模板。
- 为学生端增加实验报告生成能力，自动汇总参数、图表、关键结果和分析文字。

### 12.2 技术架构

- 将 JupyterLab 和 nbgrader 的定制改动整理成 patch 或 fork 分支，降低上游同步成本。
- 将课程数据、Hub 数据库、exchange 目录纳入明确的备份和迁移方案。
- 为 JupyterHub 生产部署引入 HTTPS、强密码策略、管理员审批或邮箱验证。
- 对 Docker 镜像构建进行分层缓存优化，减少完整构建时间。
- 增加端到端测试，覆盖教师创建作业、学生获取提交、教师回收评分的完整流程。

### 12.3 仿真能力

- 扩展二维瞬态导热、非线性材料属性、边界条件组合和更复杂的 FVM/FEM 模型。
- 为复杂模型引入 Numba 或向量化优化，保证滑块交互下的计算响应。
- 增加误差分析、网格无关性分析、稳定性条件提示和自动诊断。
- 增加更多系统级能源示例，如热泵系统、换热网络、电池热管理、工业余热回收。

## 13. 总结

SimLab / simhub 是一个围绕工程教育场景深度定制的 Jupyter 平台。它使用 JupyterHub 解决多用户与容器隔离，使用 JupyterLab 提供可扩展工作台，使用 nbgrader 构建作业评测闭环，使用 TypeScript + React 开发图形化仿真入口，使用 Python + NumPy/SciPy/Matplotlib 承载可见代码的科学计算。

项目最突出的特点是把“代码透明性”和“图形交互性”结合起来。参数自动绑定插件让 Notebook 中的 Python 赋值变成可拖动控件，仿真平台生成器让复杂模型以结构化 Notebook 呈现，nbgrader 集成又把这些 Notebook 纳入课程作业流程。对于工程类课程，这种设计既降低学生入门门槛，又保留了数学模型、数值方法和代码实现的可学习性。

从工程实现看，项目已经不只是几个独立插件，而是一套完整的教学仿真平台发行版：认证、权限、容器、课程数据、仿真模板、参数交互、作业评测、中文 UI 和品牌体验都已经串联成系统。后续如果继续完善生产安全、课程管理、模板扩展和端到端测试，它具备发展成通用工程仿真教学云平台的基础。
