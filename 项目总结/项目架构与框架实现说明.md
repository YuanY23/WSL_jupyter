# 项目架构与框架实现说明

## 1. 文档目的

这份文档不是简单的“功能清单”，而是从**代码结构、运行结构、官方框架机制、二次开发落点**四个角度，解释这个项目到底是怎样拼起来的。

从当前仓库代码看，这个项目本质上是一个四层集成系统：

1. **JupyterHub 层**：负责登录、用户注册、用户分组、角色控制、启动用户容器。
2. **JupyterLab 层**：提供浏览器里的交互式 IDE、本体前端插件系统、文档与 Kernel 交互能力。
3. **nbgrader 层**：负责课程目录规范、作业发布/提交/收集/自动评分、成绩簿管理。
4. **自研扩展层**：包括角色化注册流程、品牌化 UI、自定义 Logo、热设计原理仿真插件等。

这个项目不是“在一个官方系统里改几处样式”，而是把 **JupyterHub + DockerSpawner + JupyterLab + nbgrader + 自定义 Lab 扩展** 做成了一个面向教学场景的完整平台。

---

## 2. 先看整个系统的全景

### 2.1 运行时全景图

```text
浏览器
  |
  v
ConfigurableHTTPProxy
  |
  v
JupyterHub (宿主机进程)
  |  认证/注册/分组/RBAC/Spawner
  |
  +--> CustomNativeAuthenticator
  +--> Custom Jinja2 Templates
  +--> DockerSpawner
  |
  v
单用户 Docker 容器
  |
  +--> Jupyter Server
  +--> JupyterLab 4.x
  +--> nbgrader
  +--> custom-logo 扩展
  +--> jupyterlab-thermal-design 扩展
  |
  +--> Python Kernel
  +--> 课程目录 /home/jovyan/{course_id} (教师)
  +--> 用户家目录 /home/jovyan
  +--> 共享交换目录 /srv/nbgrader/exchange
```

### 2.2 仓库分层

从仓库目录可以直接看出这不是单一应用，而是“集成工程 + 上游源码 + 自研扩展”的组合：

| 目录/文件 | 作用 |
| --- | --- |
| `jupyterhub_config.py` | JupyterHub 主配置，定义认证器、Spawner、角色、模板、服务令牌 |
| `custom_authenticator.py` | 自定义认证器，扩展注册逻辑，处理角色与课程分组 |
| `templates/` | JupyterHub 页面模板，登录/注册/控制面板/启动等待页都做了定制 |
| `Dockerfile-nbgrader` | 单用户镜像构建入口，安装 JupyterLab、nbgrader、自研插件 |
| `docker_nbgrader_config.py` | 容器内 nbgrader 全局配置 |
| `docker_jupyter_server_config.py` | 容器内 Jupyter Server 扩展开关控制 |
| `disable_teacher_extensions.sh` | 容器启动前脚本，根据角色禁用教师端扩展 |
| `jupyterlab/` | JupyterLab 官方源码仓库副本，当前项目用于构建和部分资源定制 |
| `nbgrader/` | nbgrader 官方源码仓库副本，当前项目在此基础上做了定制 |
| `custom-logo/` | 自定义 Logo 的 JupyterLab 扩展 |
| `jupyterlab-thermal-design/` | 自研热设计仿真插件，含前端与求解器代码 |
| `courses/` | 多课程数据根目录，教师课程目录会动态创建在这里 |
| `course_test/` | 本地测试用的单课程 nbgrader 示例目录 |

---

## 3. 官方框架层是怎么工作的

这一节讲“原有官方框架”的骨架，也就是你的二次开发到底挂在了哪些官方扩展点上。

## 3.1 JupyterHub 官方架构

### 3.1.1 JupyterHub 的标准职责

JupyterHub 官方架构里，通常有 5 个核心角色：

1. **Proxy**：统一接收浏览器请求，再转发给 Hub 或某个用户服务器。
2. **Hub**：保存用户、服务器状态、认证状态，协调整个系统。
3. **Authenticator**：决定“这个用户能否登录、如何创建/校验用户”。
4. **Spawner**：决定“登录成功后如何启动用户的单用户服务器”。
5. **SingleUser Server**：每个用户真正进入的 Notebook/JupyterLab 服务器。

在本项目中，这 5 个角色全部都能在代码里找到对应落点：

| 官方角色 | 本项目落点 |
| --- | --- |
| Proxy | `jupyterhub_config.py` 里的 `ConfigurableHTTPProxy.command` |
| Hub | `jupyterhub_config.py` 本身，配合 `jupyterhub.sqlite` |
| Authenticator | `CustomNativeAuthenticator` |
| Spawner | `dockerspawner.DockerSpawner` |
| SingleUser Server | 由 `my_jupyterhub:latest` 镜像启动的 Jupyter Server/JupyterLab |

### 3.1.2 本项目如何套用 JupyterHub 官方机制

`jupyterhub_config.py` 说明了你们没有改 JupyterHub 官方底层，而是通过**官方允许的配置扩展点**做了深度定制：

- `c.JupyterHub.authenticator_class = CustomNativeAuthenticator`
  - 这是替换官方认证器的标准做法。
- `c.JupyterHub.spawner_class = dockerspawner.DockerSpawner`
  - 这是替换官方启动器的标准做法。
- `c.JupyterHub.load_roles = [...]`
  - 这是使用官方 RBAC 的标准做法。
- `c.JupyterHub.template_paths = [...]`
  - 这是替换 JupyterHub 页面模板的标准做法。

也就是说：**你们对 JupyterHub 的二次开发是“插件式/配置式”的，不是改 JupyterHub 内核源码**。

### 3.1.3 JupyterHub 在本项目里的运行边界

这是理解项目的关键点：

- **JupyterHub 本身运行在宿主机环境**。
- **用户进入的 JupyterLab/nbgrader 运行在 Docker 容器中**。

这个边界非常清晰：

- `jupyterhub_config.py` 指向了宿主机上的 `configurable-http-proxy`。
- `DockerSpawner` 用 `my_jupyterhub:latest` 作为单用户环境镜像。
- `pre_spawn_hook()` 在启动容器前，根据用户组动态挂载课程目录和注入环境变量。

因此，本项目实际上是：

> **宿主机 JupyterHub 管控层 + 容器化 JupyterLab/nbgrader 执行层**

---

## 3.2 JupyterLab 官方架构

### 3.2.1 JupyterLab 的官方本体由两部分组成

JupyterLab 不是单纯的前端页面，它有两个官方层：

1. **Python 服务器层**
2. **TypeScript/Lumino 前端插件层**

在源码里可以直接看到这两个层次：

- `jupyterlab/jupyterlab/`
  - Python 包，负责 `jupyter lab`、server extension、构建与静态资源提供。
- `jupyterlab/packages/`
  - 前端 monorepo，各个功能模块以 package 形式存在。

### 3.2.2 Python 服务器层

在 `jupyterlab/jupyterlab/__init__.py` 里可以看到，JupyterLab 通过 `_jupyter_server_extension_points()` 暴露给 Jupyter Server。

在 `jupyterlab/jupyterlab/labapp.py` 里可以看到：

- `LabApp` / `LabBuildApp` 负责 JupyterLab 启动与前端资源构建。
- `build_handler.py` 负责 `/lab/api/build` 相关接口。
- `commands.py` 体系负责 `jupyter lab build`、`watch`、`clean` 等构建逻辑。

这意味着：

- **JupyterLab 前端不是散落文件，而是被 Python 服务器层统一打包、提供和管理的**。
- 你们在 Dockerfile 里执行的 `jupyter lab build`，本质上是在调用这一套官方构建系统。

### 3.2.3 前端插件层

JupyterLab 前端核心类在 `jupyterlab/packages/application/src/frontend.ts`：

- `JupyterFrontEnd` 继承自 Lumino `Application`
- 内部持有：
  - `commandLinker`
  - `docRegistry`
  - `serviceManager`
  - `contextMenu`
  - `shell`

这说明 JupyterLab 官方前端框架的核心机制是：

1. **命令系统**
2. **插件激活系统**
3. **文档注册系统**
4. **服务访问系统**
5. **Shell 区域布局系统**

你们自己的 `jupyterlab-thermal-design`，就是直接挂在这个官方插件系统上。

### 3.2.4 文档与 Kernel 的官方数据通路

在 `jupyterlab/packages/docmanager/src/manager.ts` 可以看到：

- `DocumentManager` 负责打开、保存、跟踪文档上下文。
- 它依赖 `ServiceManager` 去访问服务。

在 `jupyterlab/packages/services/src/index.ts` 可以看到：

- `services` 包统一封装了 contents、kernel、session、terminal、workspace 等客户端 API。

这套官方结构决定了：

- 前端要创建文件，不是自己直接写磁盘，而是走 `ContentsManager`。
- 前端要执行代码，不是自己起 Python，而是走 Kernel/Session API。
- 前端要打开 Notebook，不是自己拼 UI，而是交给 `docmanager:open` 等命令。

这也是为什么你们的热设计插件里：

- 能通过 `app.serviceManager.contents.save()` 生成 `.ipynb`
- 能通过 `app.commands.execute('docmanager:open', ...)` 打开 Notebook

### 3.2.5 Notebook 是怎样挂进 JupyterLab 的

`jupyterlab/packages/notebook-extension/src/index.ts` 展示了官方 Notebook 模块的典型模式：

- 注册大量 `notebook:*` 命令
- 提供 Notebook widget factory
- 接入 toolbar、菜单、状态栏、搜索、metadata form、LSP 等系统

所以对你们来说，Notebook 并不是“一个特殊页面”，而是 JupyterLab 插件生态里一个非常标准的文档类型。

### 3.2.6 JupyterHub 与 JupyterLab 官方对接点

`jupyterlab/jupyterlab/labhubapp.py` 和 `jupyterlab/packages/hub-extension/src/index.ts` 很关键：

- `SingleUserLabApp` 说明 JupyterHub 单用户服务器默认就是 JupyterLab。
- `hub-extension` 会在检测到 `hubPrefix` 时自动添加 Hub 菜单、控制面板入口、重启服务器逻辑。

这说明：

- **JupyterHub 与 JupyterLab 的结合不是你们从零拼接出来的，而是官方就提供了“Lab on Hub”的标准接法**。
- 你们的工作重点是在这个官方接法上叠加角色、课程、模板和业务扩展。

---

## 3.3 nbgrader 官方架构

### 3.3.1 nbgrader 不是单一插件，而是一整套作业管理框架

从 `nbgrader/pyproject.toml`、`nbgrader/nbgrader/apps/baseapp.py`、`nbgrader/nbgrader/__init__.py` 可以看出，nbgrader 官方有 4 个并行层：

1. **CLI / Traitlets 配置层**
2. **课程目录规范层**
3. **交换目录与作业流转层**
4. **Jupyter Server + JupyterLab 扩展层**

### 3.3.2 CLI / 配置层

`nbgrader/nbgrader/apps/baseapp.py` 里的 `NbGrader` 是官方总基类：

- 继承 `JupyterApp`
- 用 Traitlets 管理配置
- 持有 `CourseDirectory`、`Authenticator`、`ExchangeFactory`

所以 nbgrader 官方不是“写死逻辑”，而是围绕 Traitlets 配置对象组织起来的。

### 3.3.3 课程目录规范层

`nbgrader/nbgrader/coursedir.py` 定义了官方课程目录模型：

- `source/`
- `release/`
- `submitted/`
- `autograded/`
- `feedback/`
- `gradebook.db`

这正是你们 `course_test/` 和 `courses/{course_id}/` 目录结构的来源。

也就是说：

- **课程目录规范不是你们自定义发明的，而是 nbgrader 官方约定。**
- **你们的工作是把这个官方约定和 JupyterHub 多用户/多课程模型绑定起来。**

### 3.3.4 交换目录层

`nbgrader/nbgrader/exchange/exchange_factory.py` 表明 nbgrader 官方把作业流转抽象成一组动作：

- `ReleaseAssignment`
- `FetchAssignment`
- `Submit`
- `Collect`
- `FetchFeedback`
- `List`

这些动作共同围绕 `/srv/nbgrader/exchange` 这个共享交换目录工作。

因此，教师/学生之间的作业交互，并不是直接互相访问对方目录，而是通过 **exchange 共享通道** 完成。

### 3.3.5 Server extension 层

`nbgrader/nbgrader/__init__.py` 和 `nbgrader/etc/jupyter/jupyter_server_config.d/nbgrader.json` 说明 nbgrader 官方会向 Jupyter Server 注册四类服务端扩展：

- `assignment_list`
- `course_list`
- `formgrader`
- `validate_assignment`

其中：

- `assignment_list`：学生端作业列表
- `course_list`：课程/可用 formgrader 服务列表
- `formgrader`：教师端评分管理
- `validate_assignment`：Notebook 校验

`nbgrader/nbgrader/server_extensions/formgrader/formgrader.py` 表明：

- `FormgradeExtension` 会初始化 Jinja2 模板环境
- 向 Tornado 注入 handler
- 把 gradebook、authenticator、coursedir 挂到 webapp settings 里

这就是 nbgrader 教师端 Web 管理界面的官方骨架。

### 3.3.6 JupyterLab 前端扩展层

`nbgrader/src/index.ts` 可以看到，nbgrader 官方前端也是典型的 JupyterLab 插件：

- 菜单：`@jupyter/nbgrader:menu`
- 作业列表：`assignment-list`
- 课程列表：`course-list`
- Formgrader：`formgrader`
- 创建作业：`create-assignment`
- 校验作业：`validate-assignment`

这意味着：

- nbgrader 前端本质上就是一组 JupyterLab 插件。
- 你们能够“按角色禁用某些 nbgrader 功能”，是因为这些能力本来就是离散插件，而不是一个不可拆分的大页面。

---

## 4. 本项目的集成架构是怎样形成的

这一节讲“官方框架到了你这个仓库里之后，最终被拼装成什么样”。

## 4.1 整体集成思路

本项目的集成策略可以概括成一句话：

> **让 JupyterHub 负责人和环境，让 JupyterLab 负责交互界面，让 nbgrader 负责教学作业流，再用课程分组把三者绑成一个角色化教学平台。**

其中最关键的统一主线不是 URL，也不是数据库，而是：

> **JupyterHub Group 命名约定**

当前代码里已经形成了明确的约定：

- `formgrade-{course_id}`：教师所属课程组
- `nbgrader-{course_id}`：学生所属课程组
- `teachers`：全局教师组

这个约定同时驱动：

1. 教师/学生身份判断
2. 课程目录挂载
3. nbgrader 权限判断
4. 前后端扩展开关
5. JupyterHub RBAC 授权

这就是本项目真正的“领域主键”。

---

## 4.2 登录、注册、角色分组链路

### 4.2.1 注册页定制

`custom_authenticator.py` 里的 `CustomSignUpHandler` 做了两件核心事情：

1. 用 `ChoiceLoader` 把 `templates/` 注入 Jinja2 loader，使自定义 `signup.html` 优先于原生模板。
2. 重写 `post()`，额外读取 `role` 和 `course_id` 字段。

而 `templates/signup.html` 又在前端做了角色化表单：

- 学生：课程 ID 选填，用于加入已有课程
- 教师：课程 ID 必填，用于创建/绑定新课程

所以注册流程不是普通账号注册，而是**带角色与课程语义的注册**。

### 4.2.2 用户创建后的自动分组

`CustomNativeAuthenticator.create_user()` 是整个角色模型的核心：

- 教师注册：
  - 加入 `formgrade-{course_id}`
  - 加入 `teachers`
- 学生注册：
  - 加入 `nbgrader-{course_id}`（如果选择了课程）

这一步非常重要，因为后面几乎所有功能都依赖这些分组。

### 4.2.3 这不是“纯前端角色”

本项目的“教师/学生”不是前端菜单状态，而是**真正落在 JupyterHub ORM Group 表里的后端角色**。

因此这个角色模型具备以下特性：

- 重启后仍保留
- 可被 Spawner 读取
- 可被 nbgrader AuthPlugin 读取
- 可被 JupyterHub RBAC 读取

这属于真正的系统级角色模型，而不是页面级开关。

---

## 4.3 容器启动与课程环境注入

### 4.3.1 DockerSpawner 负责“每人一个容器”

`jupyterhub_config.py` 中：

- `c.JupyterHub.spawner_class = dockerspawner.DockerSpawner`
- `c.DockerSpawner.image = 'my_jupyterhub:latest'`
- `c.DockerSpawner.default_url = '/lab'`

说明每个用户登录后得到的是一个独立单用户容器，并默认进入 JupyterLab。

### 4.3.2 容器挂载策略

默认所有用户都有这些挂载：

1. `jupyterhub-user-{username}` -> `/home/jovyan`
   - 用户家目录持久化
2. `/srv/nbgrader/exchange` -> `/srv/nbgrader/exchange`
   - 教师与学生共享交换目录
3. `/home/yuan/my_project/nbgrader` -> `/src/nbgrader`
   - nbgrader 源码热挂载
4. `/home/yuan/my_project/jupyterlab` -> `/opt/jupyterlab`
   - JupyterLab 源码热挂载

这说明当前仓库既服务于运行，也服务于开发调试。

### 4.3.3 教师专属课程挂载

`pre_spawn_hook()` 会遍历用户组，如果用户属于 `formgrade-*`：

- 自动创建宿主机目录 `/home/yuan/my_project/courses/{course_id}`
- `chown` 给容器里的 `jovyan`
- 挂载到容器内 `/home/jovyan/{course_id}`
- 注入 `NBGRADER_COURSE_ID={course_id}`

这一步把“用户属于哪个课程组”转换成了“容器里能看到哪个课程根目录”。

### 4.3.4 教师 API 权限注入

如果用户是教师，`pre_spawn_hook()` 还会注入：

- `NBGRADER_ADMIN_API_TOKEN`

这个 token 来自 `c.JupyterHub.services` 中注册的 `nbgrader` 服务条目。

这里要特别说明：

- 这个 `nbgrader` service 在当前配置下更像一个**被 Hub 认可的 API 凭据载体**
- 它的主要目的不是独立跑一个 service 进程
- 而是给教师容器提供**可管理 JupyterHub group 的管理员 token**

这个设计非常实用，因为它把“教师在 formgrader 里加学生”这件事，延伸成了“同步修改 Hub 里的课程分组”。

---

## 4.4 容器内角色化开关

### 4.4.1 nbgrader 全局配置

`docker_nbgrader_config.py` 是单用户容器内的核心配置文件：

- 使用 `JupyterHubAuthPlugin`
- 统一设置 `c.Exchange.root = '/srv/nbgrader/exchange'`
- 开启 `c.Exchange.path_includes_course = True`

这个 `path_includes_course = True` 是多课程支持的关键，它使 exchange 路径按课程分层。

### 4.4.2 为什么教师设置 `CourseDirectory.course_id`，学生不设置

代码逻辑是：

- 如果容器里有 `NBGRADER_COURSE_ID`
  - 说明这是教师容器
  - 设置 `c.CourseDirectory.course_id`
  - 设置 `c.CourseDirectory.root = /home/jovyan/{course_id}`
- 如果没有
  - 说明这是学生容器
  - 不固定设置 course_id

这样做的含义是：

- 教师容器通常围绕一门课进行管理，所以可以绑定到单一课程根目录。
- 学生可能属于多门课，因此不能在容器启动时把整个环境锁死在单一课程上。
- 学生侧课程可见性应该由 group + course list/assignment list 动态推导。

### 4.4.3 教师容器里为什么要覆盖 `JUPYTERHUB_API_TOKEN`

`docker_nbgrader_config.py` 中，如果发现 `NBGRADER_ADMIN_API_TOKEN`：

- 会在容器内把 `JUPYTERHUB_API_TOKEN` 改成管理员级 token

这样做是为了让 `JupyterHubAuthPlugin` 和 formgrader 后端在教师容器里拥有：

- 建课程组
- 给学生加组
- 给学生移组

的能力。

这一步发生在单用户服务器已完成 OAuth 之后，所以它不是登录用 token，而是**运行期权限提升 token**。

### 4.4.4 学生端扩展裁剪

学生容器启动时，`docker_jupyter_server_config.py` 会关闭后端：

- `nbgrader.server_extensions.formgrader`
- `nbgrader.server_extensions.course_list`

同时 `disable_teacher_extensions.sh` 会在前端写入 `page_config.json`，禁用：

- `@jupyter/nbgrader:formgrader`
- `@jupyter/nbgrader:create-assignment`

所以本项目采用的是**双层角色控制**：

1. 后端接口层禁用
2. 前端插件层隐藏

这比只藏按钮更安全。

---

## 4.5 JupyterHub RBAC 在这里扮演什么角色

`jupyterhub_config.py` 里定义了角色 `formgrade-teacher`：

- 作用对象：`teachers` 组
- 授权范围包括：
  - `list:users`
  - `read:users`
  - `read:groups`
  - `list:groups`
  - `admin:groups`
  - `access:services`

这说明本项目不是只靠“传统管理员权限”，而是用了 JupyterHub 官方的 **RBAC scope 机制**。

换句话说：

- 教师不是 Hub 超级管理员
- 但教师组可以拥有“足够完成教学管理”的 scoped 权限

这是一个很合理的架构选择。

---

## 5. nbgrader 在本项目里被怎样定制

本项目对 nbgrader 的定制，比对 JupyterLab 更深入。

## 5.1 官方骨架保持不变

你们并没有推翻 nbgrader 官方结构，以下骨架仍然是官方原型：

- `CourseDirectory`
- `ExchangeFactory`
- `assignment_list` / `course_list` / `formgrader` / `validate_assignment`
- JupyterLab 前端插件注册方式
- gradebook 数据库存储方式

所以这个项目仍然是一个**nbgrader 兼容体系**。

## 5.2 真正的定制点

从源码看，定制主要集中在四块。

### 5.2.1 认证插件扩展

`nbgrader/nbgrader/auth/jupyterhub.py` 中的 `JupyterHubAuthPlugin` 被改造成：

- 同时识别 `nbgrader-{course}` 和 `formgrade-{course}`
- `get_student_courses()` 返回当前用户从 group 推导出的课程集合
- `add_student_to_course()` / `remove_student_from_course()` 通过 Hub API 改组

这意味着：

- **课程权限的单一事实来源不再是 nbgrader 自己的配置，而是 JupyterHub group。**

这是本项目最重要的架构改造之一。

### 5.2.2 教师端“加学生”会反向同步 Hub 分组

`nbgrader/nbgrader/server_extensions/formgrader/apihandlers.py` 的 `StudentHandler.put()` 里做了额外逻辑：

- 更新 gradebook 学生信息后
- 读取 `NBGRADER_COURSE_ID`
- 调 Hub API 确保 `nbgrader-{course_id}` 存在
- 再把学生加进这个组

这一步让教师在 formgrader 里维护学生名单时，Hub 层的课程分组也会同步变化。

所以：

- nbgrader 不再只是课程内局部管理工具
- 它开始反向驱动整个平台的身份与权限关系

### 5.2.3 前端与模板中文化

从 `nbgrader/src/` 和 `nbgrader/nbgrader/server_extensions/formgrader/templates/` 可以看到，大量文案被改成了中文：

- 菜单名
- 作业列表
- 课程列表
- formgrader 模板
- JS 提示信息

这说明你们不是在外层做简单语言包覆盖，而是直接对 nbgrader 的前端源码和模板做了本地化。

### 5.2.4 前端插件仍然遵守 JupyterLab 官方规范

虽然做了中文化与业务适配，`nbgrader/src/index.ts` 仍然保持官方插件结构：

- `JupyterFrontEndPlugin`
- command 注册
- `WidgetTracker`
- `MainAreaWidget`
- `IRouter`、`ILayoutRestorer`、`INotebookTracker` 等官方 token

这非常重要，因为它意味着你们的 nbgrader 二次开发仍然是**顺着官方框架写的**，不是完全分叉出一套私有前端框架。

---

## 6. JupyterLab 在本项目里被怎样定制

和 nbgrader 相比，本项目对 JupyterLab 的改造更偏向“框架承载层”和“品牌层”。

## 6.1 JupyterLab 源码仓库为何放在项目里

`jupyterlab/` 目录基本上是一份 JupyterLab 官方 monorepo 副本。

从 `jupyterlab/package.json`、`jupyterlab/pyproject.toml`、`jupyterlab/packages/` 结构来看，这里保留的是完整开发仓库，而不是只装了个 wheel。

这样做的好处是：

1. 可以基于源码构建 JupyterLab
2. 可以直接修改前端资源与核心包
3. 可以和本地自研扩展一起重新打包

## 6.2 当前项目对 JupyterLab 的定制重点

从已检查代码看，JupyterLab 的二次开发主要集中在以下几类：

### 6.2.1 品牌资源替换

`jupyterlab/packages/ui-components/style/icons/jupyter/` 下的官方图标资源已经被替换成黄色 `Y` 标识。

Dockerfile 还会把这些 SVG 再复制到：

- JupyterLab 已构建的 static 目录
- 已安装的 `@jupyterlab/ui-components` labextension static 目录

这说明你们对品牌资源做的是**源码层 + 构建产物层双重覆盖**。

### 6.2.2 默认设置覆盖

Dockerfile 在构建后写入了 `overrides.json`：

- 默认语言：`zh_CN`
- 禁用 extension manager
- 禁用 toc

这属于官方支持的 JupyterLab 设置覆盖机制。

### 6.2.3 主题 CSS 注入

Dockerfile 还会直接向 Light/Dark 主题 CSS 追加 Logo 样式，使主 Logo 显示为黄色 `Y`。

这说明品牌化不只靠 SVG，还补了一层 CSS 兜底。

### 6.2.4 自定义 Lab 扩展挂载

JupyterLab 的官方扩展机制被用来承载：

- `custom-logo`
- `jupyterlab-thermal-design`
- `@jupyter/nbgrader`

所以 JupyterLab 在项目里更像一个**前端插件宿主平台**。

## 6.3 一个重要结论

从当前仓库看：

- **JupyterLab 核心 TypeScript 框架没有被大规模业务侵入式修改**
- **主要改造点在构建、资源、设置、扩展接入**

也就是说：

> 你们把 JupyterLab 当成可编译、可插拔、可品牌化的 IDE 平台来用，而不是把业务硬塞进它的核心源码里。

这是很成熟的工程做法。

---

## 7. 自研扩展一：JupyterHub 页面与认证框架

这个模块虽然没有独立目录名，但它其实已经形成了一套自己的“小框架”。

## 7.1 组成部分

| 组件 | 作用 |
| --- | --- |
| `custom_authenticator.py` | 认证与注册逻辑扩展 |
| `templates/native-login.html` | 登录页 |
| `templates/signup.html` | 注册页 |
| `templates/home.html` | Hub 控制面板主页 |
| `templates/spawn_pending.html` | 启动等待页 |

## 7.2 它的设计模式

这套模块的设计模式是：

1. **保留 JupyterHub 原始路由与生命周期**
2. **替换模板**
3. **替换认证器**
4. **在认证器里扩展数据字段**

所以它不是自建一个外部前端，而是**嵌入 JupyterHub 原生渲染链**。

## 7.3 为什么说它是一个“自定义门户层”

因为它已经同时承担了：

- 品牌展示
- 登录/注册入口
- 角色选择
- 课程归属输入
- 启动服务器控制
- 启动等待反馈

在系统架构上，这一层可以视为平台的 **Edu Portal / 教学门户层**。

---

## 8. 自研扩展二：custom-logo 扩展

`custom-logo/` 是一个非常轻量，但很典型的 JupyterLab 预构建扩展。

## 8.1 它不是改业务逻辑，而是改壳层体验

该扩展的核心几乎只有两部分：

- `package.json`
- `static/style.js`

`style.js` 会在浏览器里动态插入 CSS：

- 隐藏默认 JupyterLab SVG Logo
- 用黄色 `Y` 取代

这说明：

- 它不是 Python server extension
- 也不是复杂的 TypeScript 插件
- 而是一个 **CSS-only labextension**

## 8.2 它和 JupyterLab 源码替换的关系

本项目的品牌定制实际上有三层：

1. `custom-logo` 运行时注入 CSS
2. JupyterLab 源码中的 SVG 被替换
3. Dockerfile 在主题 CSS 上追加兜底样式

这三层叠加后，Logo 定制基本不会因为构建路径不同而失效。

---

## 9. 自研扩展三：热设计原理仿真插件

这是仓库里最完整的业务扩展模块。

## 9.1 它的本质

`jupyterlab-thermal-design/` 不是一个简单 widget，而是一个完整的：

- JupyterLab 前端扩展
- Python 打包扩展
- 数值求解器代码库
- Notebook 自动生成器

的组合工程。

## 9.2 包结构

从目录和 `setup.py`/`package.json` 看，它由三部分组成：

| 部分 | 代码位置 | 作用 |
| --- | --- | --- |
| 前端扩展 | `src/` | 注册菜单、显示工作台、收集参数 |
| Labextension 打包层 | `jupyterlab_thermal_design/` | 提供 `_jupyter_labextension_paths()`，把前端构建产物交给 JupyterLab |
| Python 求解器 | `thermal_solver/` | 提供各类传热问题求解函数 |

## 9.3 它如何挂进 JupyterLab 官方框架

`src/index.ts` 是标准的 `JupyterFrontEndPlugin<void>`：

- `autoStart: true`
- 依赖 `ICommandPalette` 和 `IMainMenu`
- 注册命令 `thermal-design:open-workbench`
- 增加菜单“热设计仿真系统”

这说明你们完全遵循了 JupyterLab 官方扩展模式。

## 9.4 前端工作台结构

`src/MainWidget.tsx` 展示了这个插件的 UI 组织方式：

- 外层用 Lumino `Widget`
- 内层嵌一个 `ReactWidget`
- 真正的业务 UI 在 `ControlPanel` React 组件中

这个模式很标准，也很适合 JupyterLab：

- JupyterLab 外壳继续用 Lumino
- 复杂业务表单用 React 实现

## 9.5 场景与参数系统

`src/components/ControlPanel.tsx` 定义了当前插件的业务模型：

- 4 大类场景
  - 稳态导热
  - 瞬态导热
  - 对流换热
  - 热辐射
- 共 14 个具体场景
- 默认参数字典
- 参数标签/单位/上下限验证字典

这说明前端不是死页面，而是一个**配置驱动的场景系统**。

## 9.6 它当前真正的执行路径：不是直接调用后端 API，而是生成 Notebook

这是理解该插件最关键的一点。

从 `MainWidget.tsx` 可见，用户点击执行后，当前流程是：

1. 读取场景与参数
2. 调 `generateNotebook(scenarioId, params)`
3. 生成完整 `.ipynb` JSON
4. 用 `ContentsManager` 保存到 `仿真结果归档/`
5. 用 `docmanager:open` 打开新 Notebook

也就是说，当前插件的主路径是：

> **“参数面板 -> 自动生成教学型 Notebook -> 让用户在 Notebook 中运行/修改/观察结果”**

这非常符合 Jupyter 教学场景，因为：

- 结果可留痕
- 可复算
- 可教学讲解
- 可二次修改参数

## 9.7 `thermal_solver` 的定位

`thermal_solver/` 中的结构很完整：

- `core/base.py`
- `models/steady_conduction/*`
- `models/transient_conduction/*`
- `models/convection/*`
- `models/radiation/*`
- `models/__init__.py` 中的 `ROUTER`

它说明你们设计过一个统一求解器入口：

- `run_simulation(scenario_id, params)`
- 输出标准 JSON 结构

但是从当前前端主路径看：

- `MainWidget.tsx` 并没有直接调用 `run_simulation()`
- `NotebookGenerator.ts` 主要生成“自包含 notebook 代码”

因此，当前代码里 `thermal_solver` 更像：

1. 一个可复用的数值算法库
2. 一个未来可直接接入 kernel/后端执行的统一抽象层
3. 一个帮助你们规范场景路由的后端模型层

换句话说：

- **当前上线主链路是 Notebook 生成式**
- **求解器包更像架构预留与算法沉淀层**

## 9.8 这套插件的工程价值

这个插件的价值不只是“多了个实验工具”，而是说明项目已经具备了把领域业务嵌入 JupyterLab 的能力：

- 菜单扩展
- React 表单工作台
- 文件系统操作
- Notebook 生成
- 数值算法封装
- 结果归档

这证明该平台已经从“教学作业平台”扩展成“教学实验平台”。

---

## 10. Docker 镜像构建层承担了什么职责

`Dockerfile-nbgrader` 是项目的另一个关键框架文件。

## 10.1 它不是普通依赖安装脚本

这个 Dockerfile 实际上定义了**平台运行时的标准单用户环境**，负责：

1. 安装 Python/Node/系统依赖
2. 安装 JupyterLab、Notebook、Jupyter Server、JupyterHub
3. 安装 nbgrader
4. 安装热设计插件
5. 注入中文语言包
6. 重建 JupyterLab 前端
7. 注入品牌资源
8. 写入默认设置

因此它相当于：

> **本项目“单用户教学工作台”的镜像化规范**

## 10.2 构建顺序说明了架构关系

Dockerfile 的顺序非常能说明系统层次：

1. 先装基础环境
2. 再装 JupyterLab 官方本体
3. 再装 nbgrader
4. 再装自研插件
5. 最后统一 `jupyter lab build`

这表明：

- JupyterLab 是前端宿主
- nbgrader 是教学作业插件层
- thermal-design 是业务实验插件层

架构层次很清楚。

---

## 11. 项目里真正的“统一数据主线”

如果只看目录，容易以为系统很散；但从代码看，它其实有非常清楚的统一主线。

## 11.1 身份主线：JupyterHub Group

统一身份模型靠：

- `formgrade-{course_id}`
- `nbgrader-{course_id}`
- `teachers`

## 11.2 课程主线：`course_id`

统一课程主线靠：

- 注册表单里的 `course_id`
- 教师组和学生组命名里的 `course_id`
- 教师容器环境变量 `NBGRADER_COURSE_ID`
- `CourseDirectory.course_id`
- exchange 子路径中的 course 前缀

## 11.3 文件主线：课程根目录 + exchange

统一文件主线靠：

- 教师课程根目录 `/home/yuan/my_project/courses/{course_id}`
- 学生个人家目录 `/home/jovyan`
- 共享交换目录 `/srv/nbgrader/exchange`

这三条主线叠加后，整个平台就形成了稳定的系统闭环。

---

## 12. 官方框架与二次开发的映射关系

这一节用最直接的方式总结“哪些是官方提供的，哪些是你们自己做的”。

| 层级 | 官方框架能力 | 本项目二次开发 |
| --- | --- | --- |
| JupyterHub | 认证器、Spawner、Proxy、RBAC、模板扩展点 | `CustomNativeAuthenticator`、自定义模板、课程分组、教师 scoped role、pre_spawn_hook |
| DockerSpawner | 每用户独立容器 | 动态挂载课程目录、注入 `NBGRADER_COURSE_ID` 与管理员 token |
| JupyterLab Python 层 | `jupyter lab` 启动与构建、server extension 机制 | 源码级品牌资源替换、默认语言/设置覆盖 |
| JupyterLab 前端层 | 命令系统、插件系统、Shell、DocumentManager、ServiceManager | `custom-logo`、`jupyterlab-thermal-design`、nbgrader 前端中文化 |
| nbgrader CLI/配置层 | `NbGrader`、`CourseDirectory`、`ExchangeFactory` | 多课程 group 绑定、course-aware exchange、测试课程配置 |
| nbgrader Server 层 | formgrader/assignment list/course list/validate | JupyterHub group 同步、教师/学生权限裁剪、模板中文化 |
| 领域业务层 | 官方无 | 热设计原理仿真工作台、Notebook 自动生成、求解器库 |

---

## 13. 代码阅读建议

如果你以后还要继续维护这个项目，建议按下面顺序读：

### 第一轮：先理解系统拼装

1. `jupyterhub_config.py`
2. `custom_authenticator.py`
3. `Dockerfile-nbgrader`
4. `docker_nbgrader_config.py`
5. `docker_jupyter_server_config.py`
6. `disable_teacher_extensions.sh`

### 第二轮：再理解官方教学链路

1. `nbgrader/nbgrader/apps/baseapp.py`
2. `nbgrader/nbgrader/coursedir.py`
3. `nbgrader/nbgrader/exchange/exchange_factory.py`
4. `nbgrader/nbgrader/auth/jupyterhub.py`
5. `nbgrader/nbgrader/server_extensions/formgrader/formgrader.py`
6. `nbgrader/src/index.ts`

### 第三轮：再理解 JupyterLab 插件宿主

1. `jupyterlab/packages/application/src/frontend.ts`
2. `jupyterlab/packages/docmanager/src/manager.ts`
3. `jupyterlab/packages/services/src/index.ts`
4. `jupyterlab/packages/notebook-extension/src/index.ts`
5. `jupyterlab/packages/hub-extension/src/index.ts`

### 第四轮：最后理解自研业务扩展

1. `jupyterlab-thermal-design/src/index.ts`
2. `jupyterlab-thermal-design/src/MainWidget.tsx`
3. `jupyterlab-thermal-design/src/components/ControlPanel.tsx`
4. `jupyterlab-thermal-design/src/utils/NotebookGenerator.ts`
5. `jupyterlab-thermal-design/thermal_solver/`

---

## 14. 结论

从当前代码全貌看，这个项目的架构特点可以概括成 6 句话：

1. **它的核心不是单体 Web 应用，而是多开源框架的集成平台。**
2. **JupyterHub 负责身份、分组、容器调度，是系统控制面。**
3. **JupyterLab 负责交互 IDE 与插件宿主，是系统工作台。**
4. **nbgrader 负责课程目录、作业流转、评分与成绩，是教学流程引擎。**
5. **你们的二次开发主要围绕“角色化教学场景”展开，而不是大规模改写官方底层。**
6. **热设计插件说明这个平台已经具备继续承载更多专业实验模块的能力。**

如果再进一步抽象，这个项目可以被理解为：

> **一个以 JupyterLab 为交互内核、以 JupyterHub 为权限与资源调度中枢、以 nbgrader 为教学流程引擎、并支持专业领域插件扩展的教学实验平台。**

这就是它最准确的系统定位。
