# 项目详细总结

生成时间：2026-05-24  
项目根目录：`/home/yuan/my_project`

## 1. 项目定位

本项目是一个面向教学、课程管理和可执行仿真实验的 Jupyter 平台集成工程。它不是单一 Web 应用，而是把 `JupyterHub`、`DockerSpawner`、`JupyterLab`、`nbgrader` 与两套自研 JupyterLab 扩展组合成一个完整的教学仿真系统。

核心目标可以概括为三句话：

1. 用 JupyterHub 负责用户登录、注册、角色、课程分组和容器生命周期。
2. 用 JupyterLab 作为浏览器端 IDE 和插件宿主，让学生、教师在 Notebook 中完成学习、实验和作业。
3. 用 nbgrader 和自研仿真插件把课程作业、自动评分、白箱式仿真、可见代码 Notebook 生成串成闭环。

项目整体更接近“教学平台 + 仿真实验平台 + Jupyter 发行环境”的组合，而不是普通脚手架项目。

## 2. 总体架构

### 2.1 运行时全景

```text
浏览器
  |
  v
ConfigurableHTTPProxy
  |
  v
JupyterHub 宿主管控层
  |
  +-- CustomNativeAuthenticator
  |     - 中文注册页
  |     - 教师/学生角色
  |     - course_id 课程标识
  |     - JupyterHub group 自动写入
  |
  +-- JupyterHub ORM / SQLite
  |     - users
  |     - groups
  |     - roles
  |
  +-- RBAC
  |     - teachers 组获得课程管理所需 scopes
  |
  +-- DockerSpawner
        |
        v
单用户 Docker 容器 my_jupyterhub:latest
  |
  +-- Jupyter Server
  +-- JupyterLab 4.2.x
  +-- nbgrader 0.9.5
  +-- custom-logo
  +-- jupyterlab-thermal-design
  +-- jupyterlab-simulation-platform
  +-- Python Kernel / NumPy / SciPy / Matplotlib / Numba
  |
  +-- /home/jovyan                 用户家目录卷
  +-- /srv/nbgrader/exchange       作业交换目录
  +-- /home/jovyan/{course_id}     教师课程目录挂载
```

### 2.2 分层职责

| 层级 | 主要职责 | 关键文件或目录 |
| --- | --- | --- |
| 宿主管控层 | 登录、注册、分组、RBAC、容器启动 | `jupyterhub_config.py`、`custom_authenticator.py` |
| 门户页面层 | 中文登录、注册、控制面板、启动等待页 | `templates/` |
| 容器运行层 | 构建单用户 Jupyter 环境，安装 JupyterLab、nbgrader、自研扩展 | `Dockerfile-nbgrader`、`docker_nbgrader_config.py`、`docker_jupyter_server_config.py` |
| JupyterLab 平台层 | 浏览器 IDE、菜单、命令、Notebook、插件体系 | `jupyterlab/`、`custom-logo/` |
| 作业评分层 | 作业发布、提交、收集、自动评分、反馈 | `nbgrader/`、`courses/`、`course_test/` |
| 热设计仿真层 | 热物理场景配置、Notebook 生成、参数滑块双向绑定、求解器沉淀 | `jupyterlab-thermal-design/` |
| 通用仿真模板层 | 七类可见代码仿真模板，生成可修改 Notebook | `jupyterlab-simulation-platform/` |
| 汇报资产层 | 中期答辩 PPT 生成、Logo/Favicon 生成 | `create_midterm_6slides.py`、`convert_ico.py`、`ppt_assets/` |

## 3. 技术栈

### 3.1 Python 与后端

| 技术 | 用途 |
| --- | --- |
| Python | JupyterHub 配置、Authenticator 扩展、nbgrader、热求解器、PPT 脚本 |
| JupyterHub | 多用户入口、认证、RBAC、服务 token、用户服务器管理 |
| NativeAuthenticator | 用户注册和密码认证基础，本项目继承扩展 |
| DockerSpawner | 每个用户一个单用户 Docker 容器 |
| Jupyter Server | 容器内 Notebook/Lab 服务端 |
| nbgrader | 课程作业流转和评分体系 |
| SQLAlchemy / Alembic | nbgrader 和 JupyterHub 数据模型相关依赖 |
| NumPy | 数值数组、矩阵、有限差分计算 |
| SciPy | 优化模板中使用 `scipy.optimize.linprog` |
| Matplotlib | 生成 Notebook 中的图表输出 |
| Numba | 数值计算加速依赖，当前主要作为运行环境预装能力 |
| python-pptx / PIL | 生成中期答辩 PPT 和图像资产 |

### 3.2 前端与扩展

| 技术 | 用途 |
| --- | --- |
| TypeScript | JupyterLab 扩展主语言 |
| React | 自研仿真面板、模板表单、参数侧边栏 |
| Lumino Widgets | JupyterLab 前端 widget 外壳 |
| JupyterLab Plugin API | 注册菜单、命令、左侧栏、主工作区面板 |
| `ICommandPalette` | 将命令加入命令面板 |
| `IMainMenu` | 将插件入口加入 JupyterLab 顶部菜单 |
| `INotebookTracker` | 监听当前 Notebook，用于参数侧边栏自动扫描 |
| `NotebookActions` | 自动执行参数单元格和下游单元格 |
| `ContentsManager` | 保存自动生成的 `.ipynb` 文件 |
| CSS Labextension | `custom-logo` 运行时覆盖 JupyterLab Logo |

### 3.3 构建与运行环境

| 技术 | 用途 |
| --- | --- |
| Docker | 构建单用户镜像 `my_jupyterhub:latest` |
| `jupyter/base-notebook:ubuntu-22.04` | Docker 基础镜像 |
| Node.js / npm / jlpm | 构建 JupyterLab 和 labextension |
| editable install | `pip install -e .` 开发态安装 JupyterLab、nbgrader、自研扩展 |
| `.dockerignore` | 默认拒绝构建上下文，只重新包含 Dockerfile 需要的源码 |
| `.gitignore` | 排除数据库、课程数据、生成产物、依赖缓存和敏感运行文件 |

## 4. 仓库目录说明

### 4.1 顶层目录

| 路径 | 说明 |
| --- | --- |
| `jupyterhub_config.py` | JupyterHub 主配置。定义认证器、DockerSpawner、容器卷、RBAC、服务 token、模板路径。 |
| `custom_authenticator.py` | 自定义 NativeAuthenticator。替换注册 handler，读取 `role` 和 `course_id`，创建用户后写入 Hub group。 |
| `templates/` | 自定义 JupyterHub 页面。包含登录、注册、主页、启动等待页。 |
| `Dockerfile-nbgrader` | 单用户 Docker 镜像构建文件。安装 JupyterLab、nbgrader、自研扩展、中文语言包、字体、样式覆盖。 |
| `docker_nbgrader_config.py` | 容器内 nbgrader 配置。使用 Hub group 判断课程权限，配置 exchange root。 |
| `docker_jupyter_server_config.py` | 容器内 Jupyter Server 配置。学生容器关闭教师端 nbgrader 服务扩展。 |
| `disable_teacher_extensions.sh` | 容器启动前脚本。学生容器禁用教师端前端插件，清理 Matplotlib 字体缓存。 |
| `jupyterlab/` | JupyterLab 官方 monorepo 副本，用于源码构建和品牌资源替换。 |
| `nbgrader/` | nbgrader 官方源码副本，当前项目在其中做了 JupyterHub group 集成与中文化。 |
| `custom-logo/` | CSS-only JupyterLab labextension，用于运行时覆盖主 Logo。 |
| `jupyterlab-thermal-design/` | 热设计原理仿真插件和 Python 热求解器。 |
| `jupyterlab-simulation-platform/` | 通用仿真模板 Notebook 生成器。 |
| `courses/` | 多课程运行数据目录。包含 source、release、submitted、autograded、feedback、gradebook 等。 |
| `course_test/` | 本地 nbgrader 测试课程目录。 |
| `docs/superpowers/` | 双向绑定功能的设计规格与实现计划。 |
| `项目架构与框架实现说明.md` | 已有架构说明文档。 |
| `5月20日gemini内容总结.md` | 已有项目总结文档。 |
| `create_midterm_6slides.py` | 中期答辩 PPT 生成脚本。 |
| `convert_ico.py` | 将 `yellow-y.svg` 转换为 PNG 和 ICO 的脚本。 |

### 4.2 运行数据和生成产物

以下内容属于运行态或生成态，不应作为主要源码理解：

| 路径 | 类型 |
| --- | --- |
| `jupyterhub.sqlite` | Hub 本地数据库，记录用户、服务、服务器状态等 |
| `jupyterhub_cookie_secret` | Hub cookie secret |
| `courses/**/gradebook.db` | nbgrader 课程成绩簿 |
| `courses/**/submitted/` | 学生提交数据 |
| `courses/**/feedback/` | 作业反馈文件 |
| `jupyterlab-thermal-design/lib/` | TypeScript 编译产物 |
| `jupyterlab-thermal-design/jupyterlab_thermal_design/labextension/` | labextension 打包产物 |
| `jupyterlab-simulation-platform/lib/` | TypeScript 编译产物 |
| `jupyterlab-simulation-platform/jupyterlab_simulation_platform/labextension/` | labextension 打包产物 |
| `jupyterlab-simulation-platform/simulation-platform-results/` | 模板生成器输出目录 |

`.gitignore` 已经明确排除了这些内容，但当前工作区里仍能看到部分运行文件，说明项目既承担开发，也承担本地实验运行。

## 5. JupyterHub 管控层

### 5.1 认证器接入

`jupyterhub_config.py` 使用：

```python
c.JupyterHub.authenticator_class = CustomNativeAuthenticator
```

这表示项目没有修改 JupyterHub 核心源码，而是通过官方 Authenticator 扩展点替换认证逻辑。

当前认证配置特点：

| 配置 | 当前值 | 含义 |
| --- | --- | --- |
| `enable_signup` | `True` | 开启注册 |
| `open_signup` | `True` | 注册无需管理员审批 |
| `minimum_password_length` | `1` | 测试环境弱密码门槛 |
| `admin_users` | `{'yuan'}` | Hub 管理员 |
| `allow_all` | `True` | 允许已认证用户登录 |

注意：`minimum_password_length = 1` 明确是测试配置，正式部署应提高密码强度要求并考虑审批、邮箱验证或外部身份源。

### 5.2 自定义注册流程

`custom_authenticator.py` 继承 `NativeAuthenticator`，替换默认 `/signup` handler。

注册页 `templates/signup.html` 增加两个业务字段：

| 字段 | 说明 |
| --- | --- |
| `role` | `student` 或 `teacher` |
| `course_id` | 课程标识，教师必填，学生选填 |

`CustomSignUpHandler.post()` 在标准用户名、密码、邮箱之外读取这两个字段，然后调用：

```python
self.authenticator.create_user(role=role, course_id=course_id, **user_info)
```

### 5.3 Group 命名约定

本项目最关键的领域模型是 JupyterHub group 命名：

| Group | 含义 |
| --- | --- |
| `teachers` | 全局教师组 |
| `formgrade-{course_id}` | 某课程教师组 |
| `nbgrader-{course_id}` | 某课程学生组 |

实际代码行为：

1. 教师注册并填写 `course_id` 后，会加入 `formgrade-{course_id}` 和 `teachers`。
2. 学生注册并填写 `course_id` 后，会加入 `nbgrader-{course_id}`。
3. 学生未填写课程时，只创建账号，不绑定课程。

这条 group 主线同时驱动容器挂载、nbgrader 权限、学生可见课程和教师管理权限。

### 5.4 DockerSpawner 容器启动

`jupyterhub_config.py` 使用 DockerSpawner：

```python
c.JupyterHub.spawner_class = dockerspawner.DockerSpawner
c.DockerSpawner.image = 'my_jupyterhub:latest'
c.DockerSpawner.default_url = '/lab'
c.DockerSpawner.notebook_dir = '/home/jovyan'
```

所有用户基础挂载：

| 宿主机或卷 | 容器内路径 | 作用 |
| --- | --- | --- |
| `jupyterhub-user-{username}` | `/home/jovyan` | 用户家目录持久化 |
| `/srv/nbgrader/exchange` | `/srv/nbgrader/exchange` | nbgrader 作业交换目录 |
| `/home/yuan/my_project/nbgrader` | `/src/nbgrader` | nbgrader 源码开发挂载 |
| `/home/yuan/my_project/jupyterlab` | `/opt/jupyterlab` | JupyterLab 源码开发挂载 |

### 5.5 教师容器动态注入

`pre_spawn_hook(spawner)` 会读取用户 group。如果发现 `formgrade-{course_id}`：

1. 创建宿主机课程目录 `/home/yuan/my_project/courses/{course_id}`。
2. 将目录 owner 调整为容器内 `jovyan` 对应 UID/GID。
3. 挂载到容器内 `/home/jovyan/{course_id}`。
4. 注入 `NBGRADER_COURSE_ID={course_id}`。
5. 注入 `NBGRADER_ADMIN_API_TOKEN`。

这个钩子把“教师属于某课程 group”转换为“教师容器拥有该课程目录和管理 token”。

### 5.6 RBAC 权限

`c.JupyterHub.load_roles` 定义了 `formgrade-teacher`：

```python
"scopes": [
  "list:users",
  "read:users",
  "read:groups",
  "list:groups",
  "admin:groups",
  "access:services"
],
"groups": ["teachers"]
```

这使教师组可以读用户、读 group、管理 group、访问服务。项目没有把教师直接提升为 Hub 管理员，而是使用 JupyterHub scopes 做最小范围授权。

## 6. JupyterHub 门户页面

`templates/` 中的页面都使用中文和简洁黑白界面：

| 模板 | 说明 |
| --- | --- |
| `native-login.html` | 登录页。包含密码显示/隐藏、注册入口和未加密连接提示。 |
| `signup.html` | 注册页。包含角色选择、课程 ID、密码确认、TOS、2FA、reCAPTCHA 兼容逻辑。 |
| `home.html` | Hub 控制面板首页。显示服务器状态，提供启动、进入、停止等操作。 |
| `spawn_pending.html` | 用户服务器启动等待页。优先用 SSE `progress_url` 更新状态，失败时展示错误，缺失进度接口时 fallback 轮询。 |

页面层没有脱离 JupyterHub，而是继续使用 JupyterHub 的 Jinja2 模板渲染链。这样可以保留 Hub 原生路由、XSRF、登录态和服务器控制逻辑。

## 7. Docker 单用户镜像

### 7.1 基础镜像和依赖

`Dockerfile-nbgrader` 基于：

```dockerfile
FROM jupyter/base-notebook:ubuntu-22.04
```

系统依赖：

| 依赖 | 用途 |
| --- | --- |
| `git` | JupyterLab 源码构建需要 git 仓库状态 |
| `nodejs` / `npm` | 前端构建 |
| `fonts-wqy-zenhei` | 中文绘图和界面字体 |

Python 依赖安装包括：

| 依赖 | 用途 |
| --- | --- |
| `jupyterlab==4.2.7` | Lab 主环境 |
| `notebook==7.2.2` | Notebook 兼容 |
| `jupyter_server==2.14.2` | 服务端 |
| `jupyterhub` | 单用户环境与 Hub 对接 |
| `alembic`、`sqlalchemy`、`traitlets`、`tornado` | nbgrader/Jupyter 运行依赖 |
| `rapidfuzz`、`requests` | nbgrader 与 Hub API 相关 |
| `matplotlib`、`numpy`、`scipy`、`numba` | 仿真计算与绘图 |
| `jupyterlab-language-pack-zh-CN` | JupyterLab 中文语言包 |

### 7.2 构建顺序

镜像构建大致顺序：

1. 创建 `/srv/nbgrader/exchange`。
2. 复制容器内 Jupyter/nbgrader 配置。
3. 复制启动前脚本 `disable_teacher_extensions.sh`。
4. 安装 JupyterLab、JupyterHub、nbgrader 依赖、科学计算库。
5. 复制 `custom-logo`、`nbgrader`、`jupyterlab`、两套自研插件源码。
6. `pip install -e .` 安装本地 JupyterLab 源码。
7. 初始化临时 git 仓库并执行 `jlpm install`、`jlpm run build`。
8. `pip install -e .` 安装本地 nbgrader。
9. 构建并 develop `jupyterlab-thermal-design`。
10. 构建并 develop `jupyterlab-simulation-platform`。
11. 覆盖 JupyterLab Logo SVG。
12. 执行 `jupyter lab build --minimize=False`。
13. 写入 `overrides.json`，默认中文、禁用 extension manager、禁用 toc。
14. 向 Light/Dark theme CSS 注入黄色 `Y` Logo 兜底样式。

### 7.3 角色化扩展开关

学生容器没有 `NBGRADER_COURSE_ID`，因此：

1. `docker_jupyter_server_config.py` 关闭后端：
   - `nbgrader.server_extensions.formgrader`
   - `nbgrader.server_extensions.course_list`

2. `disable_teacher_extensions.sh` 写入 `page_config.json` 禁用前端：
   - `@jupyter/nbgrader:formgrader`
   - `@jupyter/nbgrader:create-assignment`

教师容器有 `NBGRADER_COURSE_ID`，保留完整教师端扩展。

这是后端接口和前端插件的双层控制，比只隐藏菜单更可靠。

## 8. nbgrader 教学评分层

### 8.1 官方骨架

`nbgrader/` 是 nbgrader 0.9.5 源码副本。官方核心仍然保留：

| 模块 | 作用 |
| --- | --- |
| `nbgrader/apps/` | 命令行工具，如 assign、release、collect、autograde、feedback |
| `nbgrader/coursedir.py` | 课程目录结构 |
| `nbgrader/exchange/` | 作业发布、下载、提交、收集、反馈流转 |
| `nbgrader/server_extensions/` | Jupyter Server 扩展 |
| `nbgrader/src/` | JupyterLab 前端扩展源码 |
| `nbgrader/labextension/` | 构建后的 labextension |

典型课程目录：

```text
course_id/
  source/       教师源作业
  release/      发布给学生的作业
  submitted/    学生提交
  autograded/   自动评分结果
  feedback/     反馈文件
  gradebook.db  成绩簿
```

### 8.2 JupyterHub group 集成

`nbgrader/nbgrader/auth/jupyterhub.py` 中的 `JupyterHubAuthPlugin` 已经被改造成以 Hub group 为课程权限来源：

1. `get_student_courses()` 查询 Hub `/users/{student_id}`。
2. 遍历返回的 groups。
3. 对 `nbgrader-{course}` 和 `formgrade-{course}` 提取 course。
4. 返回用户可访问课程列表。

这使 nbgrader 不再单独维护课程权限，而是接受 JupyterHub group 作为统一事实来源。

### 8.3 教师添加学生时反向同步 Hub

`nbgrader/nbgrader/server_extensions/formgrader/apihandlers.py` 中的 `StudentHandler.put()` 在写入 gradebook 后，会额外执行：

1. 读取 `NBGRADER_COURSE_ID`。
2. 读取 `JUPYTERHUB_API_URL` 和 `JUPYTERHUB_API_TOKEN`。
3. 确保 `nbgrader-{course_id}` group 存在。
4. 将学生加入该 group。

这让教师在 Formgrader 中添加学生时，不只是改 nbgrader 成绩簿，也会同步修改 Hub group。后续学生容器和作业列表就能看到对应课程。

### 8.4 前端中文化

`nbgrader/src/index.ts` 中菜单名称已经中文化：

| 原功能 | 当前显示 |
| --- | --- |
| nbgrader menu | `Nbgrader 评分系统` |
| assignment list | `作业列表` / `作业管理` |
| course list | `课程列表` / `课程管理` |
| formgrader | 评分相关入口 |
| create assignment | 创建作业入口 |

Formgrader 模板中也有中文提示，例如 `manage_assignments.tpl` 中的发布、收集说明。

## 9. JupyterLab 平台层

### 9.1 JupyterLab 源码副本

`jupyterlab/` 是完整 JupyterLab monorepo，而不是只安装 PyPI wheel。它包含：

| 目录 | 作用 |
| --- | --- |
| `jupyterlab/jupyterlab/` | Python 包、LabApp、server extension、构建逻辑 |
| `jupyterlab/packages/` | JupyterLab 前端 packages |
| `jupyterlab/builder/` | labextension 构建工具 |
| `jupyterlab/buildutils/` | monorepo 构建工具 |
| `jupyterlab/dev_mode/` | 开发模式应用 |
| `jupyterlab/galata/` | JupyterLab UI 测试 |
| `jupyterlab/docs/` | 上游文档 |

当前项目主要利用它做三件事：

1. 在 Docker 中以 editable 模式安装。
2. 重新构建前端。
3. 替换 Logo、主题样式和默认设置。

### 9.2 品牌化实现

品牌化有三层：

1. `custom-logo/static/style.js` 运行时注入 CSS，隐藏原 Logo，显示黄色 `Y`。
2. Dockerfile 复制替换 JupyterLab 官方 SVG 图标。
3. Dockerfile 向 Light/Dark theme 的 `index.css` 追加 CSS 兜底。

这种多层覆盖使 Logo 在源码构建、labextension 加载、主题渲染不同阶段都能生效。

### 9.3 默认设置

Dockerfile 写入：

```json
{
  "@jupyterlab/translation-extension:plugin": { "locale": "zh_CN" },
  "@jupyterlab/extensionmanager-extension:plugin": { "enabled": false },
  "@jupyterlab/toc-extension:registry": { "enabled": false }
}
```

含义：

1. 默认简体中文。
2. 禁用扩展管理器，减少学生误操作安装扩展的风险。
3. 禁用目录树扩展。

## 10. 自研扩展一：`jupyterlab-thermal-design`

### 10.1 模块定位

`jupyterlab-thermal-design` 是热设计原理仿真插件，包含：

1. JupyterLab 前端扩展。
2. React 参数面板。
3. 自动 Notebook 生成器。
4. Notebook 参数双向绑定侧边栏。
5. Python 热求解器包 `thermal_solver`。

### 10.2 包结构

| 路径 | 说明 |
| --- | --- |
| `package.json` | 前端扩展配置，依赖 JupyterLab 4、React、Lumino、ECharts。 |
| `setup.py` | Python 包安装配置，安装 labextension 静态资源和 `thermal_solver`。 |
| `src/index.ts` | 插件入口。注册菜单、命令、左侧参数侧边栏。 |
| `src/MainWidget.tsx` | 主工作台 widget。负责调用 `ControlPanel` 并保存 Notebook。 |
| `src/components/ControlPanel.tsx` | 热仿真场景选择和参数输入面板。 |
| `src/components/ParamSidebar.tsx` | 自动参数绑定侧边栏。 |
| `src/utils/NotebookGenerator.ts` | 按场景生成自包含 `.ipynb` JSON。 |
| `src/utils/CellCodeParser.ts` | 扫描 Notebook 中的 `# @param` 元数据。 |
| `src/utils/CellCodeUpdater.ts` | 将侧边栏值写回 Notebook 代码行。 |
| `src/utils/CellExecutionManager.ts` | 自动运行目标单元格及下游单元格。 |
| `thermal_solver/` | Python 传热求解器库。 |

### 10.3 插件挂载方式

`src/index.ts` 是标准 JupyterLab 插件：

```typescript
const plugin: JupyterFrontEndPlugin<void> = {
  id: 'jupyterlab-thermal-design:plugin',
  autoStart: true,
  requires: [ICommandPalette, IMainMenu, INotebookTracker],
  activate: (...)
}
```

它注册了：

| 入口 | 说明 |
| --- | --- |
| 命令 `thermal-design:open-workbench` | 打开热设计工作台 |
| 命令面板分类 `Thermal Design` | 从 Command Palette 访问 |
| 顶部菜单 `热设计仿真系统` | 从主菜单访问 |
| 左侧栏 `ParamSidebarWidget` | 自动参数绑定面板 |

### 10.4 热设计场景

`ControlPanel.tsx` 定义了 4 大类、14 个仿真场景：

| 类别 | 场景 |
| --- | --- |
| 稳态导热 | 无限大平板导热、多层复合平板导热、圆筒壁径向导热、等截面直肋导热、平壁内热源稳态导热、矩形平板二维稳态导热 |
| 瞬态导热 | 两侧恒温加热/冷却、一侧恒定热流 |
| 对流换热 | 平板层流强制对流、平板湍流强制对流、竖板自然对流、圆管内强迫对流 |
| 热辐射 | 平行平板辐射换热、三表面封闭空腔辐射 |

每个场景都有：

1. 默认参数。
2. 参数中文名称。
3. 单位。
4. 上下限验证。
5. 用户点击执行后生成 Notebook。

### 10.5 Notebook 生成路径

`MainWidget.tsx` 中的主流程：

```text
用户选择场景和参数
  |
  v
generateNotebook(scenarioId, params)
  |
  v
生成 nbformat 4 JSON
  |
  v
保存到 仿真结果归档/热设计仿真_{场景名}_{时间戳}.ipynb
  |
  v
执行 docmanager:open 自动打开 Notebook
```

生成的 Notebook 采用教学型结构，典型章节包括：

1. 标题。
2. 仿真问题说明。
3. 模型假设。
4. 参数说明表。
5. 数学模型或计算规则。
6. 参数层代码。
7. 模型层代码。
8. 求解层代码。
9. 结果可视化代码。
10. 关键结果输出。
11. 可修改参数提示。
12. 结果分析提示。

这是项目“白箱式仿真”的核心：用户不是只得到结果，而是得到一份可运行、可读、可改的 Notebook。

### 10.6 `# @param` 双向绑定机制

当前热设计插件已经加入自动参数绑定侧边栏，设计目标来自 `docs/superpowers/specs/2026-05-21-bidirectional-binding-design.md`。

Notebook 参数行格式：

```python
k = 5.0  # @param {"type":"slider","label":"导热系数","min":0.01,"max":5000,"step":0.1,"group":"参数层代码"}
```

执行逻辑：

```text
Notebook 当前活动变化
  |
  v
ParamSidebar 使用 INotebookTracker 获取当前 Notebook
  |
  v
CellCodeParser 扫描 markdown heading 和 code cell
  |
  v
识别 # @param 行，生成控件列表
  |
  v
用户拖动滑块
  |
  v
CellCodeUpdater 写回 Notebook 源码
  |
  v
用户松开滑块或提交输入
  |
  v
CellExecutionManager 运行该参数所在单元格及其下游单元格
  |
  v
Notebook 输出和图表刷新
```

关键实现点：

| 模块 | 职责 |
| --- | --- |
| `scanNotebookParams()` | 遍历 Notebook cells，解析参数行和分组标题 |
| `parseMetadata()` | 支持严格 JSON 和宽松 JSON-like 元数据 |
| `parsePythonValue()` | 将 Python 字面量转换为 JS 值 |
| `updateCellParamValue()` | 保留原代码格式，只替换赋值部分 |
| `runCellAndDownstream()` | 通过 `NotebookActions.runCells()` 运行目标单元格之后的所有单元格 |
| `isSyncingRef` | 避免侧边栏更新和 Notebook 执行事件互相触发死循环 |

支持控件：

| type | 前端控件 |
| --- | --- |
| `slider` | range 滑块 + number 输入框 |
| `number` | number 输入框 |
| `dropdown` | select 下拉框 |
| `boolean` | checkbox 开关 |

### 10.7 `thermal_solver`

`thermal_solver` 是 Python 传热求解器沉淀层。

入口：

```python
from thermal_solver import run_simulation
```

`run_simulation(scenario_id, params)` 通过 `models.ROUTER` 分发到具体求解器，然后输出统一 JSON：

```json
{
  "status": "success",
  "scenario": "...",
  "msg": "计算成功",
  "indicators": {},
  "chart_data": {}
}
```

模型路由包含：

| 类别 | 文件 |
| --- | --- |
| 稳态导热 | `flat_plate.py`、`multilayer_plate.py`、`cylindrical_wall.py`、`straight_fin.py`、`internal_heat.py`、`plate_2d.py` |
| 瞬态导热 | `plate_constant_temp.py`、`plate_constant_flux.py` |
| 对流换热 | `flat_plate_laminar.py`、`flat_plate_turbulent.py`、`vertical_plate_natural.py`、`internal_tube.py` |
| 热辐射 | `parallel_plates.py`、`enclosure_3surface.py` |

当前主链路以 Notebook 生成式为主，`thermal_solver` 更像未来直接通过 Kernel 或后端 API 执行的算法库基础。

## 11. 自研扩展二：`jupyterlab-simulation-platform`

### 11.1 模块定位

`jupyterlab-simulation-platform` 是模板化可见代码 Notebook 生成器。它不局限于热设计，而是面向通用教学仿真场景，让用户通过表单生成包含模型、求解、可视化和分析提示的 Notebook。

### 11.2 包结构

| 路径 | 说明 |
| --- | --- |
| `package.json` | JupyterLab 扩展配置。 |
| `setup.py` | Python 安装配置，发布 labextension 静态资源。 |
| `src/index.ts` | 插件入口。注册 `仿真平台` 菜单和 `仿真模板生成器` 命令。 |
| `src/MainWidget.tsx` | ReactWidget 外壳。 |
| `src/components/SimulationPlatformApp.tsx` | 主应用状态管理，处理模板选择、校验、生成。 |
| `src/components/forms/TemplateForms.tsx` | 不同模板的表单。 |
| `src/components/NotebookPreview.tsx` | Notebook 结构预览。 |
| `src/templates/registry.ts` | 模板注册、默认配置和校验分发。 |
| `src/templates/types.ts` | 模板配置和 Notebook 数据结构类型。 |
| `src/generators/` | 每类模板的 Notebook 片段生成器。 |
| `src/notebook/notebookFactory.ts` | 将生成器片段组装成 nbformat Notebook。 |
| `src/notebook/fileService.ts` | 保存并打开 Notebook。 |
| `src/validators/` | 公式、矩阵、时序、通用输入校验。 |
| `tests/notebook-generators.test.js` | 生成器回归测试。 |

### 11.3 七类模板

`TEMPLATE_REGISTRY` 注册了 7 类模板：

| 模板 ID | 中文名称 | 说明 |
| --- | --- | --- |
| `algebraic-formula` | 代数方程 / 经验公式 | 公式计算、参数扫描、曲线或柱状结果 |
| `first-order-dynamic` | 一阶动态系统 | 一阶 ODE 或状态更新过程 |
| `second-order-dynamic` | 二阶动态系统 | 振动、转子或频率响应简化模型 |
| `linear-system` | 线性方程组 / 网络平衡 | 矩阵方程 `A x = b` 与节点结果图 |
| `one-dimensional-transfer` | 一维传热 / 扩散 | 稳态或瞬态有限差分模板 |
| `time-series-energy-balance` | 时序能量平衡 | 光伏、储能、负荷逐时运行规则 |
| `optimization-dispatch` | 简单优化调度 | 线性规划形式的能源调度优化 |

### 11.4 生成路径

```text
用户选择模板
  |
  v
getDefaultConfig(templateId)
  |
  v
React 表单编辑 SimulationConfig
  |
  v
validateSimulationConfig(config)
  |
  v
saveAndOpenNotebook(app, config)
  |
  v
generateSimulationNotebook(config.templateId, config)
  |
  v
保存到 simulation-platform-results/
  |
  v
docmanager:open 打开 Notebook
```

模板生成器不会把核心计算隐藏成 `run_simulation(params)`，测试中也明确断言生成的 Notebook 不应包含该黑箱调用。这与项目白箱式仿真的设计理念一致。

### 11.5 校验体系

| 校验器 | 作用 |
| --- | --- |
| `validators/common.ts` | 非空、正数、范围、参数列表校验 |
| `validators/formula.ts` | 安全表达式字符集、变量白名单、数学函数白名单 |
| `validators/matrix.ts` | 线性方程组矩阵维度校验 |
| `validators/timeSeries.ts` | 时间序列长度一致性、SOC 上下限校验 |

公式表达式白名单允许常见数学函数，如 `sin`、`cos`、`exp`、`log`、`sqrt`，并禁止双下划线和非白名单标识符，降低用户公式注入风险。

## 12. 作业和课程数据流

### 12.1 教师流程

```text
教师注册
  |
  v
选择 teacher 并填写 course_id
  |
  v
CustomNativeAuthenticator 创建用户
  |
  v
加入 teachers 和 formgrade-{course_id}
  |
  v
登录后 DockerSpawner 启动教师容器
  |
  v
pre_spawn_hook 挂载 /courses/{course_id}
  |
  v
容器内设置 NBGRADER_COURSE_ID
  |
  v
启用 formgrader / create-assignment / course-list
  |
  v
教师创建作业、发布、收集、自动评分、生成反馈
```

### 12.2 学生流程

```text
学生注册
  |
  v
选择 student，可填写 course_id
  |
  v
加入 nbgrader-{course_id}
  |
  v
登录后 DockerSpawner 启动学生容器
  |
  v
没有 NBGRADER_COURSE_ID
  |
  v
后端关闭教师端服务扩展
  |
  v
前端禁用 formgrader 和 create-assignment
  |
  v
学生在作业列表中获取、完成、提交作业
```

### 12.3 nbgrader 文件流

```text
教师 source/
  |
  v
generate assignment
  |
  v
release/
  |
  v
/srv/nbgrader/exchange/{course_id}/
  |
  v
学生 fetch
  |
  v
学生完成 Notebook
  |
  v
学生 submit
  |
  v
exchange
  |
  v
教师 collect
  |
  v
submitted/
  |
  v
autograde/
  |
  v
autograded/
  |
  v
manual grading / feedback/
```

## 13. 仿真数据流

### 13.1 热设计工作台生成 Notebook

```text
热设计菜单
  |
  v
ControlPanel 选择场景
  |
  v
填写参数
  |
  v
NotebookGenerator 生成教学型 Notebook
  |
  v
保存到 仿真结果归档/
  |
  v
打开 Notebook
  |
  v
用户运行、修改、分析
```

### 13.2 参数侧边栏实时联动

```text
打开含 # @param 的 Notebook
  |
  v
左侧参数侧边栏自动扫描
  |
  v
生成滑块、输入框、下拉框、开关
  |
  v
用户调参
  |
  v
Notebook 源码同步变化
  |
  v
自动运行目标单元格及下游
  |
  v
图表和关键结果刷新
```

### 13.3 通用模板生成器

```text
仿真平台菜单
  |
  v
选择 7 类模板之一
  |
  v
填写公式、矩阵、时序、优化约束等配置
  |
  v
前端校验
  |
  v
生成 12 栏结构 Notebook
  |
  v
保存到 simulation-platform-results/
  |
  v
打开 Notebook
```

## 14. 实现路径总结

### 阶段一：JupyterHub + DockerSpawner 基础平台

实现内容：

1. 配置 JupyterHub 监听 `0.0.0.0:8000`。
2. 使用 `ConfigurableHTTPProxy`。
3. 接入 `DockerSpawner`，让每个用户进入独立容器。
4. 配置用户家目录卷和 exchange 共享目录。
5. 默认进入 `/lab`。

结果：形成多用户隔离的 JupyterLab 平台。

### 阶段二：角色化注册与课程分组

实现内容：

1. 继承 NativeAuthenticator。
2. 替换注册页面。
3. 注册时读取教师/学生角色和课程 ID。
4. 使用 Hub ORM 自动创建并维护 `teachers`、`formgrade-*`、`nbgrader-*`。

结果：课程和角色从页面字段变成了后端可持久化、可授权、可查询的 Hub group。

### 阶段三：nbgrader 多课程集成

实现内容：

1. 容器内使用 `JupyterHubAuthPlugin`。
2. `Exchange.path_includes_course = True` 支持多课程交换目录。
3. 教师容器注入 `NBGRADER_COURSE_ID`。
4. 教师在 formgrader 添加学生时反向同步 Hub group。
5. 学生容器禁用教师端扩展。

结果：作业发布、提交、收集、评分与 JupyterHub 课程权限打通。

### 阶段四：JupyterLab 品牌化和中文化

实现内容：

1. 使用 JupyterLab 源码副本进行构建。
2. 安装中文语言包并设置默认 `zh_CN`。
3. 替换 Logo SVG。
4. 添加 CSS-only `custom-logo` 扩展。
5. 注入主题 CSS 兜底。
6. 对 nbgrader 前端菜单和模板中文化。

结果：平台从默认 Jupyter 风格变成中文教学仿真系统风格。

### 阶段五：热设计仿真插件

实现内容：

1. 创建 `jupyterlab-thermal-design` labextension。
2. 注册 `热设计仿真系统` 菜单。
3. 实现 React 参数面板。
4. 定义 14 个热设计场景。
5. 生成包含公式、模型、求解代码、图表和分析提示的 Notebook。
6. 沉淀 Python `thermal_solver` 求解器库。

结果：平台不再只是作业系统，而具备专业热设计实验能力。

### 阶段六：通用仿真模板平台

实现内容：

1. 创建 `jupyterlab-simulation-platform` labextension。
2. 定义 7 类模板。
3. 建立模板配置类型、默认值、表单、校验器、生成器。
4. 统一生成 12 栏可见代码 Notebook。
5. 加入生成器测试。

结果：热设计之外的通用建模、动态系统、能量系统和优化调度也能通过模板生成 Notebook。

### 阶段七：Notebook 参数双向绑定

实现内容：

1. 设计 `# @param` 注释语法。
2. 通过 `INotebookTracker` 监听当前 Notebook。
3. 扫描代码单元格生成侧边栏控件。
4. 控件变化写回 Notebook 源码。
5. 自动运行参数单元格及下游。
6. 手动执行 Notebook 后反向刷新侧边栏。

结果：从“一次性生成 Notebook”升级为“Notebook 和前端侧边栏可联动的交互式实验环境”。

## 15. 构建与运行方式

### 15.1 构建单用户镜像

在项目根目录执行：

```bash
docker build -f Dockerfile-nbgrader -t my_jupyterhub:latest .
```

`.dockerignore` 使用默认拒绝策略，只把 Dockerfile 需要的目录重新纳入构建上下文，避免把课程数据、数据库、node_modules、生成产物带进镜像。

### 15.2 启动 JupyterHub

常见启动方式：

```bash
jupyterhub -f jupyterhub_config.py
```

启动前需要确保：

1. Docker 服务可用。
2. `my_jupyterhub:latest` 已构建。
3. `/srv/nbgrader/exchange` 存在并可写。
4. `configurable-http-proxy` 路径与 `jupyterhub_config.py` 一致。
5. 当前用户有 Docker 权限。

### 15.3 自研扩展本地构建

热设计扩展：

```bash
cd jupyterlab-thermal-design
jlpm install
jlpm run build
```

通用仿真平台：

```bash
cd jupyterlab-simulation-platform
jlpm install
jlpm run build
```

通用仿真平台测试：

```bash
cd jupyterlab-simulation-platform
npm run test
```

热设计参数解析测试：

```bash
cd jupyterlab-thermal-design
npx ts-node src/utils/test-parser.ts
```

## 16. 测试与验证现状

当前项目中可见的验证点：

| 位置 | 验证内容 |
| --- | --- |
| `jupyterlab-simulation-platform/tests/notebook-generators.test.js` | 7 类模板都能生成包含参数层、模型层、求解层、可视化层和分析提示的 Notebook |
| `jupyterlab-simulation-platform/tests/notebook-generators.test.js` | 公式校验拒绝未知变量 |
| `jupyterlab-simulation-platform/tests/notebook-generators.test.js` | 文件名清理非法字符 |
| `jupyterlab-thermal-design/src/utils/test-parser.ts` | `# @param` 扫描、分组、更新滑块值、更新下拉值、更新布尔值 |
| `course_test/` | 本地 nbgrader 测试课程与作业样例 |

建议后续增加：

1. JupyterHub 注册流程端到端测试。
2. 教师/学生容器扩展启用状态验证。
3. nbgrader 添加学生后 Hub group 同步测试。
4. 热设计 14 个场景 Notebook 生成快照测试。
5. `ParamSidebar` 在真实 Notebook 中的 Playwright/Galata UI 测试。
6. Docker 镜像构建 smoke test。

## 17. 当前工程注意点

### 17.1 工作区存在未提交变化

当前 `git status --short` 显示 `jupyterlab-thermal-design` 有修改和新增文件，包括：

| 状态 | 内容 |
| --- | --- |
| 修改 | `src/index.ts`、`src/utils/NotebookGenerator.ts`、`style/base.css`、`yarn.lock` |
| 新增 | `src/components/ParamSidebar.tsx`、`src/utils/CellCodeParser.ts`、`src/utils/CellCodeUpdater.ts`、`src/utils/CellExecutionManager.ts`、`src/utils/test-parser.ts` |
| 新增目录 | `docs/` |
| 删除 | 多个 PPTX 文件 |

这说明双向绑定功能处于当前工作区变更中，尚未形成干净提交。

### 17.2 `setup.py` 包列表需要复核

`jupyterlab-thermal-design/setup.py` 显式列出了多个 `thermal_solver` 子包，但当前列表中没有看到 `thermal_solver.models.transient_conduction`。如果依赖 `setup.py` 打包安装，瞬态导热子包可能需要补充进 `packages`。

更稳妥的方式是使用：

```python
packages=find_packages()
```

或显式补全所有子包。

### 17.3 测试环境弱密码配置

`jupyterhub_config.py` 中：

```python
c.CustomNativeAuthenticator.minimum_password_length = 1
```

这是测试便利配置。正式环境应提高密码长度，并考虑关闭开放注册或启用审批。

### 17.4 运行数据和敏感文件

项目根目录可以看到：

| 文件 | 风险 |
| --- | --- |
| `jupyterhub.sqlite` | 包含本地用户、组和服务状态 |
| `jupyterhub_cookie_secret` | Hub cookie secret |
| `courses/**/submission_secret.txt` | nbgrader 提交校验 secret |
| `courses/**/gradebook.db` | 成绩数据 |

这些文件已在 `.gitignore` 中列入排除范围。维护时应继续避免提交。

### 17.5 学生端安全依赖双层控制

学生端当前通过：

1. 后端 server extension 禁用。
2. 前端 labextension 禁用。

共同限制教师功能。后续如果新增教师插件，也应同时在 `docker_jupyter_server_config.py` 和 `disable_teacher_extensions.sh` 中补齐控制。

## 18. 项目价值总结

这个项目已经形成了比较完整的教学仿真平台架构：

1. JupyterHub 提供多用户、容器隔离、身份和权限基础。
2. nbgrader 提供课程作业和评分闭环。
3. JupyterLab 提供可扩展浏览器 IDE。
4. 自研热设计插件提供专业传热仿真实验。
5. 通用仿真模板平台提供更宽泛的建模和 Notebook 生成能力。
6. `# @param` 双向绑定把静态 Notebook 进一步升级为可交互实验界面。

最核心的工程设计是把“课程”和“角色”统一绑定到 JupyterHub group 上，然后让 Spawner、nbgrader、JupyterLab 扩展开关和课程目录挂载都围绕这个事实来源工作。这样系统能够同时支持多课程、教师管理、学生隔离、作业流转和仿真实验扩展。

从实现路径看，项目已经不是简单的 JupyterHub 配置项目，而是一套基于 Jupyter 生态二次开发的教学科研平台。后续如果继续推进，应优先清理提交状态、补齐打包细节、提高正式部署安全配置，并为关键链路增加端到端测试。
