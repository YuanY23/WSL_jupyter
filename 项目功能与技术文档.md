# Jupyter 教学管理平台 — 项目功能与技术文档

## 一、项目总体概述

本项目是一个**基于 Docker 容器化部署的 Jupyter 在线教学管理平台**，核心定位为高校课程教学与实验环境。平台深度定制了 JupyterHub、JupyterLab 与 nbgrader 三大开源组件，并自主开发了**热设计原理仿真插件**，实现了从用户注册、角色权限管理、作业分发/提交/自动评分到工程仿真实验的全流程功能闭环。

### 技术栈总览

| 层级 | 技术 |
|------|------|
| 容器化部署 | Docker, DockerSpawner |
| 多用户管理 | JupyterHub 4.x |
| 交互式编程环境 | JupyterLab 4.2.7 |
| 作业管理系统 | nbgrader (定制版) |
| 后端语言 | Python 3 |
| 前端框架 | TypeScript, React 18, Lumino Widgets |
| 数值计算 | NumPy, SciPy, Numba |
| 数据可视化 | Matplotlib, ECharts |
| Web 框架 | Tornado |
| 数据库 | SQLite (JupyterHub), SQLAlchemy (nbgrader) |
| 认证系统 | NativeAuthenticator (定制版) |
| 模板引擎 | Jinja2 |
| 国际化 | jupyterlab-language-pack-zh-CN |
| 包管理 | Conda (Mamba), pip, npm/jlpm |

---

## 二、平台架构

```
┌─────────────────────────────────────────────────────────────┐
│                        宿主机 (Linux)                        │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐    │
│  │              JupyterHub (主控服务)                    │    │
│  │   · 用户认证 (CustomNativeAuthenticator)             │    │
│  │   · RBAC 角色管理                                    │    │
│  │   · DockerSpawner → 为每个用户创建独立容器            │    │
│  └───────────┬─────────────────────────────┬───────────┘    │
│              │                             │                │
│    ┌─────────▼──────────┐       ┌──────────▼─────────┐     │
│    │  教师容器 (Docker)   │       │  学生容器 (Docker)  │     │
│    │  · JupyterLab       │       │  · JupyterLab      │     │
│    │  · Formgrader       │       │  · Assignment List  │     │
│    │  · Create Assignment│       │  · 热设计仿真插件   │     │
│    │  · 热设计仿真插件   │       │  (无教师工具)       │     │
│    │  · 课程目录挂载     │       └────────────────────┘     │
│    └────────────────────┘                                   │
│              │                             │                │
│    ┌─────────▼─────────────────────────────▼───────────┐    │
│    │           共享 Exchange 目录 (/srv/nbgrader)        │    │
│    │           (作业分发 / 提交 / 收集)                   │    │
│    └───────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

---

## 三、功能详细介绍

### 3.1 用户认证与注册系统

#### 功能描述

- **自助注册**：支持开放注册（无需管理员审批），用户可自主选择角色（教师/学生）和所属课程
- **角色选择**：注册时可选 "学生" 或 "老师"，教师必须填写课程 ID（用于创建新课程），学生可选填课程 ID（加入已有班级）
- **密码安全**：支持密码确认、密码可见性切换、最小密码长度验证
- **2FA 支持**：可选开启双因素认证 (TOTP)
- **reCAPTCHA**：可选集成 Google reCAPTCHA 防机器人注册
- **中文消息反馈**：所有注册错误/成功提示信息均已翻译为简体中文

#### 使用的技术

| 技术 | 作用 |
|------|------|
| `NativeAuthenticator` | JupyterHub 原生认证器基类，提供用户名/密码注册和验证 |
| `CustomNativeAuthenticator` | 自定义扩展类，增加角色和课程分组逻辑 |
| `CustomSignUpHandler` | 自定义 Tornado RequestHandler，重写 POST 处理和消息翻译 |
| `JupyterHub ORM (orm.User, orm.Group)` | 直接操作 Hub 数据库创建用户和分组 |
| `Jinja2 ChoiceLoader` | 模板加载器链，实现自定义模板优先加载 |
| `Tornado Web Framework` | 异步 Web 请求处理 |

#### 核心逻辑（`custom_authenticator.py`）

```python
# 教师注册 → 自动加入 formgrade-{course_id} 和 teachers 组
# 学生注册 → 自动加入 nbgrader-{course_id} 组
# 这些分组决定了用户在 nbgrader 中的权限
```

---

### 3.2 前端页面定制（自定义模板系统）

#### 功能描述

平台完全重写了 JupyterHub 的 4 个核心 HTML 页面，采用现代 UI 设计风格：

| 页面 | 文件 | 功能 |
|------|------|------|
| 登录页 | `templates/native-login.html` | 用户名密码登录，2FA 输入，HTTP 安全警告 |
| 注册页 | `templates/signup.html` | 角色选择、课程填写、密码确认 |
| 主控制面板 | `templates/home.html` | 启动/停止服务器，命名服务器管理 |
| 等待页 | `templates/spawn_pending.html` | 容器启动过程中的加载动画和进度追踪 |

#### 使用的技术

| 技术 | 作用 |
|------|------|
| **Glassmorphism（毛玻璃效果）** | `backdrop-filter: blur(24px)` 实现半透明卡片效果 |
| **CSS 动画** | `@keyframes float` 浮动光球背景、`slide-up` 入场动画、`spin` 加载旋转 |
| **Google Fonts (Inter)** | 现代无衬线字体，搭配 PingFang SC / 微软雅黑中文回退 |
| **Font Awesome 6.4** | 图标库（登录/退出/启动等图标） |
| **SSE (Server-Sent Events)** | `spawn_pending.html` 使用 EventSource 实时监听容器启动进度 |
| **Jinja2 模板继承** | home.html 继承 `page.html` 基础模板 |
| **响应式设计** | 全页面 Flexbox 布局，适配不同屏幕尺寸 |
| **全中文本地化** | 所有 UI 文案、按钮、提示均为简体中文 |

---

### 3.3 Docker 容器化部署

#### 功能描述

- 每个用户登录后自动获得一个**独立的 Docker 容器**作为工作环境
- 教师容器挂载对应课程目录，学生容器共享 Exchange 目录
- 容器停止后自动清理（`remove = True`）
- 用户数据通过 Docker Volume 持久化

#### 使用的技术

| 技术 | 作用 |
|------|------|
| `DockerSpawner` | JupyterHub 的 Docker 集成 Spawner，为每用户创建容器 |
| `jupyter/base-notebook:ubuntu-22.04` | 官方基础镜像 |
| `Dockerfile-nbgrader` | 多阶段构建，安装所有依赖和插件 |
| `Docker Volume` | `jupyterhub-user-{username}` 卷持久化用户目录 |
| `Bind Mount` | 宿主机课程目录、Exchange 目录挂载到容器 |
| `pre_spawn_hook` | Python 钩子函数，在容器启动前动态注入挂载卷和环境变量 |
| `Mamba/Conda` | 容器内 Python 环境管理，安装 JupyterLab、nbgrader 等 |

#### Dockerfile 关键构建步骤

```dockerfile
# 1. 安装系统依赖（git, nodejs, 中文字体）
# 2. 安装 Python 包（jupyterlab, nbgrader, numpy, scipy, numba, matplotlib）
# 3. 安装中文语言包（jupyterlab-language-pack-zh-CN）
# 4. 编译安装自定义 JupyterLab 源码
# 5. 编译安装自定义 nbgrader 源码
# 6. 编译安装热设计仿真插件
# 7. 替换 Logo/Favicon 为自定义黄色 "Y" 标识
# 8. 注入 CSS 覆盖主题 Logo
# 9. 设置中文为默认语言
# 10. 重建 JupyterLab 前端
```

---

### 3.4 角色权限管理 (RBAC)

#### 功能描述

平台实现了教师/学生两级权限分离：

| 功能 | 教师 | 学生 |
|------|:----:|:----:|
| Formgrader（评分面板） | ✅ | ❌ |
| Create Assignment（创建作业） | ✅ | ❌ |
| Assignment List（领取/提交作业） | ✅ | ✅ |
| Course List（课程列表） | ✅ | ❌ |
| 热设计仿真插件 | ✅ | ✅ |
| Notebook 编程环境 | ✅ | ✅ |

#### 使用的技术

| 技术 | 作用 |
|------|------|
| **JupyterHub Groups** | `formgrade-{course_id}` (教师组), `nbgrader-{course_id}` (学生组), `teachers` (全局教师组) |
| **JupyterHub RBAC Roles** | `formgrade-teacher` 角色，授予 `list:users`, `read:users`, `admin:groups` 等权限 |
| **环境变量 `NBGRADER_COURSE_ID`** | 教师容器注入此变量，学生容器不注入，以此区分角色 |
| **`docker_jupyter_server_config.py`** | 根据环境变量动态禁用学生端服务器扩展 (Formgrader, Course List 后端 API) |
| **`disable_teacher_extensions.sh`** | 容器启动钩子脚本，写入 `page_config.json` 禁用学生端前端扩展 |
| **`NBGRADER_ADMIN_API_TOKEN`** | 仅注入教师容器，允许 Formgrader 管理 JupyterHub 用户分组 |

---

### 3.5 nbgrader 作业管理系统

#### 功能描述

完整的作业生命周期管理：

```
教师创建作业 (source/) → 生成学生版 (release/) → 发布到 Exchange
    → 学生领取 → 学生完成作业 → 学生提交 → 教师收集
    → 自动评分 (autograded/) → 教师手动复批 → 生成反馈
```

- **多课程支持**：不同教师可管理不同课程，课程间完全隔离
- **自动评分**：nbgrader 支持基于测试用例的自动代码评分
- **Exchange 目录**：通过共享文件系统实现跨容器的作业分发和收集

#### 使用的技术

| 技术 | 作用 |
|------|------|
| `nbgrader` | Jupyter 生态的作业管理扩展 |
| `JupyterHubAuthPlugin` | nbgrader 认证插件，通过 Hub Groups 判断用户权限 |
| `Exchange.path_includes_course = True` | 多课程支持，Exchange 路径包含课程 ID 前缀 |
| `CourseDirectory.course_id` | 动态设置课程 ID（通过环境变量注入） |
| `SQLAlchemy / Alembic` | nbgrader 数据库 ORM 和迁移 |
| 共享 Volume `/srv/nbgrader/exchange` | 跨容器作业分发通道 |

---

### 3.6 JupyterLab 界面定制

#### 功能描述

- **自定义 Logo**：将默认 Jupyter Logo 替换为黄色 "Y" 品牌标识
- **Favicon 替换**：自定义网站图标
- **中文默认语言**：JupyterLab 界面默认显示简体中文
- **扩展管理器禁用**：防止用户安装未经授权的扩展
- **目录面板禁用**：简化界面

#### 使用的技术

| 技术 | 作用 |
|------|------|
| 自定义 SVG 图标 | 替换 `jupyter.svg`, `jupyter-favicon.svg`, `jupyterlab-wordmark.svg` |
| CSS 注入 (主题覆盖) | 通过 `index.css` 追加 CSS 隐藏 SVG 并用 `::before` 伪元素显示 "Y" Logo |
| `overrides.json` | JupyterLab 设置覆盖，配置默认语言和禁用扩展 |
| `jupyterlab-language-pack-zh-CN` | 简体中文语言包 |
| `custom-logo` labextension | 自定义 Logo 以 labextension 形式发布 |

---

### 3.7 热设计原理仿真插件 (`jupyterlab-thermal-design`)

这是本项目的**核心创新功能**，是一个完整的 JupyterLab 扩展插件，实现了传热学课程的交互式工程仿真。

#### 3.7.1 整体架构

```
┌──────────────────────────────────────────────────────────────┐
│                   JupyterLab 前端                             │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  index.ts (插件入口)                                    │  │
│  │  · 注册命令: thermal-design:open-workbench             │  │
│  │  · 添加菜单: "热设计仿真系统"                           │  │
│  │  · 添加命令面板快捷方式                                 │  │
│  └───────────────────────┬────────────────────────────────┘  │
│                          │                                    │
│  ┌───────────────────────▼────────────────────────────────┐  │
│  │  MainWidget.tsx (工作台主容器)                           │  │
│  │  · ThermalDesignWorkbench (Lumino Widget)               │  │
│  │  · ControlPanelWidget (ReactWidget 桥接)                │  │
│  └───────────────────────┬────────────────────────────────┘  │
│                          │                                    │
│  ┌───────────────────────▼────────────────────────────────┐  │
│  │  ControlPanel.tsx (React 参数控制面板)                   │  │
│  │  · 4 大类别, 14 个仿真场景选择                          │  │
│  │  · 动态参数表单（含中文标签、单位、上下限）              │  │
│  │  · "开始仿真" 触发 Notebook 生成                        │  │
│  └───────────────────────┬────────────────────────────────┘  │
│                          │                                    │
│  ┌───────────────────────▼────────────────────────────────┐  │
│  │  NotebookGenerator.ts (Notebook 自动生成器)             │  │
│  │  · 为每个场景生成自包含 .ipynb JSON                     │  │
│  │  · 包含 Markdown 物理模型说明 + 控制方程                │  │
│  │  · 包含参数定义代码 + 完整求解算法 + Matplotlib 可视化  │  │
│  └───────────────────────┬────────────────────────────────┘  │
│                          │                                    │
│  ┌───────────────────────▼────────────────────────────────┐  │
│  │  ContentsManager API                                    │  │
│  │  · 保存 Notebook 到 "仿真结果归档/" 目录                │  │
│  │  · 自动在 JupyterLab 中打开生成的 Notebook              │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                              │
├──────────────────────────────────────────────────────────────┤
│                   Python 后端 (thermal_solver)                │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  core/base.py — run_simulation() 求解器统一入口         │  │
│  │  models/ — 14 个场景求解器，按物理类别分包               │  │
│  │  ROUTER 字典 — 场景 ID 到求解函数的映射                 │  │
│  └────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
```

#### 3.7.2 前端技术栈

| 技术 | 作用 |
|------|------|
| **TypeScript 5.0** | 类型安全的前端开发语言 |
| **React 18** | ControlPanel 组件的 UI 框架 |
| **Lumino Widgets** | JupyterLab 底层窗口管理框架，管理标签页 |
| **ReactWidget** | JupyterLab 提供的 Lumino↔React 桥接组件 |
| **JupyterLab Extension API** | `JupyterFrontEndPlugin`, `ICommandPalette`, `IMainMenu` |
| **ContentsManager API** | 通过 JupyterLab 服务层保存和打开 Notebook 文件 |
| **ECharts 6.0** | 前端图表库（依赖中列出） |

#### 3.7.3 仿真场景全览（14 个场景，4 大类别）

##### 类别一：稳态导热 (Steady Conduction) — 6 个场景

| # | 场景名称 | 数值方法 | 核心算法 | 求解器文件 |
|---|---------|---------|---------|-----------|
| 1 | 无限大平板导热 | FDM 有限差分法 | 三对角线性方程组 `np.linalg.solve` | `flat_plate.py` |
| 2 | 多层复合平板导热 | FVM 有限体积法 | 界面热阻处理 + 线性方程组求解 | `multilayer_plate.py` |
| 3 | 圆筒壁径向导热 | FDM 有限差分法 | 柱坐标中心差分 | `cylindrical_wall.py` |
| 4 | 等截面直肋导热 | FVM 有限体积法 | 考虑对流散热项的修正方程组 | `straight_fin.py` |
| 5 | 平壁内热源稳态导热 | FDM 有限差分法 | 含源项的泊松方程离散 | `internal_heat.py` |
| 6 | 二维矩形平板稳态导热 | FDM 有限差分法 | 2D 拉普拉斯方程五点格式 | `plate_2d.py` |

##### 类别二：瞬态导热 (Transient Conduction) — 2 个场景

| # | 场景名称 | 数值方法 | 核心算法 | 求解器文件 |
|---|---------|---------|---------|-----------|
| 7 | 两侧恒温加热 | FTCS 显式格式 | 前向时间中心空间差分，Fo≤0.5 稳定性控制 | `plate_constant_temp.py` |
| 8 | 一侧恒定热流 | 隐式 FVM | 隐式时间推进，无条件稳定 | `plate_constant_flux.py` |

##### 类别三：对流换热 (Convection) — 4 个场景

| # | 场景名称 | 分析方法 | 核心关联式 | 求解器文件 |
|---|---------|---------|-----------|-----------|
| 9 | 平板层流强制对流 | 经验关联式 | `Nu_x = 0.332·Re_x^{1/2}·Pr^{1/3}` (Blasius) | `flat_plate_laminar.py` |
| 10 | 平板湍流强制对流 | 经验关联式 | 湍流 Nu 关联式 | `flat_plate_turbulent.py` |
| 11 | 竖板自然对流 | 经验关联式 | Grashof/Rayleigh 数关联 | `vertical_plate_natural.py` |
| 12 | 圆管内强迫对流 | 经验关联式 | Dittus-Boelter 等关联式 | `internal_tube.py` |

##### 类别四：热辐射 (Radiation) — 2 个场景

| # | 场景名称 | 分析方法 | 核心公式 | 求解器文件 |
|---|---------|---------|---------|-----------|
| 13 | 平行平板辐射换热 | 辐射网络法 | `q = σ·ε_eff·(T₁⁴−T₂⁴)` + 遮热板分析 | `parallel_plates.py` |
| 14 | 三表面封闭空腔辐射 | 辐射网络法 | 角系数 + 多表面辐射方程组 | `enclosure_3surface.py` |

#### 3.7.4 后端求解器技术

| 技术 | 作用 |
|------|------|
| **NumPy** | 矩阵运算、线性方程组求解 (`np.linalg.solve`)、数组操作 |
| **SciPy** | 稀疏矩阵求解、高级数值算法 |
| **Numba** | JIT 编译加速数值计算 (依赖中列出) |
| **FDM (有限差分法)** | 将微分方程离散为代数方程组 |
| **FVM (有限体积法)** | 基于守恒积分的离散方法 |
| **FTCS 显式格式** | 前向时间中心空间差分，适用于瞬态问题 |
| **隐式差分格式** | 无条件稳定的时间推进方法 |
| **经验关联式** | Re、Nu、Pr、Gr、Ra 等无量纲数关联式 |
| **解析解对比** | 每个场景均提供解析解用于验证数值精度 |

#### 3.7.5 Notebook 自动生成器 (`NotebookGenerator.ts`)

**核心亮点**：用户通过控制面板选择仿真场景并设置参数后，前端会自动生成一个**完全自包含的 Jupyter Notebook**（.ipynb 文件），包含：

1. **Markdown 单元格**：物理模型描述 + LaTeX 控制方程
2. **参数定义代码**：用户可直接修改参数重新运行
3. **完整求解算法代码**：Python 数值求解代码（内嵌，无需额外导入）
4. **Matplotlib 可视化代码**：自动生成温度分布/换热系数等图表

生成的 Notebook 自动保存到 `仿真结果归档/` 目录，并在 JupyterLab 中打开。

| 技术 | 作用 |
|------|------|
| **nbformat JSON 规范** | 按 Jupyter Notebook v4 格式构建 JSON 结构 |
| **ContentsManager.save()** | 通过 JupyterLab 服务 API 保存文件 |
| **docmanager:open 命令** | 自动在编辑器中打开生成的 Notebook |
| **Matplotlib + 中文字体** | 仿真结果可视化，支持 SimHei 中文字体渲染 |

---

### 3.8 品牌定制

#### 功能描述

- 自定义黄色 "Y" Logo，替换 JupyterLab 默认 Logo
- 自定义 Favicon
- 通过 CSS 注入实现 Logo 显示（渐变色圆角方形 + 黑色粗体 "Y"）

#### 使用的技术

| 技术 | 作用 |
|------|------|
| SVG 图标替换 | 替换 `jupyter.svg` / `jupyterlab-wordmark.svg` |
| CSS `::before` 伪元素 | 隐藏默认 SVG，用伪元素渲染品牌 Logo |
| CSS 渐变 + 阴影 | `linear-gradient(135deg, #f59e0b, #fbbf24)` 金色渐变 |
| `custom-logo` labextension | 以 JupyterLab 扩展形式分发自定义 Logo 资源 |
| `convert_ico.py` | Python 脚本将 PNG 转换为 ICO 格式 Favicon |

---

### 3.9 开发模式支持

#### 功能描述

通过 Volume 挂载实现**热更新开发**：宿主机代码修改后无需重新构建 Docker 镜像即可生效。

| 挂载路径 | 说明 |
|---------|------|
| `/home/yuan/my_project/nbgrader → /src/nbgrader` | nbgrader 源码实时挂载 |
| `/home/yuan/my_project/jupyterlab → /opt/jupyterlab` | JupyterLab 源码实时挂载 |

---

## 四、项目文件结构

```
my_project/
├── jupyterhub_config.py            # JupyterHub 主配置（认证、Spawner、RBAC）
├── custom_authenticator.py         # 自定义认证器（角色注册、分组管理）
├── Dockerfile-nbgrader             # 容器镜像构建文件
├── docker_nbgrader_config.py       # 容器内 nbgrader 全局配置
├── docker_jupyter_server_config.py # 容器内动态扩展控制
├── disable_teacher_extensions.sh   # 容器启动钩子（禁用学生端教师扩展）
├── nbgrader_config.py              # 宿主机 nbgrader 配置
│
├── templates/                      # 自定义 Jinja2 前端页面
│   ├── native-login.html           # 登录页
│   ├── signup.html                 # 注册页
│   ├── home.html                   # 主控制面板
│   └── spawn_pending.html          # 容器启动等待页
│
├── custom-logo/                    # 自定义 Logo labextension
├── yellow-y.svg                    # Logo SVG 源文件
├── custom-favicon.ico              # 自定义网站图标
├── convert_ico.py                  # PNG→ICO 转换脚本
│
├── courses/                        # 课程数据目录（教师挂载）
│   ├── course_test/
│   ├── english2/
│   ├── math1/
│   └── ...
│
├── jupyterlab/                     # 定制版 JupyterLab 4.2.7 源码
├── nbgrader/                       # 定制版 nbgrader 源码
│
└── jupyterlab-thermal-design/      # 热设计仿真插件
    ├── package.json                # npm 配置（依赖、构建脚本）
    ├── setup.py                    # Python 后端包配置
    ├── tsconfig.json               # TypeScript 编译配置
    ├── src/                        # TypeScript 前端源码
    │   ├── index.ts                # 插件注册入口
    │   ├── MainWidget.tsx          # 工作台主容器
    │   ├── components/
    │   │   └── ControlPanel.tsx    # React 参数控制面板
    │   └── utils/
    │       └── NotebookGenerator.ts # Notebook 自动生成器
    ├── thermal_solver/             # Python 求解器后端
    │   ├── core/
    │   │   └── base.py             # 统一求解入口
    │   └── models/                 # 14 个场景求解器
    │       ├── steady_conduction/  # 稳态导热 (6 个)
    │       ├── transient_conduction/ # 瞬态导热 (2 个)
    │       ├── convection/         # 对流换热 (4 个)
    │       └── radiation/          # 热辐射 (2 个)
    └── 仿真结果归档/                # 生成的仿真 Notebook 存档
```

---

## 五、技术亮点总结

1. **全栈容器化**：基于 Docker + DockerSpawner 的多用户隔离环境，每用户独立容器
2. **自定义认证系统**：扩展 NativeAuthenticator 实现角色+课程的自动分组注册
3. **双层权限控制**：服务端（`jpserver_extensions`）+ 客户端（`page_config.json`）双重扩展禁用
4. **深度 UI 定制**：Glassmorphism 风格、CSS 动画、全中文本地化的现代化教学界面
5. **14 场景工程仿真**：覆盖传热学四大领域（导热、瞬态、对流、辐射），使用 FDM/FVM/FTCS 等数值方法
6. **Notebook 自动生成**：前端参数 → 自包含求解 Notebook，兼顾交互性和可编辑性
7. **解析解验证**：每个数值求解场景均提供解析解对比，确保计算精度可验证
8. **nbgrader 多课程集成**：支持多教师多课程的作业管理，通过 JupyterHub Groups 实现权限隔离
9. **实时开发模式**：通过 Volume 挂载实现源码热更新，免重建容器
10. **品牌化定制**：自定义 Logo、Favicon、主题色，打造专属教学平台形象
