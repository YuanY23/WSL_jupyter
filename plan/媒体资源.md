# 媒体资源 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把顶部菜单升级为“管理员工具”，菜单下直接提供“课程管理”“上传媒体资源”“插入媒体资源”三个管理员功能，并支持上传、重命名、删除视频媒体资源，以及从右侧侧边栏把已上传视频资源引用插入当前 Notebook cell。

**Architecture:** 媒体资源作为 `simlab-training-service` 的管理员资源管理能力保存，前端 `jupyterlab-training-platform` 只通过管理员接口上传和查询媒体。课程管理和上传媒体资源作为主区域页面打开；插入媒体资源作为右侧侧边栏打开，避免选中 Notebook cell 后因主区域切换丢失上下文。Notebook 中只插入 `::simlab-video` 引用块，不保存视频本体；本计划不做视频加密、HLS 切片、学生端播放器和播放授权。

**Tech Stack:** FastAPI, SQLAlchemy, Alembic, JupyterLab 4, React, TypeScript, NotebookTracker, Node test scripts, pytest.

---

## 范围

本计划包含：

- 将顶部管理员菜单名称改为“管理员工具”。
- 在“管理员工具”菜单下直接提供三个命令：`课程管理`、`上传媒体资源`、`插入媒体资源`。
- 后端增加媒体资源表、视频上传接口、媒体资源列表接口。
- 后端增加媒体资源重命名和删除接口。
- 前端增加媒体资源上传、重命名、删除 UI。
- 前端增加右侧媒体资源选择侧边栏，并把媒体引用插入当前 Notebook cell。
- 插入内容使用平台内部引用，不暴露视频真实文件地址。

本计划不包含：

- 视频加密。
- HLS/DASH 分片。
- DRM。
- 学生端视频播放器。
- `::simlab-video` 到播放器的渲染转换。
- 视频转码、封面、预览。

## 文件结构

后端：

- Modify: `simlab-training-service/simlab_training_service/models.py`
  - 新增 `MediaResource` 模型。
- Add: `simlab-training-service/alembic/versions/0003_add_media_resources.py`
  - 创建 `media_resources` 表。
- Modify: `simlab-training-service/simlab_training_service/storage.py`
  - 增加媒体文件保存能力，仍然不暴露静态文件下载地址。
- Add: `simlab-training-service/simlab_training_service/routes_media_admin.py`
  - 管理员上传、列出、重命名、删除媒体资源。
- Modify: `simlab-training-service/simlab_training_service/app.py`
  - 注册媒体管理员路由。
- Add: `simlab-training-service/tests/test_media_resources.py`
  - 覆盖管理员上传、列表、重命名、删除、非管理员拒绝、非视频拒绝。

前端：

- Modify: `jupyterlab-training-platform/src/index.tsx`
  - 管理员菜单改为“管理员工具”，菜单下直接注册课程管理、上传媒体资源、插入媒体资源三个命令。
  - 插入媒体资源命令打开右侧侧边栏。
- Modify: `jupyterlab-training-platform/src/components/AdminManager.tsx`
  - 保持为课程管理页面，不承载媒体功能页签。
- Add: `jupyterlab-training-platform/src/components/MediaUploadPanel.tsx`
  - 上传本地视频文件，并管理媒体资源的重命名和删除。
- Add: `jupyterlab-training-platform/src/components/MediaInsertPanel.tsx`
  - 查询媒体列表，作为右侧侧边栏选择后插入当前 Notebook cell。
- Add: `jupyterlab-training-platform/src/notebook/mediaInsertion.ts`
  - 生成媒体引用块、追加文本、写入 Notebook 当前 cell。
- Modify: `jupyterlab-training-platform/src/api/types.ts`
  - 新增媒体资源类型。
- Modify: `jupyterlab-training-platform/src/api/client.ts`
  - 新增管理员媒体列表、上传、重命名、删除方法，上传支持 `FormData`。
- Modify: `jupyterlab-training-platform/style/base.css`
  - 增加媒体列表、上传面板和右侧插入面板样式。
- Modify: `jupyterlab-training-platform/package.json`
  - 把新增前端测试加入 `npm test`。
- Add: `jupyterlab-training-platform/tests/media-insertion.test.js`
  - 测试媒体引用块和追加逻辑。
- Modify: `jupyterlab-training-platform/tests/admin-api-client.test.js`
  - 测试媒体列表和媒体上传请求。
- Modify: `tools/tests/ui-labels.test.js`
  - 更新“管理员工具”“课程管理”“上传媒体资源”“插入媒体资源”标签断言。
- Modify: `Dockerfile-nbgrader`
  - 更新构建后 grep 断言，确保 labextension 静态产物包含新标签。

---

### Task 1: 后端媒体资源模型和迁移

**Files:**
- Modify: `simlab-training-service/simlab_training_service/models.py`
- Add: `simlab-training-service/alembic/versions/0003_add_media_resources.py`
- Test: `simlab-training-service/tests/test_media_resources.py`

- [ ] **Step 1: 写失败测试，证明媒体资源表存在并能保存上传记录**

Create `simlab-training-service/tests/test_media_resources.py` with this initial content:

```python
from fastapi.testclient import TestClient

from simlab_training_service.app import create_app
from simlab_training_service.config import Settings
from simlab_training_service.db import Base, make_session_factory


def make_client(tmp_path):
    session_factory, engine = make_session_factory("sqlite://", connect_args={"check_same_thread": False})
    Base.metadata.create_all(engine)
    app = create_app(
        settings=Settings(
            database_url="sqlite://",
            storage_dir=str(tmp_path),
            tutorial_admins="yuan",
        ),
        session_factory=session_factory,
    )
    return TestClient(app)


def admin_headers():
    return {"X-JupyterHub-User": "yuan"}


def user_headers():
    return {"X-JupyterHub-User": "student1"}


def test_admin_can_upload_video_media_and_list_it(tmp_path):
    client = make_client(tmp_path)

    upload = client.post(
        "/api/admin/media",
        data={"title": "第一章导入视频"},
        files={"file": ("intro.mp4", b"fake-video-bytes", "video/mp4")},
        headers=admin_headers(),
    )
    listing = client.get("/api/admin/media", headers=admin_headers())

    assert upload.status_code == 200
    payload = upload.json()
    assert payload["id"].startswith("vid_")
    assert payload["title"] == "第一章导入视频"
    assert payload["original_filename"] == "intro.mp4"
    assert payload["mime_type"] == "video/mp4"
    assert payload["file_size"] == len(b"fake-video-bytes")
    assert payload["status"] == "available"
    assert "object_key" not in payload

    assert listing.status_code == 200
    assert listing.json()[0]["id"] == payload["id"]
    assert listing.json()[0]["title"] == "第一章导入视频"
```

- [ ] **Step 2: 运行测试并确认失败**

Run:

```bash
cd /home/yuan/my_project/simlab-training-service
pytest tests/test_media_resources.py -q
```

Expected: FAIL with `404 Not Found` for `/api/admin/media` or missing model/table.

- [ ] **Step 3: 增加媒体资源模型**

Modify `simlab-training-service/simlab_training_service/models.py`:

```python
def new_media_id() -> str:
    return f"vid_{uuid.uuid4().hex[:12]}"


class MediaResource(Base):
    __tablename__ = "media_resources"
    __table_args__ = (
        Index("ix_media_resources_created_at", "created_at"),
    )

    id = Column(String(32), primary_key=True, default=new_media_id)
    title = Column(Text, nullable=False)
    original_filename = Column(Text, nullable=False)
    mime_type = Column(String(255), nullable=False)
    file_size = Column(Integer, nullable=False)
    object_key = Column(Text, nullable=False)
    file_sha256 = Column(String(64), nullable=False)
    status = Column(String(32), nullable=False, default="available")
    created_by = Column(String(255), nullable=False)
    created_at = Column(DateTime(timezone=True), nullable=False, default=utcnow)
    updated_at = Column(DateTime(timezone=True), nullable=False, default=utcnow, onupdate=utcnow)
```

- [ ] **Step 4: 增加 Alembic 迁移**

Create `simlab-training-service/alembic/versions/0003_add_media_resources.py`:

```python
from __future__ import annotations

from alembic import op
import sqlalchemy as sa


revision = "0003_add_media_resources"
down_revision = "0002_add_tutorial_notebook_filename"
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.create_table(
        "media_resources",
        sa.Column("id", sa.String(length=32), primary_key=True),
        sa.Column("title", sa.Text(), nullable=False),
        sa.Column("original_filename", sa.Text(), nullable=False),
        sa.Column("mime_type", sa.String(length=255), nullable=False),
        sa.Column("file_size", sa.Integer(), nullable=False),
        sa.Column("object_key", sa.Text(), nullable=False),
        sa.Column("file_sha256", sa.String(length=64), nullable=False),
        sa.Column("status", sa.String(length=32), nullable=False, server_default="available"),
        sa.Column("created_by", sa.String(length=255), nullable=False),
        sa.Column("created_at", sa.DateTime(timezone=True), nullable=False),
        sa.Column("updated_at", sa.DateTime(timezone=True), nullable=False),
    )
    op.create_index("ix_media_resources_created_at", "media_resources", ["created_at"])


def downgrade() -> None:
    op.drop_index("ix_media_resources_created_at", table_name="media_resources")
    op.drop_table("media_resources")
```

- [ ] **Step 5: 运行测试确认仍失败在接口层**

Run:

```bash
cd /home/yuan/my_project/simlab-training-service
pytest tests/test_media_resources.py -q
```

Expected: FAIL with `404 Not Found` for `/api/admin/media`.

---

### Task 2: 后端媒体上传、列表、重命名和删除接口

**Files:**
- Modify: `simlab-training-service/simlab_training_service/storage.py`
- Add: `simlab-training-service/simlab_training_service/routes_media_admin.py`
- Modify: `simlab-training-service/simlab_training_service/app.py`
- Modify: `simlab-training-service/tests/test_media_resources.py`

- [ ] **Step 1: 扩展测试覆盖权限和文件类型**

Append to `simlab-training-service/tests/test_media_resources.py`:

```python
def test_admin_can_rename_media_resource(tmp_path):
    client = make_client(tmp_path)
    media = client.post(
        "/api/admin/media",
        data={"title": "旧标题"},
        files={"file": ("intro.mp4", b"fake-video-bytes", "video/mp4")},
        headers=admin_headers(),
    ).json()

    response = client.patch(
        f"/api/admin/media/{media['id']}",
        json={"title": "新标题"},
        headers=admin_headers(),
    )
    listing = client.get("/api/admin/media", headers=admin_headers())

    assert response.status_code == 200
    assert response.json()["title"] == "新标题"
    assert listing.json()[0]["title"] == "新标题"


def test_admin_can_delete_media_resource(tmp_path):
    client = make_client(tmp_path)
    media = client.post(
        "/api/admin/media",
        data={"title": "待删除视频"},
        files={"file": ("intro.mp4", b"fake-video-bytes", "video/mp4")},
        headers=admin_headers(),
    ).json()

    response = client.delete(f"/api/admin/media/{media['id']}", headers=admin_headers())
    listing = client.get("/api/admin/media", headers=admin_headers())

    assert response.status_code == 200
    assert listing.json() == []


def test_media_upload_requires_admin(tmp_path):
    client = make_client(tmp_path)

    response = client.post(
        "/api/admin/media",
        data={"title": "学生不能上传"},
        files={"file": ("intro.mp4", b"fake-video-bytes", "video/mp4")},
        headers=user_headers(),
    )

    assert response.status_code == 403


def test_media_upload_rejects_non_video_file(tmp_path):
    client = make_client(tmp_path)

    response = client.post(
        "/api/admin/media",
        data={"title": "错误文件"},
        files={"file": ("notes.txt", b"plain-text", "text/plain")},
        headers=admin_headers(),
    )

    assert response.status_code == 400
    assert "video" in response.text.lower()


def test_media_upload_uses_filename_when_title_is_blank(tmp_path):
    client = make_client(tmp_path)

    response = client.post(
        "/api/admin/media",
        data={"title": ""},
        files={"file": ("heat-intro.mp4", b"fake-video-bytes", "video/mp4")},
        headers=admin_headers(),
    )

    assert response.status_code == 200
    assert response.json()["title"] == "heat-intro"
```

- [ ] **Step 2: 运行测试确认失败**

Run:

```bash
cd /home/yuan/my_project/simlab-training-service
pytest tests/test_media_resources.py -q
```

Expected: FAIL because media routes are not implemented.

- [ ] **Step 3: 增加媒体存储方法**

Modify `simlab-training-service/simlab_training_service/storage.py`:

```python
@dataclass(frozen=True)
class StoredMedia:
    object_key: str
    sha256: str
    size: int


class TutorialStorage:
    def __init__(self, root: str | Path):
        self.root = Path(root)

    def save_version(self, tutorial_public_id: str, version_id: str, notebook: dict[str, Any]) -> StoredNotebook:
        relative = Path(tutorial_public_id) / f"{version_id}.ipynb"
        target = self.root / relative
        target.parent.mkdir(parents=True, exist_ok=True)
        content = json.dumps(notebook, ensure_ascii=False, indent=2, sort_keys=True).encode("utf-8")
        target.write_bytes(content)
        return StoredNotebook(
            object_key=relative.as_posix(),
            sha256=hashlib.sha256(content).hexdigest(),
        )

    def save_media(self, media_id: str, filename: str, content: bytes) -> StoredMedia:
        safe_name = safe_filename(filename)
        relative = Path("media") / media_id / safe_name
        target = self.root / relative
        target.parent.mkdir(parents=True, exist_ok=True)
        target.write_bytes(content)
        return StoredMedia(
            object_key=relative.as_posix(),
            sha256=hashlib.sha256(content).hexdigest(),
            size=len(content),
        )

    def delete_object(self, object_key: str) -> None:
        target = self.root / object_key
        if target.exists() and target.is_file():
            target.unlink()

    def load(self, object_key: str) -> dict[str, Any]:
        return json.loads((self.root / object_key).read_text(encoding="utf-8"))


def safe_filename(value: str) -> str:
    name = value.replace("\\", "/").split("/")[-1].strip()
    name = re.sub(r'[\\/:*?"<>|]+', "_", name)
    name = re.sub(r"\s+", " ", name)
    return name or "media.bin"
```

Also add `import re` near the top of `storage.py`.

- [ ] **Step 4: 新增管理员媒体路由**

Create `simlab-training-service/simlab_training_service/routes_media_admin.py`:

```python
from __future__ import annotations

from pathlib import Path

from fastapi import APIRouter, Depends, File, Form, HTTPException, Request, UploadFile, status
from pydantic import BaseModel
from sqlalchemy import select
from sqlalchemy.orm import Session

from .auth import CurrentUser, require_admin
from .db import get_db
from .models import MediaResource, new_media_id, utcnow


router = APIRouter(prefix="/api/admin/media")


VIDEO_EXTENSIONS = {".mp4", ".mov", ".m4v", ".webm", ".mkv"}


class MediaPatch(BaseModel):
    title: str


@router.get("")
def list_media(db: Session = Depends(get_db), _admin: CurrentUser = Depends(require_admin)):
    rows = db.execute(
        select(MediaResource).order_by(MediaResource.created_at.desc())
    ).scalars().all()
    return [serialize_media(row) for row in rows]


@router.post("")
async def upload_media(
    request: Request,
    title: str = Form(""),
    file: UploadFile = File(...),
    db: Session = Depends(get_db),
    admin: CurrentUser = Depends(require_admin),
):
    filename = (file.filename or "").strip()
    mime_type = (file.content_type or "").strip()
    if not is_video_upload(filename, mime_type):
        raise HTTPException(
            status_code=status.HTTP_400_BAD_REQUEST,
            detail="Only video media files are supported",
        )

    content = await file.read()
    if not content:
        raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail="Uploaded media file is empty")

    media_id = new_media_id()
    stored = request.app.state.storage.save_media(media_id, filename, content)
    media = MediaResource(
        id=media_id,
        title=media_title(title, filename),
        original_filename=filename,
        mime_type=mime_type or "application/octet-stream",
        file_size=stored.size,
        object_key=stored.object_key,
        file_sha256=stored.sha256,
        status="available",
        created_by=admin.username,
        created_at=utcnow(),
        updated_at=utcnow(),
    )
    db.add(media)
    db.commit()
    db.refresh(media)
    return serialize_media(media)


@router.patch("/{media_id}")
def rename_media(
    media_id: str,
    payload: MediaPatch,
    db: Session = Depends(get_db),
    _admin: CurrentUser = Depends(require_admin),
):
    media = media_or_404(db, media_id)
    title = payload.title.strip()
    if not title:
        raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail="Media title is required")
    media.title = title
    media.updated_at = utcnow()
    db.commit()
    db.refresh(media)
    return serialize_media(media)


@router.delete("/{media_id}")
def delete_media(
    media_id: str,
    request: Request,
    db: Session = Depends(get_db),
    _admin: CurrentUser = Depends(require_admin),
):
    media = media_or_404(db, media_id)
    request.app.state.storage.delete_object(media.object_key)
    db.delete(media)
    db.commit()
    return {"ok": True}


def is_video_upload(filename: str, mime_type: str) -> bool:
    suffix = Path(filename).suffix.lower()
    return mime_type.startswith("video/") or suffix in VIDEO_EXTENSIONS


def media_title(title: str, filename: str) -> str:
    value = title.strip()
    if value:
        return value
    return Path(filename).stem.strip() or "未命名媒体资源"


def media_or_404(db: Session, media_id: str) -> MediaResource:
    media = db.get(MediaResource, media_id)
    if not media:
        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Media resource not found")
    return media


def serialize_media(media: MediaResource) -> dict:
    return {
        "id": media.id,
        "title": media.title,
        "original_filename": media.original_filename,
        "mime_type": media.mime_type,
        "file_size": media.file_size,
        "status": media.status,
        "created_by": media.created_by,
        "created_at": media.created_at.isoformat(),
        "updated_at": media.updated_at.isoformat(),
    }
```

- [ ] **Step 5: 注册媒体路由**

Modify `simlab-training-service/simlab_training_service/app.py`:

```python
from .routes_media_admin import router as media_admin_router
```

Then include it next to existing admin route registration:

```python
    app.include_router(public_router)
    app.include_router(admin_router)
    app.include_router(media_admin_router)
    service_prefix = app_settings.jupyterhub_service_prefix.rstrip("/")
    if service_prefix:
        app.include_router(public_router, prefix=service_prefix)
        app.include_router(admin_router, prefix=service_prefix)
        app.include_router(media_admin_router, prefix=service_prefix)
```

- [ ] **Step 6: 运行后端媒体测试**

Run:

```bash
cd /home/yuan/my_project/simlab-training-service
pytest tests/test_media_resources.py -q
```

Expected: PASS.

- [ ] **Step 7: 运行后端现有测试**

Run:

```bash
cd /home/yuan/my_project/simlab-training-service
pytest tests -q
```

Expected: PASS.

---

### Task 3: 前端 API 类型和媒体管理客户端

**Files:**
- Modify: `jupyterlab-training-platform/src/api/types.ts`
- Modify: `jupyterlab-training-platform/src/api/client.ts`
- Modify: `jupyterlab-training-platform/tests/admin-api-client.test.js`
- Modify: `jupyterlab-training-platform/package.json`

- [ ] **Step 1: 写失败测试覆盖媒体列表、上传、重命名和删除请求**

Append to `jupyterlab-training-platform/tests/admin-api-client.test.js`:

```javascript
async function testAdminListsMediaResources() {
  const call = await captureRequest(client => client.adminListMedia());

  assert.equal(call.url, '/api/admin/media');
  assert.equal(call.init.method, 'GET');
}

async function testAdminUploadsMediaWithFormData() {
  const calls = [];
  global.fetch = async (url, init) => {
    calls.push({ url, init });
    return response({
      id: 'vid_123',
      title: '第一章',
      original_filename: 'intro.mp4',
      mime_type: 'video/mp4',
      file_size: 10,
      status: 'available',
      created_by: 'yuan',
      created_at: '2026-07-06T00:00:00+00:00',
      updated_at: '2026-07-06T00:00:00+00:00'
    });
  };
  PageConfig.setOption('token', '');
  const client = new TrainingApiClient('/api');
  const file = new Blob(['fake-video'], { type: 'video/mp4' });
  file.name = 'intro.mp4';

  await client.adminUploadMedia(file, '第一章');

  assert.equal(calls.length, 1);
  assert.equal(calls[0].url, '/api/admin/media');
  assert.equal(calls[0].init.method, 'POST');
  assert.ok(calls[0].init.body instanceof FormData);
  assert.equal(calls[0].init.headers['Content-Type'], undefined);
}

async function testAdminRenamesMediaResource() {
  const call = await captureRequest(client => client.adminRenameMedia('vid_123', '新标题'));

  assert.equal(call.url, '/api/admin/media/vid_123');
  assert.equal(call.init.method, 'PATCH');
  assert.deepEqual(JSON.parse(call.init.body), { title: '新标题' });
}

async function testAdminDeletesMediaResource() {
  const call = await captureRequest(client => client.adminDeleteMedia('vid_123'));

  assert.equal(call.url, '/api/admin/media/vid_123');
  assert.equal(call.init.method, 'DELETE');
}
```

Add the calls inside `run()`:

```javascript
  await testAdminListsMediaResources();
  await testAdminUploadsMediaWithFormData();
  await testAdminRenamesMediaResource();
  await testAdminDeletesMediaResource();
```

- [ ] **Step 2: 运行测试确认失败**

Run:

```bash
cd /home/yuan/my_project/jupyterlab-training-platform
npm run build:lib
node tests/admin-api-client.test.js
```

Expected: FAIL because `adminListMedia`, `adminUploadMedia`, `adminRenameMedia`, and `adminDeleteMedia` are not defined.

- [ ] **Step 3: 增加媒体类型**

Modify `jupyterlab-training-platform/src/api/types.ts`:

```typescript
export interface MediaResource {
  id: string;
  title: string;
  original_filename: string;
  mime_type: string;
  file_size: number;
  status: string;
  created_by: string;
  created_at: string;
  updated_at: string;
}

export interface PatchMediaResourceRequest {
  title: string;
}
```

- [ ] **Step 4: 增加 API client 方法**

Modify imports in `jupyterlab-training-platform/src/api/client.ts` to include `MediaResource` and `PatchMediaResourceRequest`.

Add `PatchMediaResourceRequest` to the `RequestBody` union:

```typescript
  | PatchMediaResourceRequest
```

Add methods inside `TrainingApiClient`:

```typescript
  async adminListMedia(): Promise<MediaResource[]> {
    return this.get<MediaResource[]>('/admin/media');
  }

  async adminUploadMedia(file: File | Blob, title: string): Promise<MediaResource> {
    const form = new FormData();
    form.append('title', title);
    form.append('file', file);
    return this.requestForm<MediaResource>('/admin/media', form);
  }

  async adminRenameMedia(mediaId: string, title: string): Promise<MediaResource> {
    return this.patch<MediaResource>(`/admin/media/${encodeURIComponent(mediaId)}`, { title });
  }

  async adminDeleteMedia(mediaId: string): Promise<void> {
    await this.request(`/admin/media/${encodeURIComponent(mediaId)}`, { method: 'DELETE' });
  }
```

Add `requestForm` below `patch`:

```typescript
  private async requestForm<T>(path: string, body: FormData): Promise<T> {
    const response = await fetch(`${this.baseUrl}${path}`, {
      method: 'POST',
      credentials: 'same-origin',
      body,
      headers: {
        ...authorizationHeaderFromToken()
      }
    });
    if (!response.ok) {
      const detail = await response.text();
      throw new Error(`请求失败 ${response.status}: ${detail}`);
    }
    return response.json() as Promise<T>;
  }
```

- [ ] **Step 5: 运行前端 API 测试**

Run:

```bash
cd /home/yuan/my_project/jupyterlab-training-platform
npm run build:lib
node tests/admin-api-client.test.js
```

Expected: PASS.

---

### Task 4: 管理员工具菜单拆成三个独立命令

**Files:**
- Modify: `jupyterlab-training-platform/src/index.tsx`
- Modify: `jupyterlab-training-platform/src/components/AdminManager.tsx`
- Modify: `tools/tests/ui-labels.test.js`
- Modify: `Dockerfile-nbgrader`

- [ ] **Step 1: 更新标签测试为新管理员入口**

Modify the training platform assertions in `tools/tests/ui-labels.test.js`:

```javascript
assert.ok(trainingPlatformIndex.includes("adminMenu.title.label = '管理员工具'"));
assert.ok(trainingPlatformIndex.includes("label: '课程管理'"));
assert.ok(trainingPlatformIndex.includes("label: '上传媒体资源'"));
assert.ok(trainingPlatformIndex.includes("label: '插入媒体资源'"));
assert.ok(trainingPlatformIndex.includes("content.title.label = '课程管理'"));
assert.ok(trainingPlatformIndex.includes("mediaUploadWidget.title.label = '上传媒体资源'"));
assert.ok(trainingPlatformIndex.includes("this.title.label = '插入媒体资源'"));
assert.equal(trainingPlatformIndex.includes("label: '打开管理员工具'"), false);
assert.ok(trainingPlatformAdmin.includes('>课程管理</h2>'));
assert.equal(trainingPlatformAdmin.includes('>管理员工具</h2>'), false);
```

Replace the old assertion:

```javascript
assert.ok(trainingPlatformIndex.includes("adminMenu.title.label = '课程管理'"));
```

with the new `管理员工具` assertion above.

Update the labextension static check:

```javascript
assert.ok(trainingPlatformLabextensionStatic.includes('管理员工具'));
assert.ok(trainingPlatformLabextensionStatic.includes('课程管理'));
assert.ok(trainingPlatformLabextensionStatic.includes('上传媒体资源'));
assert.ok(trainingPlatformLabextensionStatic.includes('插入媒体资源'));
```

- [ ] **Step 2: 运行 UI 标签测试确认失败**

Run:

```bash
cd /home/yuan/my_project
node tools/tests/ui-labels.test.js
```

Expected: FAIL because the source still has the old single `打开管理员工具` command.

- [ ] **Step 3: 让 AdminManager 只保留课程管理**

Modify `jupyterlab-training-platform/src/components/AdminManager.tsx` so the exported `AdminManager` component renders only the existing course-management UI. Remove the administrator-tools tab wrapper and the imports for `MediaUploadPanel` / `MediaInsertPanel`.

The component signature must be:

```typescript
interface AdminManagerProps {
  api: TrainingApiClient;
}

export function AdminManager(props: AdminManagerProps): React.ReactElement {
  // Existing course-management implementation stays here without changing behavior.
}
```

Keep these existing helper functions in `AdminManager.tsx` because they belong to course management:

```typescript
UploadPanel
InspectorPanel
LabeledInput
StatusSelect
StatusBadge
countTutorials
```

- [ ] **Step 4: 在 index.tsx 中增加上传媒体和右侧插入媒体 widget**

Modify `jupyterlab-training-platform/src/index.tsx` to import the media panels:

```typescript
import { MediaInsertPanel } from './components/MediaInsertPanel';
import { MediaUploadPanel } from './components/MediaUploadPanel';
```

Add a main-area `MediaUploadWidget` and a right-side `MediaInsertPanelWidget`. The insert widget must use `app.shell.add(mediaInsertWidget, 'right', { rank: 660 })`.

- [ ] **Step 5: 注册三个管理员工具命令**

Modify `jupyterlab-training-platform/src/index.tsx`.

Use separate command IDs:

```typescript
openCourseManager: 'training-platform:open-course-manager'
openMediaUpload: 'training-platform:open-media-upload'
openMediaInsertPanel: 'training-platform:open-media-insert'
```

The top menu must be:

```typescript
adminMenu.title.label = '管理员工具';
adminMenu.addItem({ command: CommandIDs.openCourseManager });
adminMenu.addItem({ command: CommandIDs.openMediaUpload });
adminMenu.addItem({ command: CommandIDs.openMediaInsertPanel });
```

There must be no `打开管理员工具` command.

- [ ] **Step 6: 更新 Dockerfile 构建断言**

Modify `Dockerfile-nbgrader` in the `jupyterlab-training-platform` grep block:

```dockerfile
RUN grep -R "学习课程" /src/jupyterlab-training-platform/jupyterlab_training_platform/labextension/static/*.js && \
    grep -R "管理员工具" /src/jupyterlab-training-platform/jupyterlab_training_platform/labextension/static/*.js && \
    grep -R "课程管理" /src/jupyterlab-training-platform/jupyterlab_training_platform/labextension/static/*.js && \
    grep -R "上传媒体资源" /src/jupyterlab-training-platform/jupyterlab_training_platform/labextension/static/*.js && \
    grep -R "插入媒体资源" /src/jupyterlab-training-platform/jupyterlab_training_platform/labextension/static/*.js && \
    grep -R "评论" /src/jupyterlab-training-platform/jupyterlab_training_platform/labextension/static/*.js
```

- [ ] **Step 7: 运行标签测试**

Run:

```bash
cd /home/yuan/my_project
node tools/tests/ui-labels.test.js
```

Expected: PASS after source assertions are updated.

---

### Task 5: 上传媒体资源和媒体管理前端页面

**Files:**
- Add: `jupyterlab-training-platform/src/components/MediaUploadPanel.tsx`
- Modify: `jupyterlab-training-platform/style/base.css`

- [ ] **Step 1: 创建上传媒体资源组件**

Create `jupyterlab-training-platform/src/components/MediaUploadPanel.tsx`:

```typescript
import React, { ChangeEvent, useEffect, useState } from 'react';
import { TrainingApiClient } from '../api/client';
import { MediaResource } from '../api/types';

interface MediaUploadPanelProps {
  api: TrainingApiClient;
}

export function MediaUploadPanel(props: MediaUploadPanelProps): React.ReactElement {
  const [items, setItems] = useState<MediaResource[]>([]);
  const [title, setTitle] = useState<string>('');
  const [file, setFile] = useState<File | null>(null);
  const [editingId, setEditingId] = useState<string>('');
  const [editingTitle, setEditingTitle] = useState<string>('');
  const [status, setStatus] = useState<string>('');
  const [busy, setBusy] = useState<boolean>(false);

  useEffect(() => {
    void refresh();
  }, []);

  async function refresh(): Promise<void> {
    try {
      const media = await props.api.adminListMedia();
      setItems(media);
      setStatus(media.length ? '' : '暂无媒体资源。');
    } catch (error) {
      setStatus(error instanceof Error ? error.message : String(error));
    }
  }

  function selectFile(event: ChangeEvent<HTMLInputElement>): void {
    const selected = event.target.files?.[0] ?? null;
    setFile(selected);
    if (selected) {
      setTitle(current => current || selected.name.replace(/\.[^.]+$/, ''));
      setStatus(`已选择 ${selected.name}`);
    }
  }

  async function upload(): Promise<void> {
    if (!file) {
      setStatus('请先选择本地视频文件。');
      return;
    }
    setBusy(true);
    setStatus('');
    try {
      const media = await props.api.adminUploadMedia(file, title);
      setStatus('媒体资源已上传。');
      setFile(null);
      setTitle('');
      setItems(current => [media, ...current]);
    } catch (error) {
      setStatus(error instanceof Error ? error.message : String(error));
    } finally {
      setBusy(false);
    }
  }

  async function saveRename(media: MediaResource): Promise<void> {
    const nextTitle = editingTitle.trim();
    if (!nextTitle) {
      setStatus('媒体标题不能为空。');
      return;
    }
    setBusy(true);
    setStatus('');
    try {
      const updated = await props.api.adminRenameMedia(media.id, nextTitle);
      setItems(current => current.map(item => item.id === updated.id ? updated : item));
      setEditingId('');
      setEditingTitle('');
      setStatus('媒体资源已重命名。');
    } catch (error) {
      setStatus(error instanceof Error ? error.message : String(error));
    } finally {
      setBusy(false);
    }
  }

  async function deleteMedia(media: MediaResource): Promise<void> {
    if (!window.confirm(`删除媒体资源“${media.title}”？`)) {
      return;
    }
    setBusy(true);
    setStatus('');
    try {
      await props.api.adminDeleteMedia(media.id);
      setItems(current => current.filter(item => item.id !== media.id));
      setStatus('媒体资源已删除。');
    } catch (error) {
      setStatus(error instanceof Error ? error.message : String(error));
    } finally {
      setBusy(false);
    }
  }

  return (
    <section className="training-admin-band training-media-panel">
      <div className="training-panel-heading">
        <div>
          <h3>上传媒体资源</h3>
          <p>选择本地视频文件上传，上传后可在“插入媒体资源”中选择。</p>
        </div>
      </div>
      {status && <div className="training-status">{status}</div>}
      <div className="training-form-grid">
        <input type="file" accept="video/*,.mp4,.mov,.m4v,.webm,.mkv" onChange={selectFile} />
        <input value={title} onChange={event => setTitle(event.target.value)} placeholder="媒体标题" />
      </div>
      <div className="training-upload-actions">
        <button className="training-button primary" disabled={busy || !file} onClick={upload}>
          上传媒体资源
        </button>
      </div>
      <div className="training-media-list">
        {items.map(media => (
          <article className="training-media-row" key={media.id}>
            <div>
              {editingId === media.id ? (
                <input value={editingTitle} onChange={event => setEditingTitle(event.target.value)} />
              ) : (
                <strong>{media.title}</strong>
              )}
              <span>{media.id}</span>
              <em>{media.original_filename}</em>
            </div>
            <div className="training-media-actions">
              {editingId === media.id ? (
                <>
                  <button className="training-button primary" disabled={busy} onClick={() => saveRename(media)}>
                    保存
                  </button>
                  <button className="training-button" disabled={busy} onClick={() => setEditingId('')}>
                    取消
                  </button>
                </>
              ) : (
                <>
                  <button
                    className="training-button"
                    disabled={busy}
                    onClick={() => {
                      setEditingId(media.id);
                      setEditingTitle(media.title);
                    }}
                  >
                    重命名
                  </button>
                  <button className="training-button danger" disabled={busy} onClick={() => deleteMedia(media)}>
                    删除
                  </button>
                </>
              )}
            </div>
          </article>
        ))}
      </div>
    </section>
  );
}
```

- [ ] **Step 2: 增加上传页面样式**

Append to `jupyterlab-training-platform/style/base.css`:

```css
.training-admin-tools {
  box-sizing: border-box;
  height: 100%;
  min-height: 0;
  display: flex;
  flex-direction: column;
  background: #f3f5f7;
  color: #202a33;
  font-family: "Noto Sans CJK SC", "Microsoft YaHei", sans-serif;
}

.training-admin-tools-header {
  flex: 0 0 auto;
  display: flex;
  justify-content: space-between;
  gap: 12px;
  align-items: flex-start;
  padding: 16px 18px 10px;
}

.training-admin-tools-header h2 {
  margin: 0;
  font-size: 20px;
  line-height: 1.3;
}

.training-admin-tools-header p {
  margin: 5px 0 0;
  color: #667684;
  line-height: 1.5;
}

.training-admin-tool-tabs {
  flex: 0 0 auto;
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  padding: 0 18px 12px;
  border-bottom: 1px solid #cfd7df;
}

.training-admin-tool-tabs button {
  min-height: 32px;
  border: 1px solid #bcc7d2;
  background: #ffffff;
  color: #202a33;
  padding: 6px 10px;
  cursor: pointer;
}

.training-admin-tool-tabs button.active {
  background: #1976d2;
  border-color: #1976d2;
  color: #ffffff;
}

.training-admin-tool-body {
  flex: 1 1 auto;
  min-height: 0;
  overflow: auto;
}

.training-media-panel {
  margin: 18px;
}

.training-media-actions {
  flex: 0 0 auto;
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  justify-content: flex-end;
}
```

- [ ] **Step 3: 运行前端构建**

Run:

```bash
cd /home/yuan/my_project/jupyterlab-training-platform
npm run build:lib
```

Expected: PASS.

---

### Task 6: 插入媒体资源到当前 Notebook cell

**Files:**
- Add: `jupyterlab-training-platform/src/notebook/mediaInsertion.ts`
- Add: `jupyterlab-training-platform/src/components/MediaInsertPanel.tsx`
- Add: `jupyterlab-training-platform/tests/media-insertion.test.js`
- Modify: `jupyterlab-training-platform/package.json`
- Modify: `jupyterlab-training-platform/style/base.css`

- [ ] **Step 1: 写纯函数测试**

Create `jupyterlab-training-platform/tests/media-insertion.test.js`:

```javascript
const assert = require('node:assert/strict');

const {
  appendMediaDirective,
  buildMediaDirective
} = require('../lib/notebook/mediaInsertion.js');

function testBuildMediaDirective() {
  assert.equal(
    buildMediaDirective({ id: 'vid_abc123', title: '第一章导入视频' }),
    '::simlab-video\nmedia_id: vid_abc123\ntitle: 第一章导入视频\n::'
  );
}

function testAppendMediaDirectiveToEmptyCell() {
  assert.equal(
    appendMediaDirective('', '::simlab-video\nmedia_id: vid_abc123\n::'),
    '::simlab-video\nmedia_id: vid_abc123\n::'
  );
}

function testAppendMediaDirectiveToExistingContent() {
  assert.equal(
    appendMediaDirective('# 第一章', '::simlab-video\nmedia_id: vid_abc123\n::'),
    '# 第一章\n\n::simlab-video\nmedia_id: vid_abc123\n::'
  );
}

function testAppendMediaDirectivePreservesTrailingWhitespace() {
  assert.equal(
    appendMediaDirective('print("hello")\n', '::simlab-video\nmedia_id: vid_abc123\n::'),
    'print("hello")\n\n::simlab-video\nmedia_id: vid_abc123\n::'
  );
}

function run() {
  testBuildMediaDirective();
  testAppendMediaDirectiveToEmptyCell();
  testAppendMediaDirectiveToExistingContent();
  testAppendMediaDirectivePreservesTrailingWhitespace();
  console.log('media insertion tests passed');
}

run();
```

Modify `jupyterlab-training-platform/package.json` test script:

```json
"test": "npm run build:lib && node tests/api-client.test.js && node tests/admin-api-client.test.js && node tests/tutorial-metadata.test.js && node tests/copy-tutorial.test.js && node tests/comment-visibility.test.js && node tests/media-insertion.test.js"
```

- [ ] **Step 2: 运行测试确认失败**

Run:

```bash
cd /home/yuan/my_project/jupyterlab-training-platform
npm test
```

Expected: FAIL because `lib/notebook/mediaInsertion.js` does not exist.

- [ ] **Step 3: 实现媒体引用生成和追加逻辑**

Create `jupyterlab-training-platform/src/notebook/mediaInsertion.ts`:

```typescript
import { JupyterFrontEnd } from '@jupyterlab/application';
import { INotebookTracker, NotebookActions } from '@jupyterlab/notebook';
import { MediaResource } from '../api/types';

export function buildMediaDirective(media: Pick<MediaResource, 'id' | 'title'>): string {
  return [
    '::simlab-video',
    `media_id: ${media.id}`,
    `title: ${media.title}`,
    '::'
  ].join('\n');
}

export function appendMediaDirective(source: string, directive: string): string {
  const trimmed = source.trimEnd();
  return trimmed ? `${trimmed}\n\n${directive}` : directive;
}

export async function insertMediaIntoCurrentNotebook(
  app: JupyterFrontEnd,
  notebookTracker: INotebookTracker,
  media: MediaResource
): Promise<string> {
  void app;
  const panel = notebookTracker.currentWidget;
  if (!panel) {
    return '请先打开一个教程 Notebook。';
  }
  await panel.context.ready;
  const activeCell = panel.content.activeCell;
  if (!activeCell) {
    return '请先选中要插入媒体资源的 cell。';
  }

  const directive = buildMediaDirective(media);
  const currentSource = activeCell.model.sharedModel.getSource();
  activeCell.model.sharedModel.setSource(appendMediaDirective(currentSource, directive));
  if (activeCell.model.type !== 'markdown') {
    NotebookActions.changeCellType(panel.content, 'markdown');
  }
  return `已插入媒体资源：${media.title}`;
}
```

Behavior:

- 当前没有 Notebook：返回提示。
- 当前没有选中 cell：返回提示。
- 当前 cell 是空 cell：写入媒体引用并转为 Markdown。
- 当前 cell 已有内容：把媒体引用追加到末尾。
- 当前 cell 不是 Markdown：追加后自动转成 Markdown，保证 `::simlab-video` 引用能作为 Markdown 内容渲染。

- [ ] **Step 4: 创建插入媒体资源组件**

Create `jupyterlab-training-platform/src/components/MediaInsertPanel.tsx`:

```typescript
import { JupyterFrontEnd } from '@jupyterlab/application';
import { INotebookTracker } from '@jupyterlab/notebook';
import React, { useEffect, useState } from 'react';
import { TrainingApiClient } from '../api/client';
import { MediaResource } from '../api/types';
import { insertMediaIntoCurrentNotebook } from '../notebook/mediaInsertion';

interface MediaInsertPanelProps {
  api: TrainingApiClient;
  app: JupyterFrontEnd;
  notebookTracker: INotebookTracker;
}

export function MediaInsertPanel(props: MediaInsertPanelProps): React.ReactElement {
  const [items, setItems] = useState<MediaResource[]>([]);
  const [status, setStatus] = useState<string>('正在加载媒体资源...');
  const [busyId, setBusyId] = useState<string>('');

  useEffect(() => {
    void refresh();
  }, []);

  async function refresh(): Promise<void> {
    try {
      const media = await props.api.adminListMedia();
      setItems(media);
      setStatus(media.length ? '' : '暂无媒体资源，请先上传视频。');
    } catch (error) {
      setStatus(error instanceof Error ? error.message : String(error));
    }
  }

  async function insert(media: MediaResource): Promise<void> {
    setBusyId(media.id);
    setStatus('');
    try {
      const message = await insertMediaIntoCurrentNotebook(props.app, props.notebookTracker, media);
      setStatus(message);
    } catch (error) {
      setStatus(error instanceof Error ? error.message : String(error));
    } finally {
      setBusyId('');
    }
  }

  return (
    <section className="training-admin-band training-media-panel">
      <div className="training-panel-heading">
        <div>
          <h3>插入媒体资源</h3>
          <p>选择一个已上传视频，将平台内部媒体引用写入当前 Notebook cell。</p>
        </div>
        <button className="training-button" onClick={refresh}>刷新</button>
      </div>
      {status && <div className="training-status">{status}</div>}
      <div className="training-media-list">
        {items.map(media => (
          <article className="training-media-row" key={media.id}>
            <div>
              <strong>{media.title}</strong>
              <span>{media.id}</span>
              <em>{media.original_filename}</em>
            </div>
            <button
              className="training-button primary"
              disabled={busyId === media.id}
              onClick={() => insert(media)}
            >
              插入媒体资源
            </button>
          </article>
        ))}
      </div>
    </section>
  );
}
```

- [ ] **Step 5: 增加媒体列表样式**

Append to `jupyterlab-training-platform/style/base.css`:

```css
.training-media-list {
  display: grid;
  gap: 10px;
  margin-top: 12px;
}

.training-media-row {
  display: flex;
  justify-content: space-between;
  gap: 12px;
  align-items: center;
  border: 1px solid #cfd7df;
  background: #ffffff;
  padding: 10px;
}

.training-media-row div {
  min-width: 0;
  display: grid;
  gap: 4px;
}

.training-media-row strong,
.training-media-row span,
.training-media-row em {
  min-width: 0;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.training-media-row span,
.training-media-row em {
  color: #667684;
  font-style: normal;
}
```

- [ ] **Step 6: 运行前端测试**

Run:

```bash
cd /home/yuan/my_project/jupyterlab-training-platform
npm test
```

Expected: PASS.

---

### Task 7: 集成验证

**Files:**
- Modify only if previous tasks revealed a concrete failure.

- [ ] **Step 1: 运行后端测试**

Run:

```bash
cd /home/yuan/my_project/simlab-training-service
pytest tests -q
```

Expected: PASS.

- [ ] **Step 2: 运行前端测试**

Run:

```bash
cd /home/yuan/my_project/jupyterlab-training-platform
npm test
```

Expected: PASS.

- [ ] **Step 3: 运行 UI 标签测试**

Run:

```bash
cd /home/yuan/my_project
node tools/tests/ui-labels.test.js
```

Expected: PASS.

- [ ] **Step 4: 构建 JupyterLab 插件**

Run:

```bash
cd /home/yuan/my_project/jupyterlab-training-platform
npm run build
```

Expected: PASS.

- [ ] **Step 5: 手工验收管理员工具流程**

Run the training service in dev mode:

```bash
cd /home/yuan/my_project
tools/training-service-dev-up.sh
```

Manual acceptance:

- 管理员登录后能看到“管理员工具”菜单。
- “管理员工具”菜单下直接包含 `课程管理`、`上传媒体资源`、`插入媒体资源` 三个命令，不再有 `打开管理员工具`。
- `课程管理` 保持原有课程、章节、教程上传能力。
- `上传媒体资源` 可以选择本地视频并上传成功。
- 上传成功后，点击 `插入媒体资源` 会在右侧打开侧边栏，刷新列表能看到该视频。
- 打开一个 Notebook，选中空 cell，通过右侧 `插入媒体资源` 侧边栏点击插入后 cell 内容为：

```markdown
::simlab-video
media_id: vid_xxxxxxxxxxxx
title: 第一章导入视频
::
```

- 选中已有 Markdown cell，点击插入后媒体引用追加到原内容末尾。
- 选中已有代码的 code cell，点击插入后在下方生成 Markdown cell，原代码不被修改。

- [ ] **Step 6: 停止开发服务**

Run:

```bash
cd /home/yuan/my_project
tools/training-service-down.sh
```

Expected: service containers stop cleanly.

---

## 自检

- 覆盖“插件名字改为管理员工具”：Task 4。
- 覆盖“课程管理仍作为管理员工具下的功能”：Task 4。
- 覆盖“上传媒体资源、重命名、删除”：Task 2、Task 3、Task 5。
- 覆盖“插入视频改名为插入媒体资源”：Task 4、Task 6。
- 覆盖“插入时感知当前 Notebook 状态”：Task 6。
- 覆盖“没有打开 Notebook 时提示”：Task 6。
- 覆盖“没有选中 cell 时提示”：Task 6。
- 覆盖“当前 cell 是空 cell 时自动转成 Markdown 并写入”：Task 6。
- 覆盖“当前 cell 已有内容时追加到末尾”：Task 6。
- 覆盖“不让管理员复制代码，直接通过插入功能写入”：Task 5、Task 6。
- 覆盖“视频加密先不用做”：范围部分明确排除加密、HLS、DRM 和学生端播放器。
