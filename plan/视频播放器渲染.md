# 视频播放器渲染 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 Notebook Markdown cell 中的 `::simlab-video` 媒体引用块自动渲染成可播放的视频播放器。

**Architecture:** 后端新增面向已登录用户的媒体内容读取接口，继续隐藏真实文件路径，只按 `media_id` 读取已上传视频。前端复用 JupyterLab 原生 Markdown renderer，在渲染完成后只替换 `::simlab-video` 块为播放器；播放器通过 `fetch` 携带现有 Jupyter token 下载视频 Blob，再用 object URL 赋给 `<video>`，避免 `<video src>` 不能带 `Authorization` header 的问题。

**Tech Stack:** FastAPI, SQLAlchemy, Starlette `FileResponse`, JupyterLab 4, TypeScript, React-independent DOM renderer, Node tests, pytest.

---

## 当前问题

现在 `jupyterlab-training-platform/src/notebook/mediaDirective.ts` 只生成文本引用块：

```markdown
::simlab-video
media_id: vid_xxxxxxxxxxxx
title: 课程介绍
::
```

`jupyterlab-training-platform/src/notebook/mediaInsertion.ts` 只把这段文本写入 Notebook cell。`simlab-training-service/simlab_training_service/routes_media_admin.py` 只有管理员上传、列表、重命名、删除接口，没有学生或普通登录用户可读取视频内容的接口。JupyterLab 原生 Markdown renderer 不认识 `::simlab-video`，所以它会显示成普通文字。

## Scope

包含：

- 新增公开但需要登录的媒体内容接口：`GET /api/media/{media_id}/content`。
- 支持学生用户播放已上传且状态为 `available` 的视频。
- 保持管理员上传接口不暴露 `object_key` 和真实存储路径。
- 在 JupyterLab Notebook Markdown 渲染后，把单独成块的 `::simlab-video` 替换为视频播放器。
- 同时支持标准多行引用块和截图中出现的一行写法。
- 加载失败时在播放器区域显示可理解的错误状态。
- 重复渲染时回收旧 `Blob` object URL。
- 添加后端、前端 API、前端解析与 DOM 渲染测试。

不包含：

- HLS/DASH 切片。
- 视频转码。
- DRM/加密。
- 封面图、试看、播放进度记录。
- 修改媒体插入侧边栏的交互设计。

## File Structure

后端：

- Modify: `simlab-training-service/simlab_training_service/storage.py`
  - 增加 `path_for_object()`，把数据库中的 `object_key` 安全解析成存储目录内的文件路径。
- Add: `simlab-training-service/simlab_training_service/routes_media_public.py`
  - 新增登录用户可访问的媒体内容读取接口。
- Modify: `simlab-training-service/simlab_training_service/app.py`
  - 注册 `routes_media_public.router`，同时注册 service prefix 路由。
- Modify: `simlab-training-service/tests/test_media_resources.py`
  - 增加媒体内容播放、鉴权、删除后不可播放的测试。

前端：

- Modify: `jupyterlab-training-platform/package.json`
  - 增加直接依赖 `@jupyterlab/rendermime`，并把播放器测试加入 `npm test`。
- Modify: `jupyterlab-training-platform/src/api/client.ts`
  - 增加 `mediaContentUrl()` 和 `fetchMediaBlob()`。
- Add: `jupyterlab-training-platform/src/notebook/simlabVideoDirective.ts`
  - 解析 `::simlab-video` 引用块，支持多行和一行格式。
- Add: `jupyterlab-training-platform/src/notebook/simlabVideoPlayer.ts`
  - 负责把渲染后的引用块 DOM 替换成 `<video controls>`，拉取 Blob，处理加载/失败状态，回收 object URL。
- Add: `jupyterlab-training-platform/src/notebook/simlabMarkdownRenderer.ts`
  - 包装 JupyterLab 原生 `RenderedMarkdown`，在 `renderModel()` 后执行播放器增强。
- Modify: `jupyterlab-training-platform/src/index.tsx`
  - 在插件启动时向 `IRenderMimeRegistry` 注册包装后的 Markdown renderer。
- Modify: `jupyterlab-training-platform/style/base.css`
  - 增加播放器容器、标题、状态、错误样式。
- Add: `jupyterlab-training-platform/tests/simlab-video-directive.test.js`
  - 覆盖引用块解析。
- Add: `jupyterlab-training-platform/tests/simlab-video-player.test.js`
  - 用 `jsdom` 覆盖 DOM 替换、Blob 拉取、object URL 回收。
- Modify: `jupyterlab-training-platform/tests/admin-api-client.test.js`
  - 覆盖新增媒体内容 URL 和 Blob fetch。

## Backend Design

公开视频接口使用普通登录用户鉴权，不用管理员权限：

```python
@router.get("/media/{media_id}/content")
def get_media_content(
    media_id: str,
    request: Request,
    db: Session = Depends(get_db),
    _user: CurrentUser = Depends(get_current_user),
):
    media = db.get(MediaResource, media_id)
    if not media or media.status != "available":
        raise HTTPException(status_code=404, detail="Media resource not found")
    path = request.app.state.storage.path_for_object(media.object_key)
    if not path.exists() or not path.is_file():
        raise HTTPException(status_code=404, detail="Media file not found")
    return FileResponse(
        path,
        media_type=media.mime_type or "application/octet-stream",
        filename=media.original_filename,
    )
```

`FileResponse` 让浏览器可以按普通视频响应读取内容，并让 Starlette 处理文件响应细节。路径必须通过 `TutorialStorage.path_for_object()` 从存储根目录解析，不能拼接用户输入路径。

## Frontend Design

播放器不直接写：

```html
<video src="/services/simlab-training/api/media/vid_xxx/content"></video>
```

原因：当前生产/compose 默认设置了 `JUPYTERHUB_API_URL`，后端会要求 `Authorization: token ...`。`<video src>` 无法手动设置这个 header。前端必须：

1. 用 `TrainingApiClient.fetchMediaBlob(mediaId)` 带 `Authorization` 拉取 Blob。
2. 用 `URL.createObjectURL(blob)` 生成本地 object URL。
3. 把 object URL 赋给 `<video>`。
4. cell 重新渲染或 widget dispose 时调用 `URL.revokeObjectURL(url)`。

## Directive Parsing Rules

有效格式：

```markdown
::simlab-video
media_id: vid_63f46f3e736d
title: 课程介绍
::
```

也接受截图中的一行格式：

```markdown
::simlab-video media_id: vid_63f46f3e736d title: 课程介绍 ::
```

无效格式：

```markdown
::simlab-video
title: 缺少 media_id
::
```

无效格式不会替换 DOM，继续显示原始 Markdown 渲染结果。

## Tasks

### Task 1: Add Public Media Content Endpoint

**Files:**
- Modify: `simlab-training-service/simlab_training_service/storage.py`
- Add: `simlab-training-service/simlab_training_service/routes_media_public.py`
- Modify: `simlab-training-service/simlab_training_service/app.py`
- Test: `simlab-training-service/tests/test_media_resources.py`

- [ ] **Step 1: Add failing backend tests**

Append these tests to `simlab-training-service/tests/test_media_resources.py`:

```python
def test_logged_in_user_can_fetch_uploaded_media_content(tmp_path):
    client = make_client(tmp_path)
    media = upload_video(
        client,
        title="课程介绍",
        filename="intro.mp4",
        content=b"fake-video-bytes",
    ).json()

    response = client.get(
        f"/api/media/{media['id']}/content",
        headers=user_headers(),
    )

    assert response.status_code == 200
    assert response.content == b"fake-video-bytes"
    assert response.headers["content-type"].startswith("video/mp4")


def test_media_content_requires_login(tmp_path):
    client = make_client(tmp_path)
    media = upload_video(client).json()

    response = client.get(f"/api/media/{media['id']}/content")

    assert response.status_code == 401


def test_media_content_returns_404_for_missing_media(tmp_path):
    client = make_client(tmp_path)

    response = client.get(
        "/api/media/vid_missing/content",
        headers=user_headers(),
    )

    assert response.status_code == 404


def test_deleted_media_content_is_not_available(tmp_path):
    client = make_client(tmp_path)
    media = upload_video(client, title="待删除视频").json()
    client.delete(f"/api/admin/media/{media['id']}", headers=admin_headers())

    response = client.get(
        f"/api/media/{media['id']}/content",
        headers=user_headers(),
    )

    assert response.status_code == 404
```

- [ ] **Step 2: Run backend tests and verify failure**

Run:

```bash
cd /home/yuan/my_project/simlab-training-service
pytest tests/test_media_resources.py -q
```

Expected: FAIL because `/api/media/{media_id}/content` is not registered.

- [ ] **Step 3: Add safe storage path resolver**

Modify `simlab-training-service/simlab_training_service/storage.py`:

```python
    def path_for_object(self, object_key: str) -> Path:
        target = (self.root / object_key).resolve()
        root = self.root.resolve()
        if root not in target.parents and target != root:
            raise ValueError("Stored object is outside the storage root")
        return target
```

Place this method inside `TutorialStorage`, after `load()`.

- [ ] **Step 4: Add public media route**

Create `simlab-training-service/simlab_training_service/routes_media_public.py`:

```python
from __future__ import annotations

from fastapi import APIRouter, Depends, HTTPException, Request
from fastapi.responses import FileResponse
from sqlalchemy.orm import Session

from .auth import CurrentUser, get_current_user
from .db import get_db
from .models import MediaResource


router = APIRouter(prefix="/api/media")


@router.get("/{media_id}/content")
def get_media_content(
    media_id: str,
    request: Request,
    db: Session = Depends(get_db),
    _user: CurrentUser = Depends(get_current_user),
):
    media = db.get(MediaResource, media_id)
    if not media or media.status != "available":
        raise HTTPException(status_code=404, detail="Media resource not found")

    try:
        path = request.app.state.storage.path_for_object(media.object_key)
    except ValueError:
        raise HTTPException(status_code=404, detail="Media file not found") from None

    if not path.exists() or not path.is_file():
        raise HTTPException(status_code=404, detail="Media file not found")

    return FileResponse(
        path,
        media_type=media.mime_type or "application/octet-stream",
        filename=media.original_filename,
    )
```

- [ ] **Step 5: Register public media route**

Modify `simlab-training-service/simlab_training_service/app.py`:

```python
from .routes_media_public import router as media_public_router
```

Then register it next to the other routers:

```python
    app.include_router(media_public_router)
```

And under `service_prefix`:

```python
        app.include_router(media_public_router, prefix=service_prefix)
```

- [ ] **Step 6: Run backend tests**

Run:

```bash
cd /home/yuan/my_project/simlab-training-service
pytest tests/test_media_resources.py -q
```

Expected: PASS.

- [ ] **Step 7: Run full service tests**

Run:

```bash
cd /home/yuan/my_project/simlab-training-service
pytest -q
```

Expected: PASS.

### Task 2: Add Frontend API Support for Media Blob Fetching

**Files:**
- Modify: `jupyterlab-training-platform/src/api/client.ts`
- Modify: `jupyterlab-training-platform/tests/admin-api-client.test.js`

- [ ] **Step 1: Add failing API client tests**

Append these tests to `jupyterlab-training-platform/tests/admin-api-client.test.js` before `run()`:

```javascript
async function testMediaContentUrlUsesPublicMediaEndpoint() {
  const client = new TrainingApiClient('/api');

  assert.equal(
    client.mediaContentUrl('vid_123'),
    '/api/media/vid_123/content'
  );
}

async function testFetchMediaBlobUsesAuthorizationHeaders() {
  const calls = [];
  const blob = new Blob(['fake-video'], { type: 'video/mp4' });
  global.fetch = async (url, init) => {
    calls.push({ url, init });
    return {
      ok: true,
      status: 200,
      blob: async () => blob,
      text: async () => ''
    };
  };
  PageConfig.setOption('token', 'page-token-123');
  const client = new TrainingApiClient('/api');

  const result = await client.fetchMediaBlob('vid_123');

  assert.equal(result, blob);
  assert.equal(calls.length, 1);
  assert.equal(calls[0].url, '/api/media/vid_123/content');
  assert.equal(calls[0].init.method, 'GET');
  assert.equal(calls[0].init.credentials, 'same-origin');
  assert.equal(calls[0].init.headers.Authorization, 'token page-token-123');
}
```

Update `run()`:

```javascript
  await testMediaContentUrlUsesPublicMediaEndpoint();
  await testFetchMediaBlobUsesAuthorizationHeaders();
```

- [ ] **Step 2: Run API client tests and verify failure**

Run:

```bash
cd /home/yuan/my_project/jupyterlab-training-platform
npm run build:lib && node tests/admin-api-client.test.js
```

Expected: FAIL because `mediaContentUrl()` and `fetchMediaBlob()` do not exist.

- [ ] **Step 3: Implement API methods**

Add these public methods to `TrainingApiClient` in `jupyterlab-training-platform/src/api/client.ts`, after `adminDeleteMedia()`:

```typescript
  mediaContentUrl(mediaId: string): string {
    return `${this.baseUrl}/media/${encodeURIComponent(mediaId)}/content`;
  }

  async fetchMediaBlob(mediaId: string): Promise<Blob> {
    const response = await fetch(this.mediaContentUrl(mediaId), {
      method: 'GET',
      credentials: 'same-origin',
      headers: {
        ...authorizationHeaderFromToken()
      }
    });
    if (!response.ok) {
      const detail = await response.text();
      throw new Error(`请求失败 ${response.status}: ${detail}`);
    }
    return response.blob();
  }
```

- [ ] **Step 4: Run API client tests**

Run:

```bash
cd /home/yuan/my_project/jupyterlab-training-platform
npm run build:lib && node tests/admin-api-client.test.js
```

Expected: PASS.

### Task 3: Parse SimLab Video Directives

**Files:**
- Add: `jupyterlab-training-platform/src/notebook/simlabVideoDirective.ts`
- Add: `jupyterlab-training-platform/tests/simlab-video-directive.test.js`
- Modify: `jupyterlab-training-platform/package.json`

- [ ] **Step 1: Add failing parser tests**

Create `jupyterlab-training-platform/tests/simlab-video-directive.test.js`:

```javascript
const assert = require('node:assert/strict');

const {
  parseSimlabVideoDirective,
  parseSimlabVideoDirectives,
  normalizeDirectiveText
} = require('../lib/notebook/simlabVideoDirective.js');

function testParsesMultilineDirective() {
  assert.deepEqual(
    parseSimlabVideoDirective('::simlab-video\nmedia_id: vid_abc123\ntitle: 课程介绍\n::'),
    {
      mediaId: 'vid_abc123',
      title: '课程介绍',
      raw: '::simlab-video\nmedia_id: vid_abc123\ntitle: 课程介绍\n::'
    }
  );
}

function testParsesSingleLineDirective() {
  assert.deepEqual(
    parseSimlabVideoDirective('::simlab-video media_id: vid_abc123 title: 课程介绍 ::'),
    {
      mediaId: 'vid_abc123',
      title: '课程介绍',
      raw: '::simlab-video media_id: vid_abc123 title: 课程介绍 ::'
    }
  );
}

function testUsesMediaIdWhenTitleIsMissing() {
  assert.deepEqual(
    parseSimlabVideoDirective('::simlab-video\nmedia_id: vid_abc123\n::'),
    {
      mediaId: 'vid_abc123',
      title: 'vid_abc123',
      raw: '::simlab-video\nmedia_id: vid_abc123\n::'
    }
  );
}

function testRejectsDirectiveWithoutMediaId() {
  assert.equal(
    parseSimlabVideoDirective('::simlab-video\ntitle: 缺少编号\n::'),
    null
  );
}

function testFindsMultipleDirectivesInMarkdownSource() {
  assert.deepEqual(
    parseSimlabVideoDirectives('# 第一章\n\n::simlab-video\nmedia_id: vid_a\n::\n\n::simlab-video media_id: vid_b title: 第二段 ::')
      .map(item => [item.mediaId, item.title]),
    [
      ['vid_a', 'vid_a'],
      ['vid_b', '第二段']
    ]
  );
}

function testNormalizeDirectiveTextCollapsesWhitespace() {
  assert.equal(
    normalizeDirectiveText('::simlab-video\nmedia_id: vid_a\ntitle: 第一章\n::'),
    '::simlab-video media_id: vid_a title: 第一章 ::'
  );
}

function run() {
  testParsesMultilineDirective();
  testParsesSingleLineDirective();
  testUsesMediaIdWhenTitleIsMissing();
  testRejectsDirectiveWithoutMediaId();
  testFindsMultipleDirectivesInMarkdownSource();
  testNormalizeDirectiveTextCollapsesWhitespace();
  console.log('simlab video directive tests passed');
}

run();
```

Update `jupyterlab-training-platform/package.json` test script to include:

```json
"node tests/simlab-video-directive.test.js"
```

Place it after `node tests/media-insertion.test.js`.

- [ ] **Step 2: Run parser tests and verify failure**

Run:

```bash
cd /home/yuan/my_project/jupyterlab-training-platform
npm run build:lib && node tests/simlab-video-directive.test.js
```

Expected: FAIL because `simlabVideoDirective.ts` does not exist.

- [ ] **Step 3: Implement parser**

Create `jupyterlab-training-platform/src/notebook/simlabVideoDirective.ts`:

```typescript
export interface SimlabVideoDirective {
  mediaId: string;
  title: string;
  raw: string;
}

const DIRECTIVE_PATTERN = /::simlab-video\b[\s\S]*?::/g;
const MEDIA_ID_PATTERN = /\bmedia_id:\s*([A-Za-z0-9_-]+)/;
const MULTILINE_TITLE_PATTERN = /^title:\s*(.+)$/m;
const SINGLE_LINE_TITLE_PATTERN = /\btitle:\s*([\s\S]+)$/;

export function normalizeDirectiveText(value: string): string {
  return value.replace(/\s+/g, ' ').trim();
}

export function parseSimlabVideoDirectives(source: string): SimlabVideoDirective[] {
  const directives: SimlabVideoDirective[] = [];
  for (const match of source.matchAll(DIRECTIVE_PATTERN)) {
    const directive = parseSimlabVideoDirective(match[0]);
    if (directive) {
      directives.push(directive);
    }
  }
  return directives;
}

export function parseSimlabVideoDirective(source: string): SimlabVideoDirective | null {
  const raw = source.trim();
  if (!raw.startsWith('::simlab-video') || !raw.endsWith('::')) {
    return null;
  }

  const mediaMatch = MEDIA_ID_PATTERN.exec(raw);
  if (!mediaMatch) {
    return null;
  }

  const mediaId = mediaMatch[1];
  const body = raw
    .replace(/^::simlab-video\b/, '')
    .replace(/::$/, '')
    .trim();
  const multilineTitle = MULTILINE_TITLE_PATTERN.exec(body);
  const singleLineTitle = SINGLE_LINE_TITLE_PATTERN.exec(body);
  const title = (multilineTitle?.[1] ?? singleLineTitle?.[1] ?? mediaId).trim();

  return {
    mediaId,
    title: title || mediaId,
    raw
  };
}
```

- [ ] **Step 4: Run parser tests**

Run:

```bash
cd /home/yuan/my_project/jupyterlab-training-platform
npm run build:lib && node tests/simlab-video-directive.test.js
```

Expected: PASS.

### Task 4: Replace Rendered Directive Blocks With Video Players

**Files:**
- Add: `jupyterlab-training-platform/src/notebook/simlabVideoPlayer.ts`
- Add: `jupyterlab-training-platform/tests/simlab-video-player.test.js`
- Modify: `jupyterlab-training-platform/package.json`

- [ ] **Step 1: Add failing DOM renderer tests**

Create `jupyterlab-training-platform/tests/simlab-video-player.test.js`:

```javascript
const assert = require('node:assert/strict');
const { JSDOM } = require('jsdom');

const {
  enhanceSimlabVideoDirectives,
  revokeSimlabVideoObjectUrls
} = require('../lib/notebook/simlabVideoPlayer.js');

function installDom() {
  const dom = new JSDOM('<!doctype html><html><body></body></html>');
  global.window = dom.window;
  global.document = dom.window.document;
  global.HTMLElement = dom.window.HTMLElement;
  return dom;
}

async function testReplacesParagraphDirectiveWithPlayer() {
  installDom();
  const host = document.createElement('div');
  host.innerHTML = '<p>::simlab-video media_id: vid_abc123 title: 课程介绍 ::</p>';
  const calls = [];
  const api = {
    fetchMediaBlob: async mediaId => {
      calls.push(mediaId);
      return new Blob(['fake-video'], { type: 'video/mp4' });
    }
  };
  const created = [];
  global.URL.createObjectURL = blob => {
    created.push(blob);
    return 'blob:simlab-video';
  };
  global.URL.revokeObjectURL = () => undefined;

  await enhanceSimlabVideoDirectives(host, api);

  const figure = host.querySelector('.training-video-player');
  const video = host.querySelector('video');
  assert.ok(figure);
  assert.ok(video);
  assert.equal(figure.dataset.mediaId, 'vid_abc123');
  assert.equal(video.getAttribute('controls'), '');
  assert.equal(video.src, 'blob:simlab-video');
  assert.deepEqual(calls, ['vid_abc123']);
  assert.equal(created.length, 1);
}

async function testLeavesInvalidDirectiveUntouched() {
  installDom();
  const host = document.createElement('div');
  host.innerHTML = '<p>::simlab-video title: 缺少编号 ::</p>';
  const api = {
    fetchMediaBlob: async () => {
      throw new Error('should not fetch');
    }
  };

  await enhanceSimlabVideoDirectives(host, api);

  assert.equal(host.querySelector('video'), null);
  assert.equal(host.textContent.trim(), '::simlab-video title: 缺少编号 ::');
}

async function testShowsLoadErrorWhenBlobFetchFails() {
  installDom();
  const host = document.createElement('div');
  host.innerHTML = '<p>::simlab-video media_id: vid_missing title: 丢失视频 ::</p>';
  const api = {
    fetchMediaBlob: async () => {
      throw new Error('404');
    }
  };

  await enhanceSimlabVideoDirectives(host, api);

  assert.equal(host.querySelector('video'), null);
  assert.match(host.querySelector('.training-video-error').textContent, /视频加载失败/);
}

async function testRevokesObjectUrls() {
  installDom();
  const host = document.createElement('div');
  const video = document.createElement('video');
  video.dataset.objectUrl = 'blob:old-url';
  host.appendChild(video);
  const revoked = [];
  global.URL.revokeObjectURL = value => {
    revoked.push(value);
  };

  revokeSimlabVideoObjectUrls(host);

  assert.deepEqual(revoked, ['blob:old-url']);
  assert.equal(video.dataset.objectUrl, undefined);
}

async function run() {
  await testReplacesParagraphDirectiveWithPlayer();
  await testLeavesInvalidDirectiveUntouched();
  await testShowsLoadErrorWhenBlobFetchFails();
  await testRevokesObjectUrls();
  console.log('simlab video player tests passed');
}

run().catch(error => {
  console.error(error);
  process.exit(1);
});
```

Update `jupyterlab-training-platform/package.json` test script to include:

```json
"node tests/simlab-video-player.test.js"
```

Place it after `node tests/simlab-video-directive.test.js`.

- [ ] **Step 2: Run DOM renderer tests and verify failure**

Run:

```bash
cd /home/yuan/my_project/jupyterlab-training-platform
npm run build:lib && node tests/simlab-video-player.test.js
```

Expected: FAIL because `simlabVideoPlayer.ts` does not exist.

- [ ] **Step 3: Implement DOM enhancer**

Create `jupyterlab-training-platform/src/notebook/simlabVideoPlayer.ts`:

```typescript
import { TrainingApiClient } from '../api/client';
import {
  normalizeDirectiveText,
  parseSimlabVideoDirective,
  SimlabVideoDirective
} from './simlabVideoDirective';

type MediaBlobClient = Pick<TrainingApiClient, 'fetchMediaBlob'>;

export async function enhanceSimlabVideoDirectives(
  host: HTMLElement,
  api: MediaBlobClient
): Promise<void> {
  const targets = findDirectiveElements(host);
  await Promise.all(targets.map(async target => {
    const directive = parseSimlabVideoDirective(target.textContent ?? '');
    if (!directive) {
      return;
    }
    const player = await createPlayer(directive, api);
    target.replaceWith(player);
  }));
}

export function revokeSimlabVideoObjectUrls(host: HTMLElement): void {
  host.querySelectorAll<HTMLVideoElement>('video[data-object-url]').forEach(video => {
    const objectUrl = video.dataset.objectUrl;
    if (objectUrl) {
      URL.revokeObjectURL(objectUrl);
      delete video.dataset.objectUrl;
    }
  });
}

function findDirectiveElements(host: HTMLElement): HTMLElement[] {
  return Array.from(host.querySelectorAll<HTMLElement>('p, pre')).filter(element => {
    const text = element.textContent ?? '';
    return normalizeDirectiveText(text).startsWith('::simlab-video ')
      && normalizeDirectiveText(text).endsWith(' ::')
      && parseSimlabVideoDirective(text) !== null;
  });
}

async function createPlayer(
  directive: SimlabVideoDirective,
  api: MediaBlobClient
): Promise<HTMLElement> {
  const figure = document.createElement('figure');
  figure.className = 'training-video-player';
  figure.dataset.mediaId = directive.mediaId;

  const caption = document.createElement('figcaption');
  caption.className = 'training-video-title';
  caption.textContent = directive.title;
  figure.appendChild(caption);

  const status = document.createElement('div');
  status.className = 'training-video-status';
  status.textContent = '正在加载视频...';
  figure.appendChild(status);

  try {
    const blob = await api.fetchMediaBlob(directive.mediaId);
    const objectUrl = URL.createObjectURL(blob);
    const video = document.createElement('video');
    video.controls = true;
    video.preload = 'metadata';
    video.src = objectUrl;
    video.dataset.objectUrl = objectUrl;
    figure.replaceChild(video, status);
  } catch (_error) {
    status.className = 'training-video-error';
    status.textContent = '视频加载失败，请确认媒体资源仍然存在。';
  }

  return figure;
}
```

- [ ] **Step 4: Run DOM renderer tests**

Run:

```bash
cd /home/yuan/my_project/jupyterlab-training-platform
npm run build:lib && node tests/simlab-video-player.test.js
```

Expected: PASS.

### Task 5: Register Markdown Renderer Wrapper

**Files:**
- Add: `jupyterlab-training-platform/src/notebook/simlabMarkdownRenderer.ts`
- Modify: `jupyterlab-training-platform/src/index.tsx`
- Modify: `jupyterlab-training-platform/package.json`

- [ ] **Step 1: Add direct dependency**

Modify `jupyterlab-training-platform/package.json` dependencies:

```json
"@jupyterlab/rendermime": "^4.2.7"
```

Keep it near the other `@jupyterlab/*` dependencies.

- [ ] **Step 2: Create Markdown renderer wrapper**

Create `jupyterlab-training-platform/src/notebook/simlabMarkdownRenderer.ts`:

```typescript
import {
  IRenderMime,
  RenderedMarkdown
} from '@jupyterlab/rendermime';
import { TrainingApiClient } from '../api/client';
import {
  enhanceSimlabVideoDirectives,
  revokeSimlabVideoObjectUrls
} from './simlabVideoPlayer';

export function createSimlabMarkdownRendererFactory(
  api: TrainingApiClient
): IRenderMime.IRendererFactory {
  return {
    safe: true,
    mimeTypes: ['text/markdown'],
    defaultRank: 60,
    createRenderer: options => new SimlabMarkdownRenderer(options, api)
  };
}

class SimlabMarkdownRenderer extends RenderedMarkdown {
  constructor(
    options: IRenderMime.IRendererOptions,
    private readonly api: TrainingApiClient
  ) {
    super(options);
  }

  async renderModel(model: IRenderMime.IMimeModel): Promise<void> {
    revokeSimlabVideoObjectUrls(this.node);
    await super.renderModel(model);
    await enhanceSimlabVideoDirectives(this.node, this.api);
  }

  dispose(): void {
    revokeSimlabVideoObjectUrls(this.node);
    super.dispose();
  }
}
```

- [ ] **Step 3: Register wrapper in plugin activation**

Modify imports in `jupyterlab-training-platform/src/index.tsx`:

```typescript
import { IRenderMimeRegistry } from '@jupyterlab/rendermime';
import { createSimlabMarkdownRendererFactory } from './notebook/simlabMarkdownRenderer';
```

Modify plugin `requires`:

```typescript
requires: [ICommandPalette, IMainMenu, INotebookTracker, IRenderMimeRegistry],
```

Modify `activate` signature:

```typescript
    notebookTracker: INotebookTracker,
    rendermime: IRenderMimeRegistry
```

After `const api = new TrainingApiClient();`, register the renderer:

```typescript
    rendermime.addFactory(createSimlabMarkdownRendererFactory(api), 60);
```

- [ ] **Step 4: Build TypeScript**

Run:

```bash
cd /home/yuan/my_project/jupyterlab-training-platform
npm run build:lib
```

Expected: PASS.

### Task 6: Add Player Styling

**Files:**
- Modify: `jupyterlab-training-platform/style/base.css`

- [ ] **Step 1: Add CSS**

Append to `jupyterlab-training-platform/style/base.css`:

```css
.training-video-player {
  margin: 12px 0;
  padding: 10px;
  border: 1px solid #cfd7df;
  background: #ffffff;
}

.training-video-title {
  margin: 0 0 8px;
  font-size: 14px;
  font-weight: 700;
  color: #202a33;
}

.training-video-player video {
  display: block;
  width: 100%;
  max-height: 520px;
  background: #111820;
}

.training-video-status,
.training-video-error {
  min-height: 44px;
  display: flex;
  align-items: center;
  border: 1px dashed #bcc7d2;
  padding: 8px 10px;
  color: #5c6b78;
  background: #f7f9fb;
}

.training-video-error {
  border-color: #d49a8a;
  color: #8a2f1d;
  background: #fff7f4;
}
```

- [ ] **Step 2: Build labextension**

Run:

```bash
cd /home/yuan/my_project/jupyterlab-training-platform
npm run build
```

Expected: PASS.

### Task 7: Run Full Automated Verification

**Files:**
- No source changes.

- [ ] **Step 1: Run backend tests**

Run:

```bash
cd /home/yuan/my_project/simlab-training-service
pytest -q
```

Expected: PASS.

- [ ] **Step 2: Run frontend tests**

Run:

```bash
cd /home/yuan/my_project/jupyterlab-training-platform
npm test
```

Expected: PASS.

- [ ] **Step 3: Build production frontend**

Run:

```bash
cd /home/yuan/my_project/jupyterlab-training-platform
npm run build:prod
```

Expected: PASS.

### Task 8: Manual Acceptance

**Files:**
- No source changes unless manual testing finds a bug.

- [ ] **Step 1: Start training service**

Run:

```bash
cd /home/yuan/my_project
tools/training-service-dev-up.sh
```

Expected: service starts and health check succeeds.

- [ ] **Step 2: Rebuild or reload JupyterLab extension**

Run:

```bash
cd /home/yuan/my_project/jupyterlab-training-platform
npm run build
```

Expected: build succeeds and JupyterLab picks up the updated labextension after browser refresh or server restart.

- [ ] **Step 3: Upload video**

Manual steps:

- 登录管理员用户。
- 打开 `管理员工具` -> `上传媒体资源`。
- 上传一个 `.mp4` 或 `.webm` 文件，标题填写 `课程介绍`。
- 确认列表中出现 `vid_...` 媒体 ID。

Expected: upload succeeds.

- [ ] **Step 4: Insert video reference**

Manual steps:

- 打开一个 Notebook。
- 打开 `管理员工具` -> `插入媒体资源`。
- 选中刚上传的视频，点击插入。

Expected: cell 内容是：

```markdown
::simlab-video
media_id: vid_xxxxxxxxxxxx
title: 课程介绍
::
```

- [ ] **Step 5: Render Markdown cell**

Manual steps:

- 把 cell 保持为 Markdown。
- 执行或渲染该 Markdown cell。

Expected:

- 引用块不再显示为普通文本。
- cell 中出现标题 `课程介绍`。
- cell 中出现浏览器原生视频播放器。
- 点击播放按钮可以播放视频。

- [ ] **Step 6: Verify student playback**

Manual steps:

- 用普通学生用户打开包含该媒体引用的教程 Notebook。
- 渲染 Markdown cell。

Expected:

- 普通学生用户也能看到播放器并播放视频。
- 学生用户仍然看不到 `上传媒体资源` 和 `插入媒体资源` 管理员命令。

- [ ] **Step 7: Verify failure state**

Manual steps:

- 管理员删除该媒体资源。
- 回到包含旧 `media_id` 的 Notebook。
- 重新渲染 Markdown cell。

Expected:

- 页面不崩溃。
- 播放器区域显示 `视频加载失败，请确认媒体资源仍然存在。`

- [ ] **Step 8: Stop training service**

Run:

```bash
cd /home/yuan/my_project
tools/training-service-down.sh
```

Expected: service containers stop cleanly.

## Risks And Mitigations

- **Risk:** `<video src>` 无法携带 `Authorization`，在启用 `JUPYTERHUB_API_URL` 时会 401。
  - **Mitigation:** 前端用 `fetchMediaBlob()` 携带 `Authorization`，再用 object URL 播放。
- **Risk:** object URL 重复渲染后泄漏。
  - **Mitigation:** 每次 `renderModel()` 前和 `dispose()` 时调用 `revokeSimlabVideoObjectUrls()`。
- **Risk:** 替换所有 Markdown renderer 影响普通 Markdown。
  - **Mitigation:** 包装 JupyterLab 原生 `RenderedMarkdown`，原生渲染完成后只处理独立的 `::simlab-video` 段落或 pre 块。
- **Risk:** 手写一行格式和自动插入的多行格式不一致。
  - **Mitigation:** parser 同时支持多行和一行格式，测试覆盖两者。
- **Risk:** 学生端无权访问管理员媒体列表。
  - **Mitigation:** 播放接口独立于管理员列表接口，只要求普通登录用户。

## Definition Of Done

- `GET /api/media/{media_id}/content` 对登录用户返回视频内容。
- 管理员接口仍然不暴露 `object_key`。
- Notebook Markdown cell 的 `::simlab-video` 块渲染为 `<video controls>`。
- 启用 `JUPYTERHUB_API_URL` 时播放器仍能加载视频 Blob。
- 删除媒体后旧引用显示失败状态，不导致前端异常。
- `pytest -q` 在 `simlab-training-service` 通过。
- `npm test` 在 `jupyterlab-training-platform` 通过。
- `npm run build:prod` 在 `jupyterlab-training-platform` 通过。
