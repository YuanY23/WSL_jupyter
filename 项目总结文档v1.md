# SimLab / simhub 项目总结报告 (v1)

本报告对 **SimLab / simhub**（原理仿真与作业评测平台）的设计架构、技术实现、核心组件以及关键技术创新进行了深度梳理与总结。

---

## 一、 项目概述

**SimLab / simhub** 是一款面向热能工程、过程装备及通用工程学科的**交互式可视化仿真教学与作业评测平台**。

该项目基于 JupyterHub 容器化集群架构，通过深度重构与定制 JupyterLab 核心、自主开发四大交互式仿真插件，并深度整合 JupyterHub 与 nbgrader（作业评测系统），打破了传统数值计算“纯写代码”的高门槛，实现了“**GUI 参数拖拽调节 - 代码实时同步修改 - 物理模型自动重算 - 结果图表毫秒级响应**”的全新交互教学体验，同时为教师提供了无缝集成的作业分发与自动评测环境。

---

## 二、 核心技术栈与系统架构

项目采用模块化、微服务化的容器集群架构，其底层技术栈如下所示：

```mermaid
graph TD
    User([用户浏览器]) -->|HTTP / WebSockets| Proxy[可配置 HTTP 反向代理 Configurable HTTP Proxy]
    Proxy -->|路由分发| Hub[JupyterHub 核心服务]
    Hub -->|OAuth 授权认证 & 用户数据库 SQLite| Auth[自定义原生认证器 CustomNativeAuthenticator]
    Hub -->|Docker API 动态生成容器| Spawner[DockerSpawner]
    Spawner -->|启动用户专用容器| UserContainer[JupyterLab 单人工作区容器]
    
    subgraph 单人工作区容器 (JupyterLab Single-user Container)
        UserContainer -->|前端 UI 层| JLabCore[定制版 JupyterLab 核心]
        JLabCore -->|左侧栏| Ext1[参数自动绑定插件 param-binding]
        JLabCore -->|主工作区 Tab 页| Ext2[传热仿真平台 thermal-design]
        JLabCore -->|主工作区 Tab 页| Ext3[通用仿真平台 simulation-platform]
        JLabCore -->|主工作区 Tab 页| Ext4[官方热力建模示例 official-thermal-examples]
        JLabCore -->|评测系统集成| Nbgrader[定制版 nbgrader 作业平台]
        
        UserContainer -->|后端计算内核| Kernel[IPython Kernel]
        Kernel -->|科学计算基础库| SciLibraries[numpy / scipy / numba / matplotlib]
    end
    
    HostCourses[(宿主机课程数据目录 /courses)] -->|宿主机卷挂载| UserContainer
    HostExchange[(宿主机交换区目录 /exchange)] -->|共享卷挂载| UserContainer
```

### 1. 基础设施与容器底座
*   **基础镜像**：基于 `jupyter/base-notebook:ubuntu-22.04`，精简了不必要的图形库（如 PySide6），确保镜像轻量、快速构建。
*   **动态容器管理**：使用 `DockerSpawner` 管理用户容器，实现多租户计算资源的强物理隔离。
*   **持久化存储**：
    *   用户 Home 目录通过数据卷 `jupyterhub-user-{username}` 持久化。
    *   教师账号启动时，根据其所属的课程组（如 `formgrade-math1`），由宿主机自动创建对应的课程目录（如 `/home/yuan/my_project/courses/math1`），并通过 `pre_spawn_hook` 动态挂载到教师容器内的 `/home/jovyan/math1`。
    *   全局共享交换区挂载于 `/srv/nbgrader/exchange`，用于 nbgrader 课件/作业的分发与回收。

### 2. 认证与权限控制
*   **自定义原生认证器 (`CustomNativeAuthenticator`)**：继承自 `nativeauthenticator`，对其进行如下拓展：
    *   **角色注入与课程绑定**：在注册页面（`signup.html`）中提取“教师/学生”角色和“班级/课程 ID”。
    *   **动态角色分配**：教师注册时，系统在 SQLite 数据库中自动创建 `formgrade-{course_id}` 组和全局 `teachers` 组，并将教师加入其中；学生注册时自动分配至 `nbgrader-{course_id}` 组。
    *   **细粒度权限控制 (RBAC)**：通过 JupyterHub 角色配置，授予全局 `teachers` 组 `list:users`、`read:users`、`read:groups`、`admin:groups` 等高级 API 访问权限，以支持其在前端同步和管理学生。

---

## 三、 四大 JupyterLab 自主插件系统

为打破传统代码环境对非计算机专业学生的门槛，SimLab 采用 TypeScript + React 技术栈开发了 4 个相互协作的 JupyterLab 核心插件：

### 1. 自动参数绑定插件 (`jupyterlab-param-binding`)
该插件是实现“低代码/无代码交互”的核心纽带，其工作机制如下：
*   **参数层代码检测**：插件会自动扫描 Notebook 中位于包含“参数层代码”关键字（如 `## 参数层代码`）的 Markdown 标题下方的代码单元格。
*   **语法解析与绑定**：后台静态解析 Python 赋值语句（如 `k = 0.05`、`enable_flag = True`），根据数据类型和元数据自动在 JupyterLab 左侧面板渲染交互式控件（**数值输入框、滑动条 Slider、开关 Switch、下拉菜单 Dropdown、文本框 Text**）。
*   **属性持久化**：用户设置 of 控件区间（`min`、`max`、`step`）与显示别名，会作为 JSON 写入该 Notebook 文件的 metadata 中（键名为 `simulation_param_bindings`），随文件一同分发和保存。
*   **下游响应式重算 (Reactive Run)**：当用户在侧边栏拖动滑块时，插件会**精准修改 Notebook 中对应代码单元格的文本行**，并利用 `NotebookTracker` 和 `sessionContext` **自动执行该单元格及其下游的所有计算单元格**，使 Matplotlib 生成的物理图像能够随着滑块拖动产生平滑的动画效果。

### 2. 传热仿真平台 (`jupyterlab-thermal-design`)
专为经典热力学与传热学课程定制的可视化计算器：
*   **支持 14 种经典物理场景**，包含：
    *   *导热类*：一维稳态无限大平板 (FDM)、多层复合平板 (FVM)、圆筒壁径向导热 (FDM)、等截面直肋导热 (FVM + 线性化对流源项)、一维瞬态导热两侧恒温 (显式 FTCS)、一维瞬态导热一侧恒定热流 (隐式 FVM)、内热源平壁导热、二维稳态平板导热。
    *   *对流类*：平板层流强制对流、平板湍流强制对流、竖板自然对流、圆管内强迫对流。
    *   *辐射类*：平行平板辐射换热、三表面空腔辐射。
*   **自包含 Notebook 生成**：在图形化界面点击“运行”后，后台 `NotebookGenerator` 会根据选定的物理场景和用户输入参数，动态拼接生成包含**物理问题描述、模型假设、参数说明表、控制微分方程、FDM/FVM 求解代数矩阵方程、Matplotlib 绘图、分析提示**在内的完整 12 栏结构 `.ipynb` 文件，并自动在工作区静默打开。

### 3. 通用仿真平台 (`jupyterlab-simulation-platform`)
提供抽象的数理模型模板生成器，包括：
*   **代数方程/经验公式**（如光伏组件输出功率计算，支持参数扫描和多曲线对比）。
*   **一阶动态系统**（如储能 SOC 变化，支持显式欧拉时间迭代）。
*   **二阶动态系统**（如弹簧-质量-阻尼器动力学响应）。
*   **线性方程组网络平衡**（输入 $A x = b$ 系数矩阵并求解）。
*   **一维传热/扩散偏微分方程有限差分求解器**。
*   **时序能量平衡**（光伏-储能-负荷多变量联合平衡）。
*   **简单优化调度**（采用 `scipy.optimize.linprog`，在时分电价下求解电网-储能协同最低购电成本）。

### 4. 官方热力建模示例 (`jupyterlab-official-thermal-examples`)
以“槽式太阳能光热发电集热-储热-发电联合过程”为核心的完整 Process 系统动态仿真：
*   输入典型日（夏季晴天、冬季晴天、多云波动、阴天）的 24 小时 DNI（直接太阳辐射度）谱与环境温度。
*   利用小时级集热器效率公式、熔盐双罐储热罐（热盐罐、冷盐罐）液位动态方程、汽轮机部分负荷发电效率模型，以及“太阳能直供”、“充热”、“放热发电”、“待机”、“弃热”等 6 种规则状态机的切换。
*   全方位展示工业级新能源电站的复杂联合动态演化过程。

---

## 四、 nbgrader (作业评测平台) 深度集成

项目对开源作业评测系统 `nbgrader` 进行了深度的定制开发，解决了其在 JupyterHub 容器化环境下的诸多痛点：

### 1. 解决 Token 冲突的“双 Token”鉴权设计
*   **痛点**：在 DockerSpawner 模式下，nbgrader 默认会用自身的管理员 Token 覆盖容器内的 `JUPYTERHUB_API_TOKEN`，但这会导致 JupyterHub 单人容器自身的 OAuth 认证失效，引发无限重定向循环（Redirect Loop）。
*   **解决**：修改了 `nbgrader/nbgrader/auth/jupyterhub.py`，保持原本 of OAuth Token 不变，改用单独的自定义环境变量 `NBGRADER_ADMIN_API_TOKEN` 传递管理权限。在 nbgrader 发送 API 请求给 Hub 时，优先读取此 Token 进行授权，从而完美兼容了 Docker 容器化运行与 JupyterHub OAuth 单点登录。

### 2. 班级学生列表零配置自动同步 (`Auto-Sync`)
*   **痛点**：传统 nbgrader 要求教师手动维护 SQLite `gradebook.db` 中的学生数据库，或者导入 CSV，这给日常教学带来了极大的维护工作量。
*   **解决**：重构了 `nbgrader/nbgrader/server_extensions/formgrader/apihandlers.py` 中的 `StudentCollectionHandler`（处理前端学生列表的 API）。当教师打开 Formgrader 页面时，系统会自动向 JupyterHub 发送 API 请求，拉取当前课程组（`nbgrader-{course_id}`）下所有已注册的学生用户名，并在 gradebook 数据库中执行 `update_or_create_student`。教师无需任何配置，即可实现学生名单的一键自动同步。

### 3. 基于启动脚本的考试环境沙箱化 (`disable_teacher_extensions.sh`)
*   **安全机制**：在 Docker 容器启动时（`before-notebook.d` 钩子），系统执行 Shell 脚本。该脚本会自动检测当前容器用户的组属性。如果用户不属于 `teachers` 组（即普通学生），脚本会利用 `jupyter labextension disable` 命令**强制禁用** `nbgrader`、`formgrader` 等教师专属评测与改卷插件，防止学生利用开发者工具获取敏感 API 接口，保证了在线作业与考试环境的安全与公平。

### 4. UI 框架兼容性平替
*   将 Formgrader 内部的静态前端依赖项（Bootstrap）固定为稳定的 `3.4.1` 版本，解决了由于 Bootstrap 5 版本冲突导致的前端表格错位、导航栏崩溃等界面渲染问题，确保界面的精致美观与操作流畅。

---

## 五、 项目核心创新点总结

| 创新维度 | 传统模式 / 开源方案 | SimLab / simhub 创新实现 | 带来的核心价值 |
| :--- | :--- | :--- | :--- |
| **交互范式** | 纯写代码（门槛高，排错难）或 纯封闭 GUI（无法查看和修改底层逻辑） | **代码与 GUI 双向绑定**（滑块直接重写 Python 赋值语句，下游响应式重算） | 降低了非计算机专业学生进行数值仿真的门槛，保留了 Notebook 的透明性和可修改性。 |
| **教学与评测闭环** | 仿真与作业管理脱节，需要多平台切换 | **JupyterHub + nbgrader + 物理仿真一站式集成**，从注册到仿真再到作业提交全程闭环 | 教师一键下发带滑块的交互式作业，学生完成后在线提交，系统自动评分并回收。 |
| **自动组网与同步** | 需要手动配置交换区权限、手动维护学生数据库 | **基于课程 ID 的动态挂载与自动同步**，自动拉取 Hub 用户组同步 gradebook | 极大地简化了系统运维和教师的教学管理成本，真正做到“零配置”。 |
| **界面与品牌定制** | 原生 JupyterLab 界面，带有强烈的“程序员开发工具”风格 | **深度定制的 UI 视觉系统**：SimLab 专属文件图标、高亮着色的侧边栏、隐藏不必要链接的极简模板、全局中文语言包 | 提供了极具科技感与和谐感的工业级产品视觉体验。 |

---

## 六、 未来展望与演进方向

目前项目已完成全部核心业务逻辑开发与 nbgrader 的无缝整合，系统运行稳定，交互逻辑流畅。未来的主要工作将集中在：
1.  **主 Logo 视觉设计收尾**：目前已替换了 favicon、文件、Notebook、文件夹等图标，主 Logo（Wordmark）即将进行最后的美化和集成。
2.  **高性能并行计算支持**：对于复杂的二维瞬态导热或大规模有限差分计算，可引入 WebAssembly（Wasm）加速或 IPython 并行内核，提升极端参数下的滑块拖动流畅度。
3.  **更丰富的学科模板扩充**：将仿真模板进一步拓展到流体力学、化工过程控制、电力系统潮流计算等领域，将 SimLab 打造为通用的工程学科在线教学云平台。
