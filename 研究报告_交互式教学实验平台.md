# 基于容器化架构的交互式教学实验平台设计与实现

## 摘要

本文设计并实现了一个面向高校理工科课程教学的**交互式实验平台**。平台采用 B/S 架构，以 Docker 容器化技术为底座，构建了多用户隔离的在线编程与实验环境。系统由三大核心引擎组成：**EduPortal 教学门户引擎**负责用户认证、角色管理与容器编排；**InteractIDE 交互式集成开发环境**提供基于浏览器的代码编辑、执行与可视化界面；**AutoGrader 智能作业评测系统**实现了作业全生命周期的自动化管理。此外，平台自主开发了 **ThermalLab 热工仿真实验模块**，涵盖稳态导热、瞬态导热、对流换热、热辐射四大类共 14 个典型传热学仿真场景，支持参数化配置与实验报告自动生成。整个平台全面实现了简体中文本地化，并采用现代化 UI 设计语言，构建了一套兼具学术性和工程实用性的智能化教学解决方案。

**关键词**：容器化教学平台；交互式编程环境；自动评分系统；传热学数值仿真；角色权限管理

---

## 第一章 绪论

### 1.1 研究背景

随着高校教学信息化改革的深入推进，传统的线下编程教学和实验教学模式面临诸多挑战：实验环境配置繁琐、学生端环境不一致导致代码无法复现、作业收集与评阅人工成本高、工程仿真实验受限于本地算力等。因此，亟需一种云端化、标准化、智能化的教学实验平台，以解决上述痛点。

### 1.2 研究目标

本研究旨在设计并实现一套完整的交互式教学实验平台，具体目标包括：

1. **多用户隔离**：每位用户拥有独立的容器化运行环境，互不干扰
2. **角色权限分离**：实现教师与学生的差异化功能权限管理
3. **作业自动化**：构建从创建、分发、提交到自动评分的作业全流程管理系统
4. **工程仿真集成**：在平台内嵌入面向传热学课程的交互式仿真实验模块
5. **全面本地化**：提供完整的简体中文用户界面

### 1.3 论文组织结构

本文共分为六章。第一章阐述研究背景与目标；第二章介绍系统总体架构设计；第三章详细描述各核心模块的设计与实现；第四章介绍 ThermalLab 热工仿真实验模块的算法与实现；第五章总结系统的关键技术与创新点；第六章为总结与展望。

---

## 第二章 系统总体架构设计

### 2.1 架构概述

本平台采用**分层架构**设计，自底向上共分为四层：基础设施层、核心服务层、功能模块层和用户界面层。各层之间通过标准化 API 进行通信，实现了松耦合和高扩展性。

```
┌─────────────────────────────────────────────────────────────┐
│                     用户界面层 (UI Layer)                     │
│  ┌─────────┐  ┌──────────┐  ┌──────────┐  ┌────────────┐  │
│  │ 登录注册 │  │ 主控面板  │  │ 实验环境  │  │ 仿真工作台 │  │
│  └─────────┘  └──────────┘  └──────────┘  └────────────┘  │
├─────────────────────────────────────────────────────────────┤
│                   功能模块层 (Module Layer)                   │
│  ┌─────────────┐  ┌────────────────┐  ┌────────────────┐   │
│  │  AutoGrader  │  │   ThermalLab   │  │  权限控制模块  │   │
│  │ 智能评测系统  │  │ 热工仿真模块   │  │  RBAC 管理    │   │
│  └─────────────┘  └────────────────┘  └────────────────┘   │
├─────────────────────────────────────────────────────────────┤
│                   核心服务层 (Core Layer)                     │
│  ┌──────────────────┐       ┌──────────────────────────┐   │
│  │    EduPortal      │       │      InteractIDE          │   │
│  │  教学门户引擎     │       │  交互式集成开发环境        │   │
│  │ (认证·编排·调度)  │       │ (编辑·执行·可视化)        │   │
│  └──────────────────┘       └──────────────────────────┘   │
├─────────────────────────────────────────────────────────────┤
│                  基础设施层 (Infrastructure Layer)            │
│  ┌───────────┐  ┌──────────────┐  ┌────────────────────┐   │
│  │  Docker    │  │ 持久化存储    │  │  网络与反向代理    │   │
│  │  容器引擎  │  │ (Volume/DB)  │  │  (HTTP Proxy)     │   │
│  └───────────┘  └──────────────┘  └────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 技术选型

| 层级 | 技术方案 | 选型理由 |
|------|---------|---------|
| 基础设施层 | Docker 容器引擎 | 轻量级虚拟化，秒级启动，资源隔离 |
| 后端语言 | Python 3 | 科学计算生态丰富，Web 框架成熟 |
| Web 框架 | Tornado | 异步非阻塞 I/O，高并发 WebSocket 支持 |
| 前端框架 | TypeScript + React 18 | 类型安全，组件化开发，丰富的生态系统 |
| 窗口管理 | Lumino Widget 框架 | 高性能多标签页/面板管理，支持拖拽布局 |
| 数值计算 | NumPy / SciPy / Numba | 高性能矩阵运算，JIT 编译加速 |
| 数据可视化 | Matplotlib / ECharts | 科学绘图 + 交互式图表 |
| 数据库 | SQLite + SQLAlchemy | 轻量级关系型数据库，ORM 映射 |
| 模板引擎 | Jinja2 | 服务端 HTML 渲染与模板继承 |
| 包管理 | Conda (Mamba) / pip / npm | 多语言依赖管理 |

### 2.3 用户角色模型

系统定义了三种用户角色，各角色的功能权限如下：

| 功能 | 管理员 | 教师 | 学生 |
|------|:------:|:----:|:----:|
| 用户管理与系统配置 | ✅ | ❌ | ❌ |
| 创建课程与作业 | ✅ | ✅ | ❌ |
| 评分面板（批改作业） | ✅ | ✅ | ❌ |
| 领取、完成、提交作业 | ✅ | ✅ | ✅ |
| 交互式编程环境 | ✅ | ✅ | ✅ |
| 热工仿真实验 | ✅ | ✅ | ✅ |

---

## 第三章 核心模块设计与实现

### 3.1 EduPortal 教学门户引擎

#### 3.1.1 模块定位

EduPortal 是平台的**入口网关与调度中枢**，承担用户认证、会话管理、角色分组、容器生命周期管理等核心职责。它是整个平台所有服务的统一入口点。

#### 3.1.2 技术实现

EduPortal 基于 **Python + Tornado 异步 Web 框架**开发。Tornado 的非阻塞 I/O 模型使其能够高效处理大量并发的 WebSocket 长连接和 HTTP 请求。具体技术构成如下：

| 组件 | 技术实现 |
|------|---------|
| HTTP 服务与路由 | Tornado Web Application，基于 `RequestHandler` 的路由分发 |
| 用户认证 | 自定义认证模块，基于 Tornado `RequestHandler` 扩展开发，支持用户名/密码注册与登录 |
| 会话管理 | 基于 Cookie 的安全会话 + XSRF Token 防护 |
| 数据持久化 | SQLite 数据库存储用户、分组、Token 等信息 |
| 容器编排 | 通过 Docker Remote API 动态创建/销毁用户容器 |
| 反向代理 | 内置可配置 HTTP 代理（Configurable HTTP Proxy），将请求按用户路由到对应容器 |
| API 服务 | RESTful API 提供用户管理、分组管理、服务状态查询等接口 |
| RBAC 权限 | 基于角色（Role）和作用域（Scope）的细粒度权限控制 |

#### 3.1.3 自定义认证模块

本文开发了 `CustomAuthenticator` 认证类，在标准用户名/密码认证基础上增加了以下功能：

**（1）角色感知注册**

注册表单中包含角色选择字段（教师/学生）和课程 ID 字段。系统在用户创建时自动完成分组分配：

- **教师注册**：自动创建并加入 `teacher-{course_id}` 课程管理组和全局 `teachers` 组
- **学生注册**：自动加入 `student-{course_id}` 课程学习组

**（2）中文消息本地化**

重写 `get_result_message` 方法，将所有认证反馈消息（注册成功、用户名已存在、密码不匹配等）翻译为简体中文。

**（3）自定义模板加载**

通过 Jinja2 的 `ChoiceLoader` 机制，将自定义模板目录注册到模板加载器链的最前端，实现模板的无缝覆盖。

#### 3.1.4 容器编排机制

EduPortal 通过 Docker Remote API 实现用户容器的全生命周期管理：

```
用户登录 → EduPortal 认证通过
    → 执行 pre_spawn_hook（注入挂载卷和环境变量）
    → 调用 Docker API 创建容器
    → 启动容器并配置网络路由
    → 反向代理将用户请求转发至对应容器
    → 用户退出 → 容器自动销毁
```

`pre_spawn_hook` 钩子函数根据用户角色动态配置容器：

- **教师容器**：挂载对应课程源文件目录，注入管理员级 API Token
- **学生容器**：仅挂载共享作业交换目录，不注入管理权限
- **通用配置**：挂载用户持久化数据卷和共享作业交换目录

---

### 3.2 InteractIDE 交互式集成开发环境

#### 3.2.1 模块定位

InteractIDE 是平台的**核心用户交互界面**，运行在每个用户的独立容器内，提供基于浏览器的代码编辑、执行、文件管理和可视化功能。

#### 3.2.2 技术实现

InteractIDE 采用**前后端分离架构**，前端基于 TypeScript 和 React 开发，后端基于 Python Tornado 提供计算内核管理和文件系统服务。

| 组件 | 技术实现 |
|------|---------|
| 前端框架 | TypeScript + React 18 组件化开发 |
| 窗口管理 | Lumino Widget 框架，支持多标签页、面板拖拽、分屏布局 |
| 代码编辑器 | CodeMirror 6，支持语法高亮、自动补全、多语言模式 |
| 计算内核通信 | 基于 WebSocket 的双向实时通信协议，支持代码执行、中断、输出流 |
| 文件系统 | RESTful Contents API，支持文件的创建、读取、更新、删除和重命名 |
| 终端模拟 | 基于 xterm.js 的浏览器内终端 |
| 扩展系统 | 插件化架构，通过 `IPlugin` 接口注册功能扩展 |
| 主题系统 | 亮色/暗色主题切换，支持 CSS 变量自定义 |
| 国际化 | 简体中文语言包，覆盖所有菜单、对话框和提示信息 |

#### 3.2.3 插件化扩展架构

InteractIDE 采用高度模块化的插件架构。每个功能模块（如文件浏览器、代码编辑器、终端、仿真工作台等）均以插件形式注册到核心应用中。插件通过依赖注入机制获取所需服务（如命令面板、主菜单、文件管理器等），实现了功能的松耦合和高扩展性。

本文开发的 ThermalLab 模块即作为 InteractIDE 的一个功能插件集成到系统中（详见第四章）。

#### 3.2.4 交互式实验文档

平台使用一种**可执行的结构化实验文档格式**（`.ipynb`），将 Markdown 富文本说明、可执行代码单元格和运行输出结果整合在同一文档中。该格式具有以下特点：

- **文档与代码融合**：实验说明、公式推导、参数定义和执行结果在同一文档中呈现
- **交互式执行**：用户可逐单元格执行代码，实时查看运行结果
- **版本化管理**：文档以 JSON 格式存储，便于版本控制
- **自动化评分兼容**：文档结构支持嵌入测试用例和评分标记，与 AutoGrader 深度集成

#### 3.2.5 界面定制与品牌化

本文对 InteractIDE 进行了深度界面定制：

- **品牌 Logo**：自定义黄色渐变 "Y" 标识，通过 CSS 伪元素注入亮色和暗色主题
- **默认中文**：通过设置覆盖机制 (`overrides.json`) 将默认界面语言设为简体中文
- **扩展管控**：禁用扩展管理器和目录面板，简化学生端界面
- **Favicon**：自定义浏览器标签页图标

---

### 3.3 AutoGrader 智能作业评测系统

#### 3.3.1 模块定位

AutoGrader 是平台的**作业全生命周期管理模块**，涵盖作业创建、分发、收集、自动评分和反馈生成的完整流程。

#### 3.3.2 技术实现

AutoGrader 基于 **Python** 开发，使用 SQLAlchemy ORM 进行数据持久化，通过 Alembic 实现数据库迁移。其核心组件包括：

| 组件 | 技术实现 |
|------|---------|
| 数据库 ORM | SQLAlchemy，管理课程、作业、学生、提交记录、评分结果等实体 |
| 数据库迁移 | Alembic，支持数据库结构的版本化演进 |
| 评分面板 | Tornado Web Handler，为教师提供作业管理和批改界面 |
| 作业创建工具 (Create Assignment) | 前端扩展，支持在代码单元格中标记可编辑区域、自动评分测试和手动评分区域 |
| 作业分发与收集 | 基于共享文件系统的作业交换通道 (`Exchange`)，路径按课程 ID 隔离 |
| 自动评分引擎 | 基于 pytest 的代码测试执行框架，自动运行预定义测试用例 |
| 权限认证 | 通过 EduPortal 的用户分组信息判断教师/学生身份 |

#### 3.3.3 作业生命周期

AutoGrader 管理的作业经历以下六个阶段：

```
┌──────────┐    ┌──────────┐    ┌──────────────┐    ┌──────────┐
│  ① 创建  │───→│  ② 生成  │───→│   ③ 发布     │───→│  ④ 领取  │
│ (source/) │    │(release/)│    │ (Exchange/)  │    │ (学生端) │
└──────────┘    └──────────┘    └──────────────┘    └──────────┘
                                                         │
┌──────────────┐    ┌──────────────┐    ┌───────────┐    │
│  ⑥ 反馈生成  │←───│  ⑤ 自动评分  │←───│  提交收集  │←───┘
│ (feedback/)  │    │(autograded/) │    │(submitted/)│
└──────────────┘    └──────────────┘    └───────────┘
```

- **创建阶段**：教师在实验文档中编写题目，使用特殊标记划定学生可编辑区域和评分测试代码
- **生成阶段**：系统自动生成学生版本（隐藏参考答案和测试用例）
- **发布阶段**：发布到共享作业交换通道，按课程 ID 隔离存储
- **领取阶段**：学生通过作业列表面板领取作业副本到个人工作区
- **评分阶段**：自动执行预定义测试用例，计算得分；教师可在评分面板中进行手动复批
- **反馈阶段**：生成包含评分明细和批注的反馈文档，学生可查阅

#### 3.3.4 多课程支持

AutoGrader 通过以下机制实现多课程并行管理：

- **课程 ID 隔离**：每个课程拥有独立的作业目录和数据库记录
- **分组映射**：教师所属的 `teacher-{course_id}` 组决定其管理的课程，学生所属的 `student-{course_id}` 组决定其可访问的课程
- **Exchange 路径前缀**：作业交换通道中的路径包含课程 ID，避免跨课程冲突
- **动态课程 ID 注入**：教师容器启动时通过环境变量 `COURSE_ID` 注入课程标识

---

### 3.4 角色权限控制系统

#### 3.4.1 设计思路

平台采用**双层权限控制**策略，从服务端和客户端两个维度同时限制学生对教师功能的访问：

| 控制层 | 实现方式 | 作用 |
|--------|---------|------|
| 服务端 (后端) | 动态禁用服务器扩展 | 使教师功能的 API 端点在学生容器中不可用 |
| 客户端 (前端) | 写入页面配置文件 | 隐藏教师功能的 UI 入口 |

#### 3.4.2 服务端权限控制

在容器内的服务器配置文件中，根据环境变量 `COURSE_ID` 的有无判断用户角色：

- **存在该变量** → 教师容器 → 启用全部扩展
- **不存在该变量** → 学生容器 → 禁用评分面板和课程管理服务端扩展

#### 3.4.3 客户端权限控制

容器启动时执行预启动钩子脚本 (`before-notebook.d/`)，对学生容器写入前端配置文件，禁用以下 UI 组件：

- 评分面板
- 作业创建工具（Create Assignment）

#### 3.4.4 RBAC 角色模型

系统通过角色（Role）和作用域（Scope）机制实现细粒度权限控制：

```python
# 教师角色定义
{
    "name": "grade-teacher",
    "scopes": [
        "list:users",      # 查看用户列表
        "read:users",      # 读取用户详情
        "admin:groups",    # 管理用户分组
        "read:groups",     # 读取分组信息
        "list:groups",     # 列出分组
        "access:services"  # 访问平台服务
    ],
    "groups": ["teachers"]  # 应用于教师组成员
}
```

---

### 3.5 前端页面设计与实现

#### 3.5.1 设计语言

平台的所有用户入口页面（登录、注册、主控面板、加载等待）均采用**现代化 Glassmorphism（毛玻璃拟态）**设计风格，具备以下视觉特征：

- **毛玻璃卡片**：`backdrop-filter: blur(24px)` 实现半透明模糊效果
- **动态背景**：CSS `@keyframes` 驱动的渐变色浮动光球动画
- **渐变色按钮**：`linear-gradient(135deg, #6366f1, #38bdf8)` 紫蓝渐变
- **入场动画**：`slide-up` 弹出动画 + `fade-in` 淡入效果
- **深色主题**：`#0f172a → #1e1b4b` 深蓝紫渐变背景

#### 3.5.2 页面清单

| 页面 | 功能描述 | 关键交互 |
|------|---------|---------|
| 登录页 | 用户名 + 密码登录 | 密码可见性切换、HTTP 安全警告、2FA 验证码输入 |
| 注册页 | 角色选择 + 课程绑定的自助注册 | 角色切换动态表单、密码确认、表单验证 |
| 主控面板 | 启动/停止个人服务器 | 命名服务器管理、实时状态展示 |
| 加载等待页 | 容器启动过程的可视化反馈 | SSE 实时进度监听、自动跳转、加载动画 |

#### 3.5.3 技术栈

| 技术 | 作用 |
|------|------|
| Jinja2 模板引擎 | 服务端模板渲染与继承 |
| Google Fonts (Inter) | 现代无衬线西文字体 |
| PingFang SC / 微软雅黑 | 中文字体回退 |
| Font Awesome 6.4 | UI 图标库 |
| CSS3 动画 | 浮动光球、入场动画、加载旋转 |
| Server-Sent Events (SSE) | 容器启动进度实时推送 |
| XSRF Token | 跨站请求伪造防护 |

---

## 第四章 ThermalLab 热工仿真实验模块

### 4.1 模块概述

ThermalLab 是本平台自主设计开发的**传热学课程仿真实验模块**。该模块以 InteractIDE 插件形式集成到系统中，为学生提供交互式的工程热力学数值仿真实验环境。

ThermalLab 的核心设计理念是**"参数化配置 → 自动生成可执行实验文档 → 交互式运行与可视化"**，即用户通过图形界面配置仿真参数后，系统自动生成包含物理模型说明、数学公式推导、数值求解代码和可视化代码的完整实验文档，学生可直接运行查看结果，也可修改参数或算法进行深入探索。

### 4.2 系统架构

```
┌────────────────────────────────────────────────────────────┐
│                       前端 (TypeScript)                     │
│                                                            │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  插件入口 (index.ts)                                  │  │
│  │  · 向 InteractIDE 注册命令和菜单项                    │  │
│  │  · 命令: "打开仿真工作台"                              │  │
│  │  · 菜单: "热设计仿真系统"                              │  │
│  └──────────────┬───────────────────────────────────────┘  │
│                 │                                          │
│  ┌──────────────▼───────────────────────────────────────┐  │
│  │  仿真工作台 (MainWidget.tsx)                          │  │
│  │  · Lumino Widget 容器                                 │  │
│  │  · ReactWidget 桥接 React ↔ Lumino                   │  │
│  └──────────────┬───────────────────────────────────────┘  │
│                 │                                          │
│  ┌──────────────▼───────────────────────────────────────┐  │
│  │  参数控制面板 (ControlPanel.tsx, React 组件)           │  │
│  │  · 4 大场景类别选择                                   │  │
│  │  · 14 个仿真场景切换                                  │  │
│  │  · 动态参数表单（含标签、单位、范围校验）               │  │
│  │  · "开始仿真" → 触发实验文档生成                       │  │
│  └──────────────┬───────────────────────────────────────┘  │
│                 │                                          │
│  ┌──────────────▼───────────────────────────────────────┐  │
│  │  实验文档生成器 (NotebookGenerator.ts)                │  │
│  │  · 14 个场景的模板工厂函数                            │  │
│  │  · 生成: Markdown 说明 + 参数代码 + 求解代码 + 可视化  │  │
│  │  · 通过 Contents API 保存到 "仿真结果归档/"            │  │
│  │  · 自动在编辑器中打开生成的文档                        │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                            │
├────────────────────────────────────────────────────────────┤
│                     后端 (Python)                           │
│                                                            │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  thermal_solver 求解器包                               │  │
│  │  ├── core/base.py       # 统一入口 run_simulation()    │  │
│  │  └── models/            # 14 个场景求解器              │  │
│  │      ├── steady_conduction/   # 稳态导热 (6 个)       │  │
│  │      ├── transient_conduction/ # 瞬态导热 (2 个)      │  │
│  │      ├── convection/          # 对流换热 (4 个)       │  │
│  │      └── radiation/           # 热辐射 (2 个)         │  │
│  └────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────┘
```

### 4.3 前端实现

#### 4.3.1 插件注册

ThermalLab 通过 InteractIDE 的插件接口 (`IPlugin`) 注册到系统中，声明对命令面板 (`ICommandPalette`) 和主菜单 (`IMainMenu`) 的依赖。插件激活后，向系统注册"打开仿真工作台"命令，并在主菜单栏添加"热设计仿真系统"菜单项。

#### 4.3.2 参数控制面板

ControlPanel 组件使用 React 18 开发，核心特性包括：

- **场景分类导航**：4 大物理类别（稳态导热、瞬态导热、对流换热、热辐射），每类包含 2-6 个具体场景
- **动态参数表单**：根据所选场景自动渲染对应的参数输入控件，每个参数包含中文标签、物理单位、最小值/最大值约束
- **参数重置**：一键恢复默认参数值
- **输入校验**：实时参数范围校验，防止不合理输入

#### 4.3.3 实验文档自动生成器

`NotebookGenerator.ts` 是 ThermalLab 的核心创新组件，为每个仿真场景实现了一个模板工厂函数。生成的实验文档包含以下结构化单元格：

| 单元格类型 | 内容 |
|-----------|------|
| Markdown | 场景标题、物理模型示意图、控制方程（LaTeX） |
| Code | 参数定义（用户可修改） |
| Code | 完整数值求解算法 |
| Code | Matplotlib 可视化代码（含中文字体配置） |

### 4.4 仿真场景与数值算法

#### 4.4.1 稳态导热（6 个场景）

| 场景 | 物理模型 | 控制方程 | 数值方法 | 核心算法 |
|------|---------|---------|---------|---------|
| 无限大平板导热 | 一维恒温边界 | d²T/dx² = 0 | FDM 有限差分法 | 三对角矩阵 + `np.linalg.solve` |
| 多层复合平板导热 | 多层异质材料 | 含界面热阻 | FVM 有限体积法 | 界面导热系数调和平均 |
| 圆筒壁径向导热 | 柱坐标系 | (1/r)·d(r·dT/dr)/dr = 0 | FDM 有限差分法 | 柱坐标中心差分 |
| 等截面直肋导热 | 肋片散热 | d²T/dx² - m²(T-T∞) = 0 | FVM 有限体积法 | 含对流源项的修正方程组 |
| 平壁内热源导热 | 含体积热源 | d²T/dx² + qᵥ/k = 0 | FDM 有限差分法 | 泊松方程离散 |
| 二维矩形平板导热 | 二维稳态 | ∂²T/∂x² + ∂²T/∂y² = 0 | FDM 有限差分法 | 五点差分格式 |

**FDM 离散示例（无限大平板）**：

将区域 [0, L] 均匀划分为 N-1 段，对内部节点 i 用中心差分近似二阶导数：

$$T_{i-1} - 2T_i + T_{i+1} = 0 \quad (i = 1, 2, \ldots, N-2)$$

加上边界条件 T₀ = T_h, T_{N-1} = T_c，组装为 **三对角线性方程组** [A]{T} = {b}，调用 `np.linalg.solve` 求解。每个场景均同时计算解析解用于精度验证。

#### 4.4.2 瞬态导热（2 个场景）

| 场景 | 物理模型 | 控制方程 | 数值方法 | 稳定性条件 |
|------|---------|---------|---------|-----------|
| 两侧恒温加热 | 突然施加恒温 | ∂T/∂t = α·∂²T/∂x² | FTCS 显式格式 | Fo = α·Δt/Δx² ≤ 0.5 |
| 一侧恒定热流 | 恒定表面热流 | ∂T/∂t = α·∂²T/∂x² | 隐式 FVM | 无条件稳定 |

**FTCS 显式格式**：

$$T_i^{n+1} = T_i^n + \text{Fo} \cdot (T_{i-1}^n - 2T_i^n + T_{i+1}^n)$$

其中 Fo 为网格 Fourier 数。求解器自动检查稳定性条件，当 Fo > 0.5 时自动减小时间步长（取 Fo = 0.4 留安全裕度）。瞬态场景支持多时间快照输出，并与傅里叶级数解析解对比验证。

#### 4.4.3 对流换热（4 个场景）

| 场景 | 流动类型 | 核心关联式 |
|------|---------|-----------|
| 平板层流强制对流 | Re_L < 5×10⁵ | Nu_x = 0.332·Re_x^{1/2}·Pr^{1/3} (Blasius) |
| 平板湍流强制对流 | Re_L ≥ 5×10⁵ | 湍流 Nusselt 数关联式 |
| 竖板自然对流 | Gr·Pr 驱动 | Grashof / Rayleigh 数关联 |
| 圆管内强迫对流 | 管内流动 | Dittus-Boelter 关联式 |

对流场景基于**工程经验关联式**求解，沿流动方向计算 Re_x、Nu_x、h_x、δ(x) 的分布曲线，同时验证 h_avg = 2·h_x|_{x=L} 等理论关系。

#### 4.4.4 热辐射（2 个场景）

| 场景 | 物理模型 | 核心公式 |
|------|---------|---------|
| 平行平板辐射换热 | 两无限大平行灰体平板 | q = σ·ε_eff·(T₁⁴ - T₂⁴), ε_eff = 1/(1/ε₁+1/ε₂-1) |
| 三表面封闭空腔辐射 | 三灰体表面围成的封闭腔 | 基于角系数和辐射网络法的方程组求解 |

辐射场景实现了以下分析功能：

- **遮热板效应分析**：计算插入 N 个遮热板后的辐射削减率
- **发射率敏感性分析**：绘制发射率 ε₁ 从 0.05 到 1.0 变化时的辐射热流分布曲线
- **黑体极限对比**：计算实际辐射量与黑体极限的比值

### 4.5 求解器统一架构

所有 14 个场景求解器遵循统一的接口规范：

```python
def solve_xxx(params: dict) -> dict:
    """
    输入: params — 参数字典
    输出: {
        "indicators": { ... },   # 工程物理量（关键结果指标）
        "chart_data": {          # 可视化数据
            "x": [...],          # x 轴数据
            "y": [...],          # y 轴数据
            "x_label": "...",    # x 轴标签
            "y_label": "...",    # y 轴标签
            "title": "..."       # 图表标题
        }
    }
    """
```

求解器通过 `ROUTER` 字典实现场景 ID 到求解函数的映射，`run_simulation()` 作为统一入口函数进行分发调用和异常处理。

---

## 第五章 关键技术与创新点

### 5.1 关键技术

| 技术领域 | 具体实现 |
|---------|---------|
| **容器化隔离** | 基于 Docker 的多用户沙箱环境，每用户独立容器，容器间网络/文件系统互不可见 |
| **动态容器编排** | pre_spawn_hook 机制，根据用户角色动态注入挂载卷、环境变量和 API Token |
| **双层权限控制** | 服务端扩展禁用 + 客户端 UI 隐藏，双重保障功能隔离 |
| **多课程并行管理** | 基于用户分组的课程路由，Exchange 路径隔离，支持多教师多课程共存 |
| **可执行实验文档自动生成** | 前端参数 → 自包含实验文档（Markdown + 代码 + 可视化），兼顾交互性和可编辑性 |
| **数值解析双重验证** | 每个数值仿真场景均提供解析解对比，确保计算精度可追溯 |

### 5.2 创新点

1. **角色感知的自助注册机制**：用户注册时即完成角色绑定和课程分组，无需管理员手动干预，降低了平台运维成本

2. **自包含实验文档自动生成**：不同于传统仿真软件仅输出结果，ThermalLab 生成的实验文档包含完整的物理模型说明、数学推导、求解算法和可视化代码，学生可自由修改探索，兼具教学性和工程实践性

3. **双层权限控制架构**：服务端 + 客户端双重权限隔离，既保证了安全性（后端 API 不可调用），又保证了用户体验（前端 UI 不可见）

4. **统一求解器接口规范**：14 个场景求解器遵循统一的输入/输出结构，实现了场景的快速扩展和前端的统一适配

5. **全面简体中文本地化**：从平台入口页面到编程环境界面、从仿真参数标签到结果输出，实现了端到端的中文化

---

## 第六章 总结与展望

### 6.1 总结

本文设计并实现了一套基于容器化架构的交互式教学实验平台，主要完成了以下工作：

1. 开发了 **EduPortal 教学门户引擎**，实现了基于 Tornado 异步框架的多用户认证、RBAC 权限管理和 Docker 容器编排功能
2. 构建了 **InteractIDE 交互式集成开发环境**，基于 TypeScript/React/Lumino 技术栈实现了浏览器端的代码编辑、执行和可视化界面
3. 开发了 **AutoGrader 智能作业评测系统**，基于 SQLAlchemy/Alembic 实现了作业从创建到评分反馈的全生命周期自动化管理
4. 自主设计并实现了 **ThermalLab 热工仿真实验模块**，覆盖传热学四大领域共 14 个典型仿真场景，采用 FDM、FVM、FTCS 等数值方法并提供解析解验证
5. 实现了全平台简体中文本地化和现代化 UI 设计

### 6.2 展望

未来工作可从以下方向继续拓展：

1. **扩展仿真场景**：增加流体力学、结构力学等更多学科的仿真模块
2. **GPU 加速**：利用 CUDA/Numba GPU 加速大规模二维/三维仿真计算
3. **协作功能**：实现多用户实时协作编辑和讨论
4. **学习分析**：基于学生的作业数据和实验操作轨迹，构建学习行为分析和个性化推荐系统
5. **移动端适配**：优化移动设备的界面交互体验
